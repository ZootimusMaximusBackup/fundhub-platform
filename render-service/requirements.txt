# Pinned on purpose. A floating version here means the documents can change
# shape on a redeploy nobody triggered.
#
# weasyprint 69.0 is the version the reference documents (12 / 9 / 9 / 14 pages)
# were produced with. Do not bump it without re-running the page-count check in
# README.md step 8.
weasyprint==69.0

# The generator draws a QR code on the call-to-action page. `qrcode.make(...)`
# returns a PIL image and calls .save(), so Pillow is required, not optional.
qrcode==8.2
pillow==12.3.0

# Production WSGI server. Only used inside the container; running wsgi.py
# directly uses the standard library server instead.
gunicorn==26.2.0
