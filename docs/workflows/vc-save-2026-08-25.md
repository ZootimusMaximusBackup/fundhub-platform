# Version control save — 2026-08-25

Chris asked to look at all threads and put saveable work on GitHub so a machine wipe cannot eat it. This is the record.

**Remote:** https://github.com/ZootimusMaximusBackup/fundhub-platform/tree/vc/save-2026-08-25

Do **not** merge this whole pile onto `main`. It is a backup of mixed thread work.

**Rule-only PR (open, not merged):** https://github.com/ZootimusMaximusBackup/fundhub-platform/pull/136 — Full End-To-End Audit owner law + URL-first, cherry-picked onto current `origin/main` (includes PR 135).

## Dirty product vs evidence vs agent files (measured)

At start, this laptop was on `soft-pull-multi-biz` (`4b4105e5`), behind `origin/soft-pull-multi-biz` by 3 (those 3 are already on GitHub and merged in PR 135).

| Kind | What it was | What we did |
|---|---|---|
| Product + rules + boards | ~90 changed tracked files + 48 small new files | Committed on `vc/save-2026-08-25` in 11 small units (this board is the last) |
| Evidence dumps | 6671 files, about **1.5 GB**, under `docs/workflows/*-evidence/` | **Not committed.** Already gitignored. Still on this disk. |
| Secrets | `.env`, `credentials/` | **Not committed.** Already gitignored. Names only. |
| Agent transcripts | 238 parent chats under `~/.cursor/projects/.../agent-transcripts/` (outside this repo) | **Not committed.** Still only on this Mac. |

Untracked evidence count in `git status`: **0** (ignored). On disk the dumps are still there.

## What threads left on disk

Recent chats that match the dirty tree (parent ids, cite as usual):

- [Full End-To-End Audit](60850960-8305-49a9-9d01-94bc893448bb) and [Full End-To-End Audit](c358863f-38ad-4b09-b6de-735fcab93fd2) — rule that worked
- [Deep coverage QA](acfb512a-60cf-453a-985f-9bcdce0ff68f)
- [Affiliate + Facebook](7ffc779e-58e2-43e5-9022-837c5c841ab3)
- [Credit Comeback short link](13865a59-67b3-4634-8fe0-7e3b664a3f2d)
- [Commas package copy](c630d4f0-9dbc-475a-ae0e-20961ef79313)
- [Notion scrape](9ed87b3d-4454-4aa7-a92b-001fec454215)
- Company Brain / Meet / closer pack / affiliate rates / employee ramp boards dated 2026-08-24

Cloud / PR: **PR 135** (soft-pull multi-biz) already merged to `main` (`cc037f02` / `3b697681`). Rule-only follow-up is **PR 136**.

## Stashes (34) — do not drop

Oldest theme: `stash@{33}` agent-editor cleanup parked during other work.

Newest: `stash@{0}` `comms-build-wip-before-main-switch` (on `feat/commission-payout-crm`).

Also parked: repair WS-B/C/D/E, inquiry-removal, company-brain, controls, chat-widget, cloud-agent move.

**Restore (keep the stash):**

```text
git stash apply stash@{N}
```

Do **not** `git stash drop` unless Chris names that stash.

## Branches + ahead/behind (after fetch)

| Branch | Remote | Note |
|---|---|---|
| `vc/save-2026-08-25` | this save | Backup. Still on GitHub. Do not delete. Do not merge onto `main`. |
| `pr/full-e2e-audit-rule` | PR 136 | Rule-only ship from current `origin/main`. |
| `soft-pull-multi-biz` | origin, behind 3 | Those 3 already on GitHub + in PR 135. |
| `main` (local) | origin, behind 4 | Update later with a normal pull. Not done here (would mix trees). |
| `feat/commission-payout-crm` | **no remote** | Unique local commit `e21abc7c`. Pushed as its own branch so it is not only local. |
| `fix/staff-profile-edit` | ahead 2, behind 1 | Unique tip already lives on `origin/fix/staff-start-date-display`. |
| `fix/ci-green-main` | on origin | Already on GitHub. |
| `feature/repair-ws-b/c/d/e` | on origin | Already on GitHub. Worktrees clean. |

Worktrees (all clean, already tracking origin):

- `/Users/zootimusmaximus/fundhub-platform-ws-b-engine`
- `/Users/zootimusmaximus/fundhub-platform-ws-d-emails`
- `/Users/zootimusmaximus/fundhub-platform-ws-e-dashboard`
- `/private/tmp/fundhub-staff-edit`
- `/Users/zootimusmaximus/fundhub-platform-pr-e2e-audit` (PR 136)

## What we committed (this save branch)

Base: `4b4105e5` (already in `main` via PR 135).

1. `20cc5ab6` — Full End-To-End Audit rule + URL-first UX guidance
2. `ad234d66` — Aug 24–25 workflow boards
3. `fdaabcc9` — closer ramp / playbook company-resource docs
4. `c25406b2` — leftover multi-business soft-pull after PR 135
5. `df0b1334` — affiliate rates, offers, `/go` short link
6. `76922ffe` — Company Brain meet-transcript + Notion scrape
7. `5a5cd679` — closer drills, ramp quizzes, sales-floor
8. `e87745bc` — Meta agency + social OAuth
9. `a6bc34b8` — Commas / money-chain copy
10. `c9653299` — leftover portal / doc-check / upload doors
11. (this file) — this board

## What we did **not** commit (still on disk)

- **Evidence trees** (~1.5 GB, 6671 files). GITIGNORE / ARCHIVE. Screenshots and prove dumps stay on this Mac.
- **`.env` and `credentials/`**. Secrets. Never git.
- **34 stashes.** Still in this repo’s stash list. Not dropped.
- **Cursor chat transcripts** in `~/.cursor/projects/.../agent-transcripts/`. Outside the repo. A wipe of this Mac loses them unless Cursor cloud sync has a copy.
- The 3 already-pushed soft-pull commits on `origin/soft-pull-multi-biz` / `origin/main` — already safe on GitHub; not re-copied into this branch.

## Risk of loss (one sentence)

Product and boards from the dirty tree are on GitHub on `vc/save-2026-08-25`; a machine wipe still eats evidence dumps, secrets folders, stashes, and local Cursor chats.

## Next action for Chris

Review **PR 136** (rule only). Leave `vc/save-2026-08-25` as the backup. Do not merge the whole pile onto `main`.
