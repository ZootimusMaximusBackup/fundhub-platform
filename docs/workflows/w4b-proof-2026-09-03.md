# W4B — the client portal, ROUND 2 rows 14 to 18

**Branch** `fix/r2-w4b-portal` · **Date** 2026-09-03 · **Pictures** `docs/workflows/w4b-proof-2026-09-03/`

**COMPLIANCE REVIEW REQUIRED — consent capture, dispute logic.**

---

## The short version

Five things were on my list. **One was still broken. Four were already fixed** by the
earlier batch and had simply never been opened in a browser to check. So I fixed the one,
and I put all five in front of a real browser and photographed them.

The one that was broken: **a customer who bought the funding package could not sign the
form that lets us send their dispute letters.** The form was hidden from them. Their
package contains those letters. So the letters could not go out.

---

## What each row turned out to be

| # | The complaint | What I found | What I did |
|---|---|---|---|
| 14 | The "sign to authorize dispute letters" form shows to people who only bought a course | Half fixed. Course buyers were correctly shut out. **Funding buyers were shut out too, and they should not be.** | Fixed it. Repair and funding both see the form now. Courses and e-products still do not. |
| 15 | The portal says "your call is next" after the call already happened | Already fixed. | Photographed it working. |
| 16 | The advisor panel shows no name | Already fixed. | Photographed it working. See the honest gap below. |
| 17 | Something you already bought still shows as a locked upsell | Already fixed. | Photographed it working. |
| 18 | "No messages yet." shows twice | Already fixed. | Photographed it working. |

"Already fixed" means fixed by commit `a733801b` on 2026-09-03, not by me. The
before-and-after pictures for rows 15 to 18 look the same on purpose — nothing about
them changed in this branch.

---

## Row 14, in plain words

Everybody who buys credit work has to sign one form first. It says we may prepare
dispute letters from their credit file. Without that signature we send nothing.

The earlier fix asked one question to decide who sees that form: **"is this a repair
customer?"** That answer is correct for repair. It is wrong for funding, because a
funding customer is not a repair customer — and the funding package we build for them
still contains dispute letters. So every funding customer had the form hidden from them.

Chris set the rule on 2026-09-03: *"It's only for repair and for the funding offer. If
they're getting deliverables, meaning e-products and courses, they don't need to sign
for shit."*

So the question is now **"did this person buy repair, or did they buy the funding
package?"** Both see the form. Nobody else does.

### One trap I stepped around

There is a second, easier-looking way to answer "is this a funding customer": look at
the grade our credit engine gives them after a credit pull. **That would have put the
form straight back in front of the course buyer this whole finding started with.** That
grade gets stamped on anyone who has a credit pull, including people who only bought a
course. So the code looks at **what they paid for**, never at the grade.

There is a test that fails if anyone changes that later.

### Two doors, not one

The form is hidden on the screen, **and** the server refuses the signature. Hiding
something is a courtesy; the refusal is the actual lock. Both use the same rule, from
the same one file, so they cannot drift apart.

If anything at all goes wrong — no answer from the database, an older version of the
site — the form stays **shut**. A course buyer wrongly asked to authorize credit
disputes is the bad outcome. A repair customer we have to send a link to by hand is the
harmless one.

---

## The pictures

`docs/workflows/w4b-proof-2026-09-03/shots-before/` and `.../shots-after/`. Every picture in
those two folders is already marked with a red box, a number, and a caption legend at the
bottom. The untouched originals are in each folder's `_raw/`, so the marks can be redrawn
with `python3 _apply-marks.py shots-before` without taking the screenshots again.

| Picture | Shows |
|---|---|
| `14a-course-buyer` | A course buyer. No sign-here form, before and after. Correct both times. |
| `14b-funding-buyer` | **The fix.** BEFORE: the form is missing entirely. AFTER: it is there. |
| `15-stage-after-signing` | A client who has been credit-pulled, had their call and signed. The page says "After your call" and no longer says "we have not run those yet". |
| `16a-advisor-named` | The advisor's name where the blank used to be. |
| `16b-advisor-empty` | Nobody assigned yet — it says so in words instead of showing a dash. |
| `17-owned-offer` | Capital Academy, already owned, priced "Included — you own this". The one next to it, not owned, still says "On your call". |
| `18-messages-one-empty` | The Messages tab. One "No messages yet.", not two. |

The pictures come from `e2e/w4b-portal-walk.spec.mjs`, which loads the real page in a
real browser. The red boxes are drawn from where the browser actually put each element,
so a box can never be pointing at nothing.

---

## What I checked, and how hard

**In a browser** — `E2E_PORT=43219 npx playwright test w4b-portal-walk client-portal-ux` — **27 of 27 pass** (10 of them mine). Before the
fix, 9 of 10 passed: the failure was the funding buyer not being asked to sign.

**Against a real database** — `src/http/portal-summary-stage.pg.test.mjs`, **4 of 4
pass.** This one is the important one. It puts real rows in real tables, one at a time,
and asks the real page-data service what it now says:

* a credit pull lands → the page stops saying "before your call"
* a closer writes down how the call went → the page says the call happened
* an agreement is signed → the page says so, and does **not** claim they paid
* money posts → the page says they paid

Then it does the same with what the customer bought: nothing → no form. A course → still
no form, **even though their stored grade says FULL_FUNDING**. The funding package → the
form. Repair → the form.

**Without a database** — `src/consent/dispute-consent.test.mjs`, 11 of 11.

**The whole test suite.** The normal half: 8,608 tests, 0 failures, 3 skipped. The
database half, on a clean scratch database of my own: **15 failures on my branch and the
exact same 15 on `main`, name for name, none of them anywhere near this work.** I built a
copy of `main` and ran it side by side to be sure. Zero new failures.

**Lint** clean. **Journeys**: `npm run journeys` regenerates them and produced no
change, which is correct — no route moved and no route's permissions moved.

### A trap the next person will hit

**Two agents running browser tests at the same time will hand each other the wrong
files.** The browser-test config uses one fixed port (43117) and is set to re-use a
server that is already listening on it. When another workflow's copy of the repo has its
test server up, your tests silently load *their* copy of the page and dozens of tests
fail for no reason. It happened to me: 70 failures in one run, all of which pass alone.

The fix is one word on the command: `E2E_PORT=43219 npx playwright test …`. Everything
reported above was measured that way, on a port nobody else was on.

---

## Two honest gaps

**1. Nobody can assign an advisor.** Row 16 asked for the advisor's name to show. It
does. But **nothing anywhere in this system ever puts an advisor on a client's file.**
The place to store it exists and is never written to. So the name only appears when a
funding advisor happened to log a call on that file; for almost everyone the honest
answer is "Not assigned yet", and that is what they see.

Building an assignment step is not in the client journey document and I did not invent
one. It needs Chris to say who assigns, when, and from where.

**2. The database half of the suite has 15 old failures.** They are on `main` already
and none of them touch the portal. Listed by name in my hand-off so nobody has to
re-derive them.

---

## Files

| File | What changed |
|---|---|
| `src/consent/dispute-consent.mjs` | New. The one place that answers "may this client be asked to sign?" |
| `src/consent/dispute-consent.test.mjs` | New. 11 tests, including the course-buyer-with-a-funding-grade trap. |
| `api/read/portal-summary.mjs` | Returns `dispute_consent` beside the existing `repair_path`. |
| `api/consent/capture.mjs` | Refuses a signature by the new rule instead of the narrower old one. |
| `public/app/client-portal.html` | Shows the form on the new answer. Body class renamed to match. |
| `src/http/client-portal-walk-fixes.test.mjs` | The pinned check now pins the wider rule, and fails if anyone puts it back on `repair_path`. |
| `src/http/portal-summary-stage.pg.test.mjs` | New. The real-database proof of the stage, the advisor and the consent gate. |
| `e2e/w4b-portal-walk.spec.mjs` | New. The browser proof, and what draws the red boxes. |
| `docs/workflows/w4b-proof-2026-09-03/` | The pictures and the marking script. |
| `docs/journeys/CHANGELOG.md` | One entry appended. |

`src/repair/on-repair-path.mjs` was **not** touched — another workflow owns it this
batch, and its own question has not changed.
