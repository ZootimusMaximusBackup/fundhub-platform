# Named builds — 2 leftover items

**Do not run these while the 19 holes are still open.** Finish `docs/workflows/nineteen-holes-2026-08-26.md` first. Then one new chat per box.

This is **not** the 19 audit holes. Only these two leftover builds. Oxylabs is **already done** (owner 2026-08-26). Everything else that used to sit on this board is out — not specced.

---

## START HERE

Open a new chat.
Copy **one** numbered box.
Paste.
That chat owns that item only.

---

## Shared laws

- One item per chat. Three steps in that same chat, in order: VERIFY → BUILD or PROVE → FINISH. Never skip VERIFY.
- VERIFY first. If it is already built and working on live `https://fundhub.ai`, write **ALREADY BUILT** and **STOP**. Do not rebuild.
- After FINISH, STOP. Do not start another item.
- BUILD = new screen or new feature. Load `.cursor/skills/fundhub-builder/SKILL.md`.
- WIRE / small hook = load `.cursor/skills/fundhub-fixer/SKILL.md`. Smallest diff.
- PROVE boxes: walk the live path. Do not greenfield-rebuild. Only a tiny wire if VERIFY named that exact missing paint.
- Isolated worktree off `origin/main`. Never merge `vc/save-2026-08-25` or `gitbutler/workspace`.
- Staff: `chris@fundhub.ai` + `STAFF_E2E_PASSWORD` from `.env`. Live `https://fundhub.ai`. You click. Do not ask Chris.
- Agent phone `+16616054248` only. Never pulse last-four 0865.
- Hard stops: no live CRS / $32 pull, no card charge, no paper mail / PostGrid, no ClickFunnels apply, no wipe, no secret rotate, no CLI-deploy if it will break `pg` bundling.
- `INNGEST_EVENT_KEY` stays ON. Never `verify:e2e` on live DB.
- Talk at 5th grade. Claim this item on this board. Write the result.

---

## 1 — BUILD Machine learning outcome storage

```
THIS THREAD IS ONLY ITEM 1 — BUILD machine learning outcome storage.
Not a chat bot. Not “AI match.” Chris corrected that (2026-08-25 11:09pm). Over time we use saved yes/no to learn later plays.
Horse: Fund 614927f7-95a9-4623-86e8-cd85420d9716 for file look only. Do not touch the other boxes.

STEPS (same chat, in order)
1. VERIFY — find what exists in src/. Search train, feedback, outcome→play, application_decisions, src/ops/discoveries.mjs. If a real outcome→play loop already works on live, write ALREADY BUILT and STOP.
2. BUILD — only if VERIFY said missing. Isolated worktree off origin/main. Load .cursor/skills/fundhub-builder/SKILL.md for a new door, or fundhub-fixer for a stamp on a row we already write. Smallest honest first slice Chris already named: save outcomes now so later plays can read them. STOP AND ASK before a new dependency or a training job. Do not invent a new ML stack.
3. FINISH — prove the save on live (or the worktree + live desk). Click twice if UI. Write PASS / FAIL / ALREADY BUILT. STOP.

REF DOC
- src/applications/status.mjs (already writes application_decisions on yes/no)
- docs/workflows/four-builds-2026-08-25.md (B1 is only the play-name stamp — not this)

REAL: no path that stores real bank / file outcomes for later plays.
NOT A PROBLEM / ALREADY BUILT: outcomes already save and a later play can read them.

HARD STOPS
- no new ML library, no training job, no chatbot
- no live CRS / $32 pull · no card charge · no paper mail / PostGrid · no ClickFunnels apply
- agent phone +16616054248 only (never pulse 0865)
- staff from .env. Never ask Chris for secrets.
- no remint · INNGEST_EVENT_KEY stays ON · never verify:e2e on live DB · no CLI deploy that breaks pg

Claim item 1 on docs/workflows/named-builds-2026-08-26.md. Talk at 5th grade.
```

## 2 — BUILD White-label custom domain steps

```
THIS THREAD IS ONLY ITEM 2 — white-label custom domain.
Affiliate /start?ref= already PASS — do not redo that. This is partner_brand.domain.
Launch-readiness: every live domain empty or unverified. /sites/{id}/apply is the real URL today.

STEPS (same chat, in order)
1. VERIFY — Partner Brand Studio + partner_brand.domain. If a real custom domain already verifies and serves the partner page, write ALREADY BUILT and STOP.
2. BUILD — only the missing verify / show path that already exists in code. Isolated worktree off origin/main. STOP AND ASK before any DNS change or buying a domain. Do not invent demo-partner.fundhub.local as live.
3. FINISH — prove the door (verify button / honest empty). Click twice. Write PASS/FAIL. STOP.

REF DOC
- docs/workflows/launch-readiness-2026-08-26.md (Custom domain)
- docs/journeys/white-label-intended.md

REAL: no working custom-domain verify door (and /sites/ is the only URL).
NOT A PROBLEM / ALREADY BUILT: verify works, or the desk is honest that /sites/ is the URL until Chris names a domain.

HARD STOPS
- do not touch live DNS · do not buy a domain · do not redo /start?ref=
- no live CRS · no card charge · no paper mail · no ClickFunnels apply
- staff from .env. Never ask Chris.
- INNGEST_EVENT_KEY stays ON · never verify:e2e on live DB · no CLI deploy that breaks pg

Claim item 2 on docs/workflows/named-builds-2026-08-26.md. Talk at 5th grade.
```

---

## Item 1 result

**PASS.** VERIFY missing: yes/no already saved on `application_decisions`; nothing later-play code could read (including rows with no play name). `src/ops/discoveries.mjs` is sales funnel only. Built the read on `build/outcome-plays` (`src/plays/outcomes.mjs`). Live Fund Horse: 3 American Express **no** rows. Desk opened twice. No model. No train job. STOP.

## Item 2 result (was leftover item 14)

**ALREADY BUILT / PASS.** No real custom domain is verified (9 rows, 0 verified; only fake `demo-partner.fundhub.local`). Brand Studio is honest: chip **Not connected**, preview `fundhub.ai/sites/{id}`, copy says pages live at `/sites/{id}/apply` until Chris names a domain. Owner **Verify** clicked twice → **Enter a domain first.** Published sim page `/sites/aaa0a105-…/apply` is 200. Did not touch DNS. Did not rebuild.

## Claim

Statuses: `pending` · `claimed` · `ALREADY BUILT` · `PASS` · `FAIL`

| # | Kind | Item | Status |
|---|---|---|---|
| 1 | BUILD | Machine learning outcome storage | PASS |
| 2 | BUILD | White-label custom domain steps | ALREADY BUILT |
| — | PROVE | Oxylabs proxy | **ALREADY BUILT** (owner 2026-08-26) |
