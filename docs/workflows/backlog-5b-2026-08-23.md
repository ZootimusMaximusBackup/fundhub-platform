# Backlog 5B — 2026-08-23

Shared board for 5B.1 (funding pause recovery) and 5B.2 (multi-invoice payment allocation).
Owner-approved spec: `docs/workflows/backlog-spec-2026-08-23.md`.

## Tasks

| id | owner | status | notes |
|---|---|---|---|
| 5B.2-allocate | allocation agent | done | Local commit only. Do not start 5B.1. |
| 5B.1-pause-recovery | — | pending | Waits until 5B.2 tests are green. |

## Shared context

- Per-round invoicing can leave several invoices open. Nothing decided which invoice a payment applies to.
- Owner: a payment applies to the invoice for its own round. Not oldest-first.
- `invoices.funding_round_id` and `invoice_payments` already exist. Finish those, do not rebuild.
- `invoices.provider_ref` was renamed to `external_ref` in migration 031.

## Change manifests

### 5B.2-allocate (done)

**Already existed:** `invoice_payments`, `v_invoice_balance`, `invoices.funding_round_id`, `invoices.external_ref` (old `provider_ref`), AR chase already keyed on `invoiceId` / `invoice.paid`. `settleOpenSuccessFee` only settled when exactly one invoice was open.

**Added:**
- `db/migrations/257_invoice_payment_allocation.sql` — `payment_links.invoice_id`, unique `invoice_payments.external_ref`
- `src/invoices/allocate.mjs` — path 1 provider ref, path 2 round, path 3 unallocated + staff task
- AR `payment.received` now allocates instead of "only if one invoice is open"
- Checkout metadata carries `invoice_id` + `funding_round_id`; Commas adapter reads them back onto `payment.received`
- `{{balance_due}}` uses remaining balance
- Tests: exact, partial, overpay, two invoices, replay

**Journeys:** no client/owner money-flow `-actual.md` path change.

**Do not start 5B.1.**
