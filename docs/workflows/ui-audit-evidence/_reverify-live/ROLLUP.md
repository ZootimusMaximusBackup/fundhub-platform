# Live re-verify rollup — 2026-08-17 ~20:20Z

Live `https://fundhub.ai`. Stamps were not trusted. Every CONFIRMED-FIXED finding-table row (plus the two REGRESSION rows) was opened again.

**175 row verdicts:** 69 HOLDS · 74 DOES-NOT-HOLD · 3 STILL-REGRESSION · 24 UNVERIFIED · 5 MIXED (visible part holds, click not run).

Per-group tables: `command-center/verify.md` · `ops-admin/verify.md` · `sales-desk/verify.md` · `client-ops/verify.md` · `owner-admin/verify.md` · `marketing/verify.md` · `portals/verify.md`.

## Still-regression (checked first)

- Command Center: money tiles still below the fold; pipeline counts still come first.
- Ops Admin: Demo Mode still sits above Company KPIs.

## Truth fixes that hold

Fake Equifax / Kayla / 2:47 PM gone. Fake ON SHIFT chip gone. Archive gone from Pipeline. Send-reminders gone for closer. Finance OS no longer crushed. Subscriptions no longer crushed. Ironwood sample book gone. Sign license gone. Partner Verify gone. Client Text/Call gone. Automations no longer claims a run. Agent Editor unset guardrails not painted ON.

## Truth / job misses that do not hold

Hiring still shows three DEMO candidate cards with Demo Mode off. Calendar still has a Demonstration-states control. Closer Call from the nav auto-opens Rick Rockwell. Client files still 403. Unlock still says checkout is not available. Payment-success says “your payment cleared” with no check. Content primary action still does not work. Social Studio still prints `scheduler.mjs` / `screen.mjs`.

## Layout stamps that do not hold

Most §3 ≤4-size, §2 8px, §5 ≥40px, and “one filled button” CONFIRMED-FIXED rows fail again. Shared chrome (Search 12px, ⌘K 10px, Chat) keeps almost every shell at 6–8 sizes.
