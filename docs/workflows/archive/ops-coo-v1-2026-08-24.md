# Ops / AI COO v1 — shared board

**Status:** done 2026-08-24. Ready to ship, not shipped (Chris did not name deploy).

**Law:** [`fundhub-conveyor-kpis-2026-08-23.md`](fundhub-conveyor-kpis-2026-08-23.md) · backlog [`build-spec-2026-08-22.md`](build-spec-2026-08-22.md) §5B.7

**Owner-set architecture:** the brain does **not** hire, fire, raise, or bonus people itself. It writes **tasks to C-suite**. Humans act. CEO = `owner`. Chris is owner today.

| Id | What | Owner | Status |
|---|---|---|---|
| A | Daily pulse (company 8 + 20/20 bars + MODEL clocks) | this thread | done |
| B | Two briefs (CEO “needs doing” / Chris “will be done”) | this thread | done |
| C | Hire closer task + LinkedIn job post when calendar is packed | this thread | done |
| Fire shape | `createCsuiteTask({ kind: 'fire' })` — no auto-enqueue, no kick | this thread | done |
| D | Hermes | — | later — do not build |
| Later | Diagnose team gaps from the belt + 20/20 | this thread | done — notes + diagnose task when short |
| Later | Better hire profiles from those gaps | this thread | done — closer LinkedIn / FA text only |
| Later | Raises | this thread | done — shape only, no auto, no invented % |
| Later | Bonus structures | this thread | done — shape only, no auto, no invented $ |
| Later | Manage ad spend (read + review task) | this thread | done — read `ad_metrics_daily`; review if spend > 0. No buy/pause |

## Locked this pass

- Setter = AI. Never hire setters. Packed calendar hires a closer.
- Fire = C-suite task only. No `suspendStaff`. No invented “under 20 deposits → fire” rule.
- No fire trigger is locked on the conveyor page or 5B.7. Briefs say “no fire rule yet.”
- LinkedIn: reuse `src/hiring/linkedin.mjs` `postJob`. Create/post only. Do not close/unpublish.
- Invite remains the way a human gets a login. `hired` does not create a login.
- Unit times are MODEL (`src/ops/role-unit-times.mjs`). Do not invent live stopwatch times.

## Change manifest

- `src/ops/pulse.mjs`, `briefs.mjs`, `hire-closer.mjs`, `csuite-tasks.mjs` + tests
- `api/read/ops-pulse.mjs`, `api/ops/hire-closer.mjs`
- Routes `read/ops-pulse`, `ops/hire-closer`
- Ops Admin briefs card + `FHData.opsPulse` / `hireCloser`
- Journeys `ops-pulse-intended.md` + `-actual.md`
