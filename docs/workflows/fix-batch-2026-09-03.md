# Fix batch — everything the 2026-09-03 walkthrough broke on

**Source of truth for every item:** `docs/workflows/manual-walkthrough-2026-09-03.md`
(the live log, F-numbers referenced below) plus the two ground-truth specs
`expected-deliverables-repair-2026-09-03.md` and `expected-deliverables-funding-2026-09-03.md`.

**Owner rule for this batch (Chris, 2026-09-03):** notes first, then fix, then walk the
whole thing again from the ad click until it is perfect. Nothing ships without the re-walk.

**Owner premise, agreed with one exception:** fixing the shared layer once fixes it for all
five offers. The exception is fulfillment — each path has its own machinery after payment
and its own defects. Those are workflow 8 and are NOT covered by fixing Sim Five.

---

## The count

| Layer | Open items | "Fix once, fixes all five"? |
|---|---|---|
| Shared: deck, contracts, portal, CRM, messaging, payments, AI setter | **26** | Yes |
| Fulfillment: repair letters, funding applications | **11** | **No** — path-specific |
| **Product defects total** | **37** | |
| Process / test-data notes (not product) | 5 | n/a |

Retracted and NOT in the count: F6 ad naming, F12 soft-pull channel, F19 duplicate sends,
F20 Commas product label. Each is written up in the log with why it was wrong.

---

## Order of operations — what has to go first

1. **Workflow 1 (payments) goes first and alone.** Until a simulated payment can post,
   nothing paid can be verified: no entitlements, no course unlock, no money chain, no
   post-payment emails. Every other workflow can BUILD in parallel, but none can PROVE
   its paid half until W1 lands. Owner decides A or B (below) before W1 starts.
2. **Workflow 3 (contracts) needs Chris's real contract text before step 4.** Steps 1-3
   of W3 do not.
3. Everything else: **no dependencies — all parallel.**
4. **Workflow 9 (re-walk) runs last**, after every other workflow has merged.

Cap: 5 concurrent. Suggested waves: W1 alone → W2 W3 W4 W5 W6 → W7 W8 → W9.

---

## Workflow 1 — Payments unblock  (BLOCKER · 2 items · Opus)

| # | Item | Ref |
|---|---|---|
| 1 | Simulated payment cannot post: Netlify returns a MASK for `COMMAS_WEBHOOK_SECRET`, so `push-payment.mjs` signs with asterisks and the live webhook answers 401 bad_signature | F26 |
| 2 | `with-prod-env.sh` must fail loudly when a secret comes back masked | F26 |

**OWNER-SET 2026-09-03: option A, the sandbox key.** Chris: "we don't rotate keys, it's a
pain in the ass." Add a non-secret `SIM_WEBHOOK_SECRET` accepted only for receipts carrying
the `simulated` marker. Option B is dead — do not propose rotating any key (CLAUDE.md §11).

<details><summary>Copy-paste prompt</summary>

```
Repo /Users/zootimusmaximus/fundhub-platform. Read CLAUDE.md first, then
docs/workflows/fix-batch-2026-09-03.md and claim Workflow 1 on that board.

Problem: scripts/sim/push-payment.mjs posts a simulated Commas receipt to
https://fundhub.ai/api/webhooks/commas and gets HTTP 401 bad_signature. Root cause
(proven 2026-09-03, see F26 in docs/workflows/manual-walkthrough-2026-09-03.md):
COMMAS_WEBHOOK_SECRET is stored as a Netlify --secret, and `netlify env:get` returns
a 20-char MASK (mostly asterisks), so scripts/sim/with-prod-env.sh exports the mask
and the script signs with it. Signing code, header name (x-webhook-signature) and
digest (HMAC-SHA256 hex) are all correct.

Chris has chosen option A (owner-set 2026-09-03; never rotate keys). 
If A: teach src/adapters/commas.mjs verifyCommasSignature / handleCommasWebhook to
ALSO accept a second key SIM_WEBHOOK_SECRET, but ONLY when the receipt carries the
existing `simulated` marker that push-payment.mjs already stamps. Real Commas
deliveries must still verify only against COMMAS_WEBHOOK_SECRET. Set
SIM_WEBHOOK_SECRET on Netlify WITHOUT --secret (it must be readable) for production,
deploy-preview and branch-deploy. Update with-prod-env.sh to export it. Tests: a real
receipt signed with the sim key must be REJECTED; a simulated receipt signed with the
sim key must be ACCEPTED.
If B: do NOT rotate without Chris confirming in this thread that Commas has been
updated with the new value first.

Either way: add a guard to with-prod-env.sh that exits non-zero with a plain message
if any exported secret consists mostly of '*' characters.

Prove it: run `scripts/sim/with-prod-env.sh push-payment --email
stanbridgejchris+sim-05@gmail.com` and show the webhook accepting it, then show the
$5,000 Funding Mastery payment on Sim Five-Academy's portal Payments tab and the
Capital Academy tile unlocked. Never print a secret value. Definition of done per
CLAUDE.md section 6; task report per section 9. Write your manifest to the board.
```
</details>

---

## Workflow 2 — Sales deck (present.html)  (7 items · Opus · COMPLIANCE REVIEW REQUIRED)

| # | Item | Ref |
|---|---|---|
| 1 | Client slides S-03 and S-04 render raw answer-option IDs (207883 etc.) instead of survey answers, including as the hero number | F11 |
| 2 | Deck shows client "PRE-APPROVED FOR APPROXIMATELY $939,500" — stacks business funding for a client with NO business on file; engine stored $199,350; portal shows $199,350 | F15 |
| 3 | Wrap script (section 07) promises advisor, submitted applications and "we're going to get you funded" on an EDUCATION sale | F23 |
| 4 | Send buttons have no pressed/disabled/cooldown state → repeated presses send duplicate texts, emails and pay links | F24 |
| 5 | Too many competing live-action buttons after the offer is chosen; black fill is not enough emphasis | F25 |
| 6 | "307 lenders match this file" shown with NO credit pull; owner: no count may show before a pull | F10 |
| 7 | Client slides leave most of the screen empty (S-19, S-20, S-23) | F22 |

<details><summary>Copy-paste prompt</summary>

```
Repo /Users/zootimusmaximus/fundhub-platform. Read CLAUDE.md, then
docs/UI-STANDARDS.md (law for public/app/), then docs/workflows/fix-batch-2026-09-03.md
and claim Workflow 2 on that board. Read F10, F11, F15, F22, F23, F24, F25 in
docs/workflows/manual-walkthrough-2026-09-03.md for the exact observed behaviour.

Files: public/app/present.js, public/app/present.html, src/sales/closer-deck.mjs.

Fix, in this order:
1. F11 — resolve survey answer-option ids to their labels before render on every
   client slide (S-03 rows, S-04 hero and body). Grep every deck screen for the same
   leak.
2. F15 — the deck computes personal+stacked-business via applyStackedBusinessFunding
   (closer-deck.mjs:297-306) and only falls back to the stored total_funding_estimate
   when that is null. Owner verdict: $939,500 is wrong, $199,350 is right. Gate
   business stacking on a REAL business record on the client, not on a loose
   businessAgeMonths value. Make ONE figure authoritative (the stored estimate the
   portal already shows) and delete the second calculation rather than syncing them.
   Label the figure personal-only vs personal+business on screen.
3. F23 — section 07 closer script must change with the selected ladder offer. An
   education sale must not promise funding, an advisor working the file, or
   submitted applications. Check every section-07 line against all six offers.
4. F24 — every send control (Send soft pull, Send agreement + pay link, Send
   contract, Send deliverables package now, Send e-book email): pressed state on
   click, disabled while in flight, 3-5 second cooldown after success (owner's
   number), and a persistent "Sent at HH:MM" line, not just a toast.
5. F25 — once a ladder offer is selected, lead section 07 with the single correct
   action for that offer and demote or hide the rest.
6. F10 — with no credit pull on file show NO lender count (owner-set). Never a number.
7. F22 — client slides must fill/balance the slide at any zoom.

F15 and F23 are credit/funding-outcome messaging to a consumer: put
COMPLIANCE REVIEW REQUIRED at the top of your summary (CLAUDE.md section 7). That is
the marker only — no advice attached. Prove with Playwright against the live site as
Sim Five-Academy (client 823c850e-deee-4022-bf80-27ec23f77915) with annotated
screenshots per CLAUDE.md section 8. Definition of done section 6, task report
section 9, manifest to the board.
```
</details>

---

## Workflow 3 — Contracts  (5 items · Opus · needs Chris's contract text for step 4)

| # | Item | Ref |
|---|---|---|
| 1 | REMOVE the "Wording for this client" form entirely (owner-set) — one-click send, no typing | F27 |
| 2 | The typed company name became the SELLER ("we") on the contract; seller must always be Fundhub | F28 |
| 3 | No signature block inside the document body | F29 |
| 4 | Contract bodies are placeholder text — seed the REAL agreements (owner-set; Chris supplies the text) | F30 |
| 5 | Capital Blueprint has NO contract template at all (SOP step 4.2) | SOP |

<details><summary>Copy-paste prompt</summary>

```
Repo /Users/zootimusmaximus/fundhub-platform. Read CLAUDE.md, then
docs/workflows/fix-batch-2026-09-03.md and claim Workflow 3. Read F27-F30 in
docs/workflows/manual-walkthrough-2026-09-03.md.

Owner-set decisions (do not re-raise):
1. Remove the "WORDING FOR THIS CLIENT" panel from deck section 07 (Company name,
   What the program includes, Program fee, Access length, Company email, Send this
   wording). The system already holds client, offer, price and template — it
   auto-matched "Funding Mastery Program Agreement" correctly. Contract send is one
   click, no typing.
2. The seller party on every contract is Fundhub, never a staff-typed value. On
   2026-09-03 "Sim Five Academy LLC" landed as "we". Fix the template variables.
3. Add a signature block inside the document body (parties, signed-by, date) so the
   saved and downloaded copy carries it.
4. Seed the REAL contract text into contract_templates by template_key via db/seed,
   one file per agreement. Chris says the real text MAY ALREADY BE IN THE REPO —
   search docs/, db/seed/, and any contracts/ or legal/ folder first and show him
   what you found. If it is not there, it is on Chris's TODO to supply. Do NOT draft
   legal language yourself. Known keys: Funding Mastery Program Agreement (Academy
   $5,000), FUNDING-AGREEMENT ($3,000 deposit), CREDIT-REPAIR-AGREEMENT ($1,000).
   The Academy text must keep, in substance: no promise of funding, approval amount,
   credit score change or any particular result; it is an education program.
5. Capital Blueprint ($1,000) has no template at all. Ask Chris whether it needs one.

Migrations run on the production deploy only (CLAUDE.md section 11) — a seed you add
is not live until merged. Steps 1-3 can be done now; step 4 waits on Chris. Prove
with a fresh contract sent to Sim Five-Academy showing Fundhub as "we" and the
signature block in the downloaded PDF. Section 6, section 9, manifest to the board.
```
</details>

---

## Workflow 4 — Client portal  (6 items · Opus)

| # | Item | Ref |
|---|---|---|
| 1 | Funnel creates an ACTIVE account with a password nobody ever gives the client; no invite sent; self-serve reset says "ask an owner or admin"; no admin control to send one. A paying client has no way in | F31 |
| 2 | Portal still says "Your call is next" / "BEFORE YOUR CALL" / "we have not run those yet" after the pull, the call and a signed $5,000 agreement | F33 |
| 3 | "YOUR FUNDING ADVISOR — Name not shown here" | F34 |
| 4 | An offer the client has bought still shows as a LOCKED upsell "On your call" | F37 |
| 5 | Messages tab renders "No messages yet." twice | F36 |
| 6 | Dispute-letter authorization form shown to an Academy buyer — VERIFY intent with Chris before changing | F35 |

<details><summary>Copy-paste prompt</summary>

```
Repo /Users/zootimusmaximus/fundhub-platform. Read CLAUDE.md, docs/UI-STANDARDS.md,
docs/journeys/client-intended.md, then docs/workflows/fix-batch-2026-09-03.md and
claim Workflow 4. Read F31, F33-F37 in docs/workflows/manual-walkthrough-2026-09-03.md.

Live evidence 2026-09-03 for Sim Five-Academy (accounts row
c27a007f-8538-4599-aff6-391ba301dc24): kind client, status active, password_hash
present, activated_at 6:27 PM MST right after the consent form, invited_at NULL.
Client portal sign-in (public/app/client-portal.html) is email+password only, and
"Send reset link" answers "Nothing was sent. Ask an owner or admin for a reset link."
account_magic_links table and src/auth/magic-link code exist; nothing sends one.

1. F31 — decide with Chris (one question) which door is intended: magic link at
   account creation, set-your-password email, or admin-sent link. Then build THAT.
   Also add an owner/admin "send portal sign-in link" action on the Client Control
   Panel, since the reset screen tells clients to ask for one. If code requires a
   step not in docs/journeys/client-intended.md, STOP AND ASK (CLAUDE.md section 4).
2. F33 — portal stage copy must advance: after the pull, after the call, after a
   signed agreement, after payment. Trace what the header and "BEFORE YOUR CALL"
   banner read from and fix the source, not the copy.
3. F34 — show the assigned advisor, or the correct empty state if none is assigned.
4. F37 — an owned offer is never a locked upsell.
5. F36 — one empty-state row, not two.
6. F35 — ask Chris whether the dispute-letter consent belongs on every client's
   portal or only repair clients. Do not change it before he answers.

Update docs/journeys/client-actual.md and CHANGELOG in the same commit. Prove signed
in as Sim Five-Academy (stanbridgejchris+sim-05@gmail.com; Chris set the password on
2026-09-03, ask him for it) with annotated screenshots. Section 6, 9, manifest.
```
</details>

---

## Workflow 5 — CRM data and screens  (4 items · Sonnet)

| # | Item | Ref |
|---|---|---|
| 1 | Survey answers land in the WRONG fields on the Client Control Panel: verify-answer written into Business revenue, real revenue dropped, four fields blank | F8 |
| 2 | Client Control Panel does not show gate / entry / primary / secondary routing (closer dashboard does) | F9 |
| 3 | Does anything move a card Booked → Confirmed when the client RSVPs? Owner: it should be tracked | F3 |
| 4 | Ads 43, 45, 82 have no title in docs/ads/registry.json | F7 |

<details><summary>Copy-paste prompt</summary>

```
Repo /Users/zootimusmaximus/fundhub-platform. Read CLAUDE.md, docs/UI-STANDARDS.md,
then docs/workflows/fix-batch-2026-09-03.md and claim Workflow 5. Read F3, F7, F8, F9
in docs/workflows/manual-walkthrough-2026-09-03.md.

1. F8 — on public/app/client-control-panel.html the SURVEY block for Sim
   Five-Academy shows "Business revenue: Yes, both" (that is the screen-8 VERIFY
   answer) while the real screen-7 answer ($1M+) is missing and Can verify revenue /
   Personal income / Can verify income read "—". Trace the survey→custom-field
   mapping (src/adapters/clickfunnels.mjs and the cf_svy_* fields) and fix it at the
   source. Check all five sims, not just #5.
2. F9 — surface GATE / ENTRY / PRIMARY / SECONDARY on the Client Control Panel's
   "How they got here" block. The data already exists (src/ads/registry.mjs; the
   closer dashboard shows it). Display gap only — do not add a new screen (owner rule:
   no new CRM surfaces).
3. F3 — trace whether a calendar RSVP moves a pipeline card into the existing
   Confirmed column. If nothing does, report that as the finding and ASK Chris before
   building — do not add a step the intended journey does not have.
4. F7 — add titles for ads 43, 45, 82 in docs/ads/registry.json (ask Chris for the
   names).

Prove with screenshots of the Client Control Panel for all five sims. Section 6, 9,
manifest.
```
</details>

---

## Workflow 6 — Messaging timing and templates  (2 items · Sonnet)

| # | Item | Ref |
|---|---|---|
| 1 | Booking confirmation email + SMS + AI call all fire but ~3 minutes late; owner target is within 60 seconds | F1 |
| 2 | One email template's signature block renders white rectangles behind the wordmark and "Josh" image; the e-book template renders the same images cleanly | F18 |

<details><summary>Copy-paste prompt</summary>

```
Repo /Users/zootimusmaximus/fundhub-platform. Read CLAUDE.md (especially section 12
on the dispatcher and the outbound-transmission rule), then
docs/workflows/fix-batch-2026-09-03.md and claim Workflow 6. Read F1 and F18 in
docs/workflows/manual-walkthrough-2026-09-03.md.

1. F1 — booking confirmation (email, SMS, AI call) lands ~3 minutes after the
   booking. Owner target: within 60 seconds. src/messaging/dispatch.mjs is swept by
   src/workflows/message-dispatch-sweeper.mjs on cron */5 * * * * — that cadence alone
   can explain up to 5 minutes. Find the actual latency source and propose the change;
   ASK before changing the sweeper cadence or adding an immediate-send path, since
   both touch every outbound message.
2. F18 — diff the signature block between the soft-pull email template (white boxes)
   and the e-book template (clean). Fix the template, not the image assets.

No new outbound fetch anywhere outside src/messaging/providers/*. Prove F1 with
timestamps from a fresh booking. Section 6, 9, manifest.
```
</details>

---

## Workflow 7 — AI setter (Josh)  (1 item, 2 parts · Opus, high effort)

| # | Item | Ref |
|---|---|---|
| 1 | Setter "does not work AT ALL" on the live walk. Owner-set causes: model too weak → upgrade; prompt bad → rewrite. Suspected regression | F5 |

<details><summary>Copy-paste prompt</summary>

```
Repo /Users/zootimusmaximus/fundhub-platform. Read CLAUDE.md, then
docs/workflows/fix-batch-2026-09-03.md and claim Workflow 7. Read F5 and its addendum
in docs/workflows/manual-walkthrough-2026-09-03.md.

The AI setter ("Josh AI") failed completely on the 2026-09-03 live walkthrough. Chris
suspects a Claude-made regression. Owner-set decisions: (1) raise the model tier,
(2) rewrite the prompt.

Order: first get the exact symptom from Chris in this thread — call not placed,
placed but silent, hangs up, or ignores its prompt. Then find when it last worked and
diff the setter's prompt, config and workflow registration (src/workflows/index.mjs
and the setter's adapter) against that point — bisect, do not redesign. Then apply
the model upgrade. Then rewrite the prompt with Chris reviewing it in this thread.
Use the fundhub-agent-tester skill to score prompt fulfilment before and after.
Note this is the SETTER, not the booking-confirmation AI call, which did fire.
Section 6, 9, manifest.
```
</details>

---

## Workflow 8 — Fulfillment (path-specific, NOT covered by fixing Sim Five)  (11 items · Opus)

Read `docs/workflows/expected-deliverables-repair-2026-09-03.md` and
`expected-deliverables-funding-2026-09-03.md` — every item below is traced to code there.

**Repair (paths 2 and 3):**

| # | Item |
|---|---|
| 1 | The letter engine produces ZERO letters for any sim profile — it fires only on Metro 2 reporting defects. OWNER-SET: any derogatory on a repair-path client gets a letter, AND every repair-path client always gets personal-info cleanup (1 name, 1 address, 0 inquiries) even with no negatives |
| 2 | Sim file has impossible dates (file date 2019, tradelines 2026) so the stale-date rule can never fire |
| 3 | Sim account numbers yield no last-4, killing cross-bureau matching and "ending 1234" text |
| 4 | Every sim account appears on all three bureaus; real damaged files do not |

**Funding (path 1):**

| # | Item |
|---|---|
| 5 | "Generate Apps" creates ZERO application rows; it only redraws the lender list. SOP says rows are created |
| 6 | Credit inquiries are NEVER logged — push-credit AND the real crs-pull emit `inquiries`, the workflow reads `newInquiries`. Hits real clients |
| 7 | Lender matcher ignores the credit file entirely (score, tier, card use, estimate). Proven live: 307 lenders with no pull |
| 8 | The book has NO personal lenders (196 InBranchBizCC + 117 OnlineBizCC) while the estimate promises $199,350 of personal money |
| 9 | Funding estimate ignores credit score — 724 and 762 both yield $199,350 to the dollar (F14). Owner question: should score move it? |
| 10 | Sim script stores 0% card use (case bug "revolving" vs "Revolving") and a total limit that includes the car loan |
| 11 | `business_age_months` written but unreachable — business funding blocked at $0 before age is read |

<details><summary>Copy-paste prompt</summary>

```
Repo /Users/zootimusmaximus/fundhub-platform. Read CLAUDE.md, then
docs/workflows/fix-batch-2026-09-03.md and claim Workflow 8. Then read BOTH
docs/workflows/expected-deliverables-repair-2026-09-03.md and
docs/workflows/expected-deliverables-funding-2026-09-03.md in full — every item is
traced to code there with file:line references. Do not re-derive them.

This workflow is the part "fix Sim Five once" does NOT cover. Both product questions were
answered by Chris on 2026-09-03 (below) — do not re-ask:
 Q1 (repair) — ANSWERED, owner-set 2026-09-03: "any derogatory deserves a letter,
     but only if they are in the correct offer path." So: every derogatory item
     (collection, charge-off, late) on a client whose offer path is REPAIR produces a
     letter; defect-only firing is not the product. Clients NOT on a repair path get
     no letters regardless of file contents. Build to that.
     PLUS, owner-set the same night: on EVERY repair-path client, clean file or not,
     ALWAYS run personal-info cleanup — consolidate to 1 name, 1 address, and dispute
     every inquiry with no matching open account. That is the floor; derogatory
     letters sit on top. Rounds: re-pull before each round, drop what is already gone
     (owner's round logic in the walk log). The sim profiles carry no name/address
     variants or SSN, so add them to `repair` and `trial` in push-credit.mjs or the
     rule cannot be exercised.
 Q2 (funding) — ANSWERED, owner-set 2026-09-03: "abide by the underwriting law that
     is in the repo. UnderwriteIQ is very specific." So: the estimate follows the
     UnderwriteIQ rules under vendor/underwriteiq-full/api/lite/crs/ exactly. Read
     them, check the current calculation against them, and fix any deviation. Do not
     invent a score factor the spec does not have, and do not remove one it does.
Then, in order of blast radius:
 - Inquiries never logged (funding #6): the key mismatch `inquiries` vs
   `newInquiries` between scripts/sim/push-credit.mjs + src/finance/crs-pull.mjs and
   workflow c-02-inquiry-created. This hits REAL pulls. Fix at the emitter, add a test.
 - Generate Apps creates no rows (funding #5): decide with Chris whether the button
   should create application rows or the SOP should be corrected. Then do that one.
 - Lender matcher ignores credit (funding #7) and no personal lenders (funding #8):
   report both to Chris with the numbers; the second is a data/business gap, not code.
 - Sim data fixes (repair #2-#4, funding #10-#11): straightforward, do them.
 - Repair #1: implement per Q1. Any derogatory → letter, repair-path clients only.
Dispute logic and credit-repair messaging are on the CLAUDE.md section 7 list: put
COMPLIANCE REVIEW REQUIRED at the top of the summary for the repair items. Marker
only. Section 6, 9, manifest.
```
</details>

---

## Workflow 9 — Re-walk from the ad click  (runs LAST · Chris + one agent)

Not a fix. The proof. After W1-W8 merge: five fresh clients, distinct FIRST names
(F4: "Sim 1" / "Funding", not "Sim" / "One-Funding"), Gmail "FS Auto" filter off (F17),
every step of `docs/workflows/manual-walkthrough-SOP.md` again, on all five paths this
time, with the expected-output specs open beside the real output. Repeat until Chris
says perfect.

Before W9, correct the SOP and runbook portal-link lines a THIRD time from whatever W4
actually builds (F31 showed the 2026-09-03 correction was wrong too).

---

## Owner decisions on this batch (2026-09-03) — final, do not re-raise

| # | Decision |
|---|---|
| 1 | Payments: **option A, sandbox key.** Keys are never rotated. |
| 2 | Contracts: real text may already be in the repo — search first. Otherwise Chris supplies it (on his TODO). Agents never draft legal text. |
| 3 | Repair letters: **any derogatory item deserves a letter, but only for clients on the correct (repair) offer path.** |
| 4 | Funding estimate: **follow the UnderwriteIQ underwriting rules in the repo exactly.** Not a free design choice. |

## Process notes (not product, owner-flagged)

| Ref | Note |
|---|---|
| F4 | Sim first names must differ next run — messages greet by first name only |
| F13 | The `academy` sim profile can never reach PREMIUM_STACK (17% card use vs ≤10% rule); premium tier is untested |
| F17 | Chris's Gmail "FS Auto" filter hides Fundhub mail from Inbox — walk from All Mail or disable it |
| F21 | Add "alec dupesh" (name verbatim, source unknown) objection handling — owner rated LOW |
| F32 | Agent permission denials cost walk time: add `scripts/sim/set-client-password.mjs` so client password resets are a named script, not a hand-run `node -e` |

---

## Board

| Workflow | Owner | Status | Manifest |
|---|---|---|---|
| W1 Payments | — | pending (option A decided) | |
| W2 Sales deck | — | pending | |
| W3 Contracts | — | pending (search repo for text first; else Chris TODO) | |
| W4 Portal | — | pending | |
| W5 CRM data | — | pending | |
| W6 Messaging | — | pending | |
| W7 AI setter | — | pending | |
| W8 Fulfillment | — | pending | |
| W9 Re-walk | Chris | blocked on W1-W8 | |

Claim by writing your name and `claimed`. Write your manifest here before reporting done.

---

## Where this batch sits — the whole ecosystem, in order (owner-set 2026-09-03)

1. **This batch** — fix everything from the ad click to the signed contract. Shared layer, covers all five offers.
2. **Re-walk** — an AI does the walk again after the fixes, **in a different thread from the one that fixed it, so it is not auditing itself.** Repeat until Chris says perfect.
3. **Fulfillment** — repair and funding. Path-specific. Workflow 8 above is the head start; the real pass is its own batch.
   - **Repair, in Chris's words: simple.** The company takes a snapshot of the client's credit. If the client is on the repair offer, every derogatory item populates a letter and the letters go out. That is the whole loop. Do not over-scope it with bureau-reply rounds or reply matching; those are not the job.
   - **UnderwriteIQ deliverables have not been done yet.** They are the next thing after the front end. Use the existing sim credit file for that run, and **clear out the sim data before it starts** (owner: "delete all the shit out of there"). Not now.
4. **Accounts receivable / re-entry into offers** — success fees, invoices, and clients coming back into a second offer.
5. **White label** — out of scope for all of the above. Last.

Chris's own words: "That's the whole ecosystem besides the white label."

---

## Added 2026-09-03 late session — two more workflows (owner-set)

### Workflow 10 — Deliverables must be produced BY THE LIVE SITE  (Opus · high effort)

Owner: "I demand that it can be produced by the site." Refs F41, F42, F43, F44, F45, F46, F50.

| # | Item |
|---|---|
| 1 | C-06 has never produced a document for any client, ever (F42). Find out why on the live site (Inngest run logs), fix it. |
| 2 | Production only has the Node/pdf-lib fallback printer; the designed set (Chris's Jordan Sample reference PDFs) comes from WeasyPrint, which Netlify lacks (F50). Make the live site produce the designed documents — WeasyPrint via a render service, or the Node printer brought up to the template. Outcome is owner-set; method is yours. |
| 3 | Accounts tripled across bureaus in every document (F43). De-duplicate in the printers regardless of input. |
| 4 | Business age never reaches the engine; docs tell a 72-month business to "form an LLC" (F44). |
| 5 | Lender buckets wrong — all "After optimization", none "Available right now" (F45). |
| 6 | Capital Readiness Summary built then dropped by the saver (F46). Save all five. |
| 7 | Deck "Send deliverables package now" builds the repair pack, delivers nothing, reports success (F41). Make it send the right pack or remove it. |
| 8 | Reference template placeholder `www.fundhubbookingurl.template` never replaced. |

<details><summary>Copy-paste prompt</summary>

```
Repo /Users/zootimusmaximus/fundhub-platform. Read CLAUDE.md, then
docs/workflows/fix-batch-2026-09-03.md and claim Workflow 10. Then read, in full:
docs/workflows/expected-deliverables-uwiq-2026-09-03.md (ground truth, file:line refs),
docs/workflows/expected-deliverables-uwiq-2026-09-03-pack/ (what the live path
produces today, plus README), and F41-F46 + F50 in
docs/workflows/manual-walkthrough-2026-09-03.md.

Chris's reference set (the DESIGNED output) is on his Desktop:
~/Desktop/funding_snapshot.pdf, credit_analysis_report.pdf, lender_match_list.pdf,
optimization_roadmap.pdf ("Jordan Sample", July 25 2026). Ask him to drop them into
docs/workflows/uwiq-reference-2026-07-25/ so they are in the repo.

Owner-set outcome, not negotiable: fundhub.ai itself must generate documents that
match that reference set for every client whose credit file lands. Two halves:
 A. Delivery. C-06 (src/workflows/c-06-crs-results-router.mjs) is registered,
    listens on analysis.completed, gate passes, and has NEVER written a document
    for any client. Get the Inngest run history for the live site, find where it
    stops, fix it, prove it with a fresh sim push (scripts/sim/push-credit.mjs)
    producing five rows in `documents` for that client and five files visible on
    their portal Documents tab.
 B. Rendering. Netlify has no WeasyPrint, so production takes the Node/pdf-lib
    fallback in src/underwrite/funding-letter-pdf.mjs and the pack is a skeleton.
    Either run WeasyPrint somewhere the site can call (a render service), or bring
    the Node printer up to the template. Match the reference: filled date, per-bureau
    score cards, charts, "What Is Costing You Money", "What Does Not Affect Your
    Funding", after-optimization lender table, application order, roadmap months,
    QR close with the REAL booking URL. Fix F43-F46 in the same pass.
Then F41: the deck button. Do not add a new screen (owner rule).
Prove with the five PDFs for Sim Five-Academy opened side by side with the reference
set, annotated per CLAUDE.md section 8. Definition of done section 6, task report
section 9, manifest to the board.
```
</details>

### Workflow 11 — SMS set: stop the runaway, fix the timing, rewrite the copy  (Opus)

Owner: "The SMS's are horrible and confusing, we will fix those." Refs F39, F40, F47, F48, F49 and the SMS review table.

| # | Item |
|---|---|
| 1 | ROOT CAUSE of the runaway: funnel events (`survey.submitted`, `entry.captured`, `booking.created`) are stored with `client_id = NULL`, so `hasBooked` in s-nobook-chase can never match. Attach the client at ingest (src/adapters/clickfunnels.mjs) — this also fixes every other workflow that looks up events by client. |
| 2 | ClickFunnels fires ~16 `survey.submitted` per survey. De-duplicate at ingest (idempotency_key exists on `events`) so one survey = one chase run. |
| 3 | SMS-NOBOOK-01 copy: "Your file is ready" is false pre-call. Rewrite (F40). |
| 4 | 24-hour reminder fires at booking time, says "tomorrow" for a call three days out (F47). Fix the schedule in s-04b-booking-reminders. |
| 5 | Josh handoff SMS has an empty link (F49). Fill the variable. |
| 6 | Deck pay-link SMS is bare (F48) and duplicated (F24, W2 owns the button). Give it context. |
| 7 | Every text opens identically; nothing says which step the client is on. Rewrite the set for stage-awareness. Chris reviews the copy. |
| 8 | Booking confirmation sequence lands ~3 min after booking; owner target 60 s (F1, moved here from W6). |

<details><summary>Copy-paste prompt</summary>

```
Repo /Users/zootimusmaximus/fundhub-platform. Read CLAUDE.md (section 12 — the
dispatcher, and outbound only in src/messaging/providers/*), then
docs/workflows/fix-batch-2026-09-03.md and claim Workflow 11. Read F39, F40, F47,
F48, F49 and the "SMS review" table in docs/workflows/manual-walkthrough-2026-09-03.md.

Proven on 2026-09-03: 51 identical "Your file is ready" texts (SMS-NOBOOK-01,
src/workflows/s-nobook-chase.mjs) hit one phone in two hours, to clients who had
all booked. Cause 1: every funnel event in `events` has client_id NULL, so the
chase's hasBooked() can never match. Cause 2: ClickFunnels sends ~16
survey.submitted per survey, each starting its own chase.

Order:
1. Fix ingest in src/adapters/clickfunnels.mjs so entry.captured, survey.submitted
   and booking.created are stored WITH the resolved client_id, and are
   de-duplicated via events.idempotency_key. Add tests. This is the root fix;
   everything else is copy.
2. Make hasBooked() also tolerate the historical NULL rows (match on email/phone
   in payload) so existing sleeping runs exit correctly.
3. F47 timing in src/workflows/s-04b-booking-reminders.mjs: 24h means 24h before
   the call, 2h means 2h before; never fire a "tomorrow" text for a call that is
   not tomorrow.
4. F49: fill the empty link in SMS-AISET04-HANDOFF.
5. F1: get the booking confirmation (email + SMS + AI call) under 60 seconds. The
   sweeper cadence (*/5) alone can cost 5 minutes — propose the change, ASK before
   changing cadence.
6. Copy rewrite for the whole set (welcome, confirm, reminders, handoff, no-show,
   no-book, pay-link). Stage-aware, one voice, real names. Draft in
   docs/ads/sms-copy-2026-09.md and have Chris approve in the thread before seeding.
   SMS consent wording is shipped copy — reuse it verbatim.
Prove with a fresh sim booking: exactly one of each text, at the right times,
with the new copy. Section 6, 9, manifest.
```
</details>

Board rows to add:

| Workflow | Owner | Status | Manifest |
|---|---|---|---|
| W10 Deliverables by the live site | — | pending | |
| W11 SMS set | ROUND 2 lane W11 | done | branch `fix/r2-w11-notifications` — manifest below |

W6 keeps only F18 (signature template); F1 moved to W11.

#### W11 manifest — `fix/r2-w11-notifications`, 2026-09-04

Items 1-7 done, item 8 (the wording) drafted and **held** for Chris to read.

| Item | Where | State |
|---|---|---|
| 1 · funnel events carry no customer | `src/adapters/clickfunnels.mjs` | done — resolved once per delivery, stamped on every event |
| 2 · one survey starts sixteen text runs | `src/adapters/clickfunnels.mjs` | done — a repeat post inside 6h is stored but starts no second run |
| 3 · runs already asleep keep firing | `src/workflows/s-nobook-chase.mjs` | done — `hasBooked` also matches email / last-10-digit phone in the payload, scoped to the company |
| 4 · F47 reminder timing | `src/workflows/s-04b-booking-reminders.mjs` | done — a reminder whose moment has passed is not sent at all |
| 5 · F49 empty handoff link | `src/workflows/ai-set-04-3way-handoff.mjs` | done — three sources, the last always answers |
| 6 · F48 bare pay-link text | `src/sales/closer-deck.mjs` (`payLinkSmsBody`) | done — wording only; the button is W2's |
| 7 · F1 confirmation under 60s | `s-04b` + `drainMessageNow` | done — those two rows only, sweep cadence untouched |
| 8 · F40 / wording of the whole set | `docs/ads/sms-copy-2026-09.md`, `db/seed/295_sms_copy_2026_09.sql` | **HELD** — one commit, subject starts `COPY SEED (HOLD):`, drop it to ship everything else |

**Files touched:** `src/adapters/clickfunnels.mjs`, `src/workflows/s-nobook-chase.mjs`,
`src/workflows/s-04b-booking-reminders.mjs`, `src/workflows/ai-set-04-3way-handoff.mjs`,
`src/sales/closer-deck.mjs`, `src/workflows/test-support.mjs`, five matching test files,
new `src/workflows/nobook-chase-historical.pg.test.mjs`,
`docs/journeys/booking-notifications-flow.md` (new), `docs/journeys/CHANGELOG.md`,
`docs/ads/sms-copy-2026-09.md`, `db/seed/295_sms_copy_2026_09.sql`,
`db/expected-migrations.mjs` (one appended line).

**Exports added:** `appointmentContext` and `reminderPlan` from
`s-04b-booking-reminders.mjs`, `hasBooked` from `s-nobook-chase.mjs`,
`payLinkSmsBody` from `closer-deck.mjs`, `FUNNEL_REPEAT_WINDOW_MINUTES` from
`clickfunnels.mjs`. No export renamed or removed. No route added or changed.

**Knock-on other lanes must know about (CLAUDE.md §5).** Funnel events now carry
a customer id, so every query shaped `FROM events WHERE client_id = $1` starts
returning rows it never returned before. Confirmed readers:
`api/read/portal-summary.mjs` `readCallHeld()`,
`dpc-02-call-outcome-enforcement.mjs` `callHappened`, and this lane's own
`hasBooked`. **Anything proved on live before this merges must be re-proved
after** — in particular W4b's portal stage ladder, where a client whose
`call.completed` event previously read as NULL will now read as call_held.

**Not fixed, named rather than hidden:** the workflow engine fan-out in
`src/events/bus.mjs` is fire-and-forget with the rejection swallowed (W10's
first suspect); the six-hour repeat window is a judgement, not a measurement;
the wording of the emails, other than one false subject line, is untouched.

---

## ROUND 2 — everything found AFTER the first fix batch was handed off (2026-09-03, 7:30-8:30 PM)

Round 1 (W1-W8) is built, 8,546 tests green, deploying on Chris's approval. This is what
the second session surfaced on top. Nothing here is in the round-1 report.

### Product fixes (agent work)

| # | What | Ref | Workflow |
|---|---|---|---|
| 1 | Funnel events saved with NO client attached (all 82 surveys, all 6 bookings) — root cause of the runaway chase and a hidden bug for every workflow that looks up events by client | F39 | W11 #1 |
| 2 | ClickFunnels fires ~16 survey events per survey; each starts its own chase run | F39 | W11 #2 |
| 3 | 51 "Your file is ready" texts to one phone; copy is false pre-call | F39, F40 | W11 |
| 4 | 24-hour reminder fires at booking time and says "tomorrow" for a call three days out | F47 | W11 |
| 5 | Josh handoff text has an empty link | F49 | W11 |
| 6 | Deck pay-link text is bare, no context | F48 | W11 |
| 7 | Whole SMS set rewritten for stage-awareness; Chris approves copy | SMS review | W11 |
| 8 | UnderwriteIQ deliverables have NEVER been produced for any client (C-06 never writes a document) | F42 | W10 |
| 9 | Live site only has the plain fallback printer; the designed set (Jordan Sample) needs WeasyPrint the site lacks. OWNER-SET: the site must produce the designed docs | F50 | W10 |
| 10 | Deck "Send deliverables" builds the repair pack, sends nothing, reports success | F41 | W10 |
| 11 | Accounts tripled in every document; business age never reaches the engine; lender buckets wrong; Capital Readiness Summary dropped; placeholder booking URL in the template | F43-F46 | W10 |
| 12 | Repair floor on EVERY repair-path client, clean or not: 1 name, 1 address, 0 inquiries. Then derogatories on top. Re-pull before each round, drop what's gone | owner rule | W8 |
| 13 | Sim profiles carry no name/address variants or SSN, so #12 can't be exercised | owner rule | W8 |
| 14 | Dispute-consent block shows only for repair + funding offers, never courses/e-products | F35 | W4 |
| 15 | Portal says "your call is next" after the call, contract and pull all happened | F33 | W4 |
| 16 | Portal advisor "Name not shown here" | F34 | W4 |
| 17 | An offer the client bought still shows as a locked upsell | F37 | W4 |
| 18 | Messages tab shows "No messages yet." twice | F36 | W4 |
| 19 | Real contract text: seed from docs/contracts/source-2026-08-28/ (now in repo) into every "NOT THE REAL AGREEMENT" block, via a new migration | F30 | W3 |
| 20 | Lender matcher still ignores the credit file after a pull (fixer flagged: needs its own pass) | F10 | W8 |

### Open questions carried from the fixer's report (owner decides)

| # | Question |
|---|---|
| A | Booking confirmations: skip the 5-minute queue wait how? (owner target 60 s) — F1 |
| B | Generate Apps: should it create application rows, or is the SOP wrong? |
| C | Booked → Confirmed: RSVP is never captured. Build capture, or drop the column? — F3 |

### Only Chris can do these

| # | Task |
|---|---|
| i | Approve the deploy (it is the compliance sign-off for the deck figure, wrap script, consent delivery) |
| ii | Name the 21 untitled ads |
| iii | Fix the "Annual Business Revenue" question inside the ClickFunnels editor (wired wrong there; answer never reaches us) |
| iv | Buy a Bland phone number (account owns none — calls ring, nobody speaks) |
| v | Read the new Josh script before it goes near a phone |
| vi | Before the re-walk: distinct first names per sim (F4), Gmail "FS Auto" filter off (F17), clear the sim data (owner said) |

### Notes only (not fixes)

- Accountability as an upsell — revisit after fulfillment.
- Premium tier unreachable by the academy sim profile (F13) — test-data, fix when the sim is rebuilt.

### Order

W10 and W11 can start now, in parallel, on top of the deployed round 1. W8's new items
(#12, #13, #20) and W4's (#14-#18) and W3's (#19) are additions to workflows that already
exist — reopen those threads or start fresh ones with the same prompts plus these rows.
Then W9, the re-walk, in a thread that fixed nothing.
