# UI polish — 2026-08-20

Owner-set scope: polish the 18 screens that survived the simplify pass. Keep the 14 retired screens reachable by direct URL but hidden from menus. Preserve Present, Call cockpit, closer context, contract, pay-link, credit, offer, and disposition behavior. Contracts wording stays owner/admin only.

## Workflows

| Workflow | Owner | Status | Files owned |
|---|---|---|---|
| W1 — shared shell and role visibility | cloud agent | done | `public/app/shell.js`, shared shell styles/tests |
| W2 — Pipeline, Lenders, Products | cloud agent | done | the three named screen files |
| W3 — remaining screen polish | cloud agent | done | surviving screen files not owned by W2 |
| W4 — proof and evidence | cloud agent | blocked | this board and `ui-polish-2026-08-20-evidence/` |

## Shared brief

- Source audit: `origin/claude/ui-standards-audit-nz790d`.
- Fix all surviving-screen CRITICAL/HIGH findings first.
- Fix MEDIUM/LOW findings only when the audit and standards give a safe answer.
- Do not act on open product questions: role-first-question changes for setter/inquiry specialist/My Numbers/Staff & Teams, a new Lenders hero metric, or audit counting differences around Search/Chat.
- Shared repeats should be fixed once in shell or shared CSS.
- No behavior, data, action, dependency, intended-journey, deploy, or protected-flow changes.
- A visual-only change does not alter a documented journey.

## Change manifests

### W1

- `public/app/shell.js`: screens without a named top bar now get one shared account row in normal page flow.
- `public/app/crm-sidebar.css`: the account row reserves space; all 14 retired menu rows are hidden on the first frame while their direct URLs stay unchanged.
- `src/http/mobile-shell.test.mjs`: guards the in-flow account row.
- `src/http/app-nav-reachability.test.mjs`: keeps the first-frame hidden list equal to `NAV_HIDDEN` and keeps Contracts owner/admin only.
- `e2e/sidebar-roles.spec.mjs`: browser role checks now include Contracts in the owner/admin-only list.
- Routes, exports, props, actions, data, and journeys: unchanged.
- Focused proof: 86 passed, 0 failed, 0 skipped.

### W2

- `public/app/pipeline.html`: the expendable clock leaves the laptop header before it can collide with the page title and org state.
- `public/app/lenders.html`: the header wraps on narrow space and drops its live clock before it can become a stacked text column.
- `public/app/products-commissions.html`: the header wraps, spacing follows the shared scale, and the closed editor no longer adds hidden page width.
- Routes, actions, data, and journeys: unchanged.

### W3

- `public/app/shell.js`: mobile title clearance and in-header account placement now include plain page headers used by Closer Call, My Numbers, and Sales Floor.
- `public/app/fundhub-brand.css`: Client Control Panel values use the shared hero-metric size.
- `public/app/client-control-panel.html`, `calendar.html`, `documents.html`, `staff-teams.html`, `content-admin.html`: phone layouts stack instead of clipping, overlapping, or leaving dead space.
- `public/app/agent-editor.html`, `messaging.html`: fixed bottom controls have reserved space and primary touch targets meet the size floor.
- `public/app/my-numbers.html`, `public/app/my-numbers.js`, `public/app/sales-floor.js`, `public/app/template-editor.html`: raw or false loading copy is replaced with plain, truthful text.
- `public/app/sales-floor.html`, `public/app/template-editor.html`: unexplained empty visual space and off-scale spacing are removed.
- Routes, actions, data, and journeys: unchanged.

## Proof

- Lint: passed (`1365` files and inline scripts parsed).
- TypeScript: not applicable; this repository has no `tsconfig.json`, so `npx tsc --noEmit` prints the compiler help and exits `1`.
- Shared-shell and role unit checks: `86` passed, `0` failed, `0` skipped.
- Full suite: `6,264` passed, `3` failed, `3` skipped. The three failures are outside this UI diff: journey gate extraction for two routes, the company-brain affiliate org filter, and the social generator network fence.
- Sidebar browser check: `4/7` passed. The three failures are old simplify/test drift: the test still expects hidden `journeys.html`, clicks hidden `partner-galaxy.html`, and expects the retired Watch group.
- Local 18-screen browser check at `1440×900` and `390×844`: `17/18` screens pass all measured overflow, header, account-chip, and copy checks. `client-portal.html` remains red because its sign button ends at `933px`, below the `900px` first view.
- Live Playwright: `5/31` required ids passed = **16.1/100**. The run stopped because this cloud workspace has no gitignored `.env` and no `STAFF_E2E_PASSWORD` or `STAFF_INITIAL_PASSWORD`; `18` tests did not run. The required `100/100` gate was not reached, so the human click walk was correctly not started.
- Screenshot capture produced all 18 desktop and phone folds. Markup did not complete: the available Python runtime has no Pillow image library, and this task forbids adding a dependency. The raw shots are not being claimed as marked evidence.

### Audit findings closed

- CRITICAL/HIGH shared phone account bar overlap on `closer-call`, `my-numbers`, `sales-floor`, `staff-teams`, `documents`, `contracts`, `template-editor`, `products-commissions`, `lenders`, and `content-admin`.
- CRITICAL role mismatch on Contracts: contract wording stays owner/admin only and the browser role list now checks it.
- CRITICAL Pipeline header collision.
- CRITICAL Products & Commissions laptop overflow.
- CRITICAL Lenders date/header overlap.
- CRITICAL Client Control Panel phone gap and squeezed columns.
- HIGH My Numbers raw API-path error text.
- HIGH Sales Floor unlabeled empty funnel cell.
- HIGH Template Editor false loading state.
- HIGH Calendar clipped phone schedule.
- HIGH Inquiry Remover weak metric hierarchy.
- HIGH Messaging chat-control overlap.
- MEDIUM Agent Editor fixed status-bar overlap.

## Owner decisions still needed

- `client-portal.html`: the owner-set order says the welcome video must stay first, while the audit requires the signature button inside the first `900px`. Two spacing-only attempts did not move the button, so this needs a layout decision before another change.
- Role-first-question rules remain open for Setter, Inquiry Specialist, My Numbers, Staff & Teams, and Closer Dashboard.
- Lenders still needs an owner call on list-first versus adding a hero number.
- Future audits still need one counting rule for shared Search and Chat controls.
