# Night ship — 2026-08-26

**Launch tonight?** **No.** Code on main is not a journey sequence.  
**Testing law (owner, this night):** journey-first. One desk click or one 0.1s Bland call is **not** a sequence. Do not claim AR workflows. Do not write 100%.  
**Never merge** `vc/save-2026-08-25`.

**Main tip when last checked:** `75705900` (#164 unrecorded on main). Do not rebuild it.

## Do not touch (sibling lanes)

- Agent-tester skill (prompts roleplay)
- AR / journeys / escalation (they should own the Bland **call sequence**)
- Aff reset + drips; PR #171 funded KPI + `/start` clicks (merge agent)
- Letters PR #170 (merge agent). Do not remake PDFs or rebuild rounds
- Oxylabs API

## Named list (this thread owns)

| ID | Owns | Code | Journey sequence |
|---|---|---|---|
| B1 | Play name on bank yes/no. Not a neural net. | **on main** [#166](https://github.com/ZootimusMaximusBackup/fundhub-platform/pull/166) | **NOT proven.** Intended funding-advisor file is route gates only (`role-funding-advisor-intended.md`). four-builds V1 sequence (open Apply list → type play → Bank yes/no → see stamp → dollar guess unchanged) was **not** walked as that full desk path. API wrote two play names on Fund Horse. That is a write, not the journey. |
| B2 | Expected vs actual inquiry name. No C-02 dump. | **on main** [#167](https://github.com/ZootimusMaximusBackup/fundhub-platform/pull/167) | **NOT proven.** Specialist intended (`role-inquiry-remover-intended.md`) is sign-in → Inquiries toggle → queue → person work. Columns are on the live desk. API saved expected on a sim row. Not the Specialist sequence. Did not plug C-02. |
| B3 | Mail pipe **code + tests only**. No live DNS. No paper. | **on main** [#168](https://github.com/ZootimusMaximusBackup/fundhub-platform/pull/168) | **Tests only — as named.** `@fundhub.ai` still Cloudflare. Bank Inbox page loads. That is **not** a live inbound mail sequence. |
| B4 | Bank form uses **client** email. Warn on fundhub.ai. | **on main** [#165](https://github.com/ZootimusMaximusBackup/fundhub-platform/pull/165) | **NOT proven.** Caption is on live CCP. Apply launch returned 422 (Oxylabs geo — sibling). four-builds V1 (open Apply, see client email, do not submit) did **not** finish. |
| Bland | Hang-up cause if AR/calls does **not** take it | **on main** [#169](https://github.com/ZootimusMaximusBackup/fundhub-platform/pull/169) first-sentence + no greeting-wait on prove line | **NOT a talk sequence.** A live call after deploy was still **0.13s**. Cause: agent line voice URL was Twilio **demo.twilio.com** (SMS still `fundhub.ai`). Voice URL was moved to a 120s pause twimlet so a later call *can* stay open. **AR/calls owns the talk sequence.** Do not claim Josh/doc-chase roleplay. |

## What this night did **not** prove

- Any AR workflow
- Any agent prompt roleplay (agent-tester lane)
- Affiliate drips / reset / social calendar / custom domains
- Letter rounds / old PDFs
- Oxylabs Apply session
- ClickFunnels apply
- Live CRS / paper mail / card charge

## Heartbeat

- `GET https://fundhub.ai/api/health` → `ok`, `db: up`, `pending: 0` (after a bad CLI deploy dropped `pg`; redeployed with a real `node_modules`)
- `INNGEST_EVENT_KEY` still set
- Pulse dry-run: no SMS. Left: gate-relay has no `heartbeat.json`

## PRs this thread shipped (code only)

#165 B4 · #166 B1 · #167 B2 · #168 B3 · #169 Bland payload. #164 unrecorded (other fixer).

## Leave alone

UnderwriteIQ 5.5×. ClickFunnels. Live CRS. Card charge. Paper mail. Darwin WhatsApp. `gitbutler/workspace` / `vc/save`. Letters. Oxylabs. Aff/WL/KPI. Agent-tester. AR escalation.
