T10 unit C-partner-page — re-run after the verifier's findings. 2026-08-19.

WHERE MEASURED: local Postgres 17 on this Mac, scratch database fundhub_t10_rc
(socket /tmp). Never fundhub_ci, never production.

1. src/http/partner-galaxy-banner.test.mjs — NEW, no database.
   DATABASE_URL unset (the blocking CI job's conditions):
     # tests 14   # pass 14   # fail 0   # skipped 0
   Against HEAD's public/app/partner-galaxy.html (git show HEAD:... > /tmp/old-galaxy.html,
   temporary copy of the test pointed at it, copy deleted afterwards):
     # tests 14   # pass 3    # fail 11
   (full list in banner-test-red-against-old-code.txt)
   Against HEAD's netlify/functions/partner-site.mjs (file swapped to HEAD, run, restored;
   md5 c5507a62ac6d4b43297a95ea70b4f8bb before and after):
     SyntaxError: ... does not provide an export named 'NOT_LIVE_HTML'  -> whole file red.

2. src/http/partner-pages-publish.pg.test.mjs — 6 tests.
   As the database owner:        # pass 6  # fail 0  # cancelled 0
   As the unprivileged fundhub_app role, which is what production uses:
                                 # pass 6  # fail 0  # cancelled 0
   Before this change that same run cancelled all 6 at the before() hook:
     ERROR: new row violates row-level security policy for table "partner_module_settings"
   (reproduced directly with psql as fundhub_app; the unscoped INSERT is still refused,
    the asPartner()-scoped one now succeeds.)
   DATABASE_URL unset: the whole describe() skips, which is exactly why item 1 exists.

3. npm run lint: 1325 file(s) and inline script(s) parse clean.
   node --check on every JS file touched: clean.
   npx tsc --noEmit: no tsconfig in this repo; the command prints its help. Pre-existing.
