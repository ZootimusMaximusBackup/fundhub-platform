T10 unit D — proof the screen test fails on the old file
Run 2026-08-19, worktree /tmp/wt-T10, branch fix/T10-affiliate-partner

$ git show HEAD:public/app/social-studio.html > /tmp/old-social.html
$ FH_SOCIAL_STUDIO_HTML=/tmp/old-social.html node --test src/http/social-studio-screen.test.mjs
not ok 1 - the screen actually calls the write that needs no connected account
not ok 2 - Queue it does not hard-stop on a missing account
not ok 3 - every waiting post can be thrown away
not ok 4 - both controls are wired to a real row, not just drawn
not ok 5 - the screen reports the send time the SERVER saved, not the one in the box
not ok 6 - the screen says plainly that emptying the box does not unschedule
not ok 7 - the empty state and the writer give the same answer about writing a post
not ok 8 - no internal error word is ever printed to a partner
# tests 8
# pass 0
# fail 8

$ node --test src/http/social-studio-screen.test.mjs   # against the fixed file
ok 1 - the screen actually calls the write that needs no connected account
ok 2 - Queue it does not hard-stop on a missing account
ok 3 - every waiting post can be thrown away
ok 4 - both controls are wired to a real row, not just drawn
ok 5 - the screen reports the send time the SERVER saved, not the one in the box
ok 6 - the screen says plainly that emptying the box does not unschedule
ok 7 - the empty state and the writer give the same answer about writing a post
ok 8 - no internal error word is ever printed to a partner
# tests 8
# pass 8
# fail 0

$ DATABASE_URL=postgresql:///fundhub_t10_rd?host=/tmp node --test src/http/social-posts-write.pg.test.mjs
# tests 13
# pass 13
# fail 0
# skipped 0
