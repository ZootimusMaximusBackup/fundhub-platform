=== T2 merge bar: "no worse than main", compared failure by failure ===
Both runs use the SAME profile as the blocking CI job, "suite (no database)",
run back to back on this machine on 2026-08-18.

  origin/main @ 7db4ba6   : tests 5758 | pass 5753 | fail 2 | skipped 3
  fix/T2-...-v2           : tests 5818 | pass 5813 | fail 2 | skipped 3

The two failures are IDENTICAL on both sides:
  1. "the extraction is faithful to the code"
     (nested: "*** no route's gate is left unverified ***" — names only
      finance/crs-pull and gifts/message-blaster, never /api/payment-links)
  2. "an endpoint excused from the org filter still passes the session's org to its store"

T2 adds 60 tests and introduces NO new failure. CI is red on main already; this
branch does not make it worse, which is the stated merge bar.

Separately, with a real database T2 measured 5817 pass / 3 fail / 0 skipped. The
third failure there is T16's new row-lock test finding a real live bug on the
`bookings` table, which T2 does not touch — see verify/after-suite.txt and
BLOCKERS #13.
