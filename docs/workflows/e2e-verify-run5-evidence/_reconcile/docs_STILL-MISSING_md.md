# Reconcile — docs/STILL-MISSING.md

Re-verified 2026-08-16 at HEAD `8887286` (main) by claude-fable-5, read-only.
Board: `docs/workflows/fable-audit-2026-08-16.md` (Reconcile table said "not re-verified in batch 2").

Method: read the whole doc; `git log -- docs/STILL-MISSING.md` (last change `ea4c03f` 2026-08-06 — the board's "2026-08-14" update date is not traceable to this file, see R0); `netlify env:list --context production --plain` read for NAMES only (CLI worked, no proxy 403; no values printed); code grep for each env name; one read-only Postgres count for the two DB-stored credential items. No production writes, no logins.

Netlify production names present today (by name only): ANTHROPIC_API_KEY, BLAND_*, CLICKFUNNELS_WEBHOOK_SECRET, COMMAS_API_KEY, COMMAS_WEBHOOK_SECRET, CRS_*, DATABASE_URL, MIGRATION_DATABASE_URL, FANBASIS_CHECKOUT_API_KEY, GHL_API_KEY, GHL_LOCATION_ID, GHL_RELAY_API_KEY, GOOGLE_DRIVE_SERVICE_ACCOUNT_JSON, INNGEST_EVENT_KEY, INNGEST_SIGNING_KEY, INQUIRY_REMOVAL_WEBHOOK_SECRET, MAILGUN_SEND_*, MAILGUN_SIGNING_KEY, PII_ENC_KEY, AD_TOKEN_ENC_KEY, POSTGRID_*, RESEND_API_KEY, RESEND_FROM, TWILIO_ACCOUNT_SID, TWILIO_API_KEY, TWILIO_API_SECRET, TWILIO_AUTH_TOKEN, TWILIO_SEND_ACCOUNT_SID, TWILIO_SEND_AUTH_TOKEN, TWILIO_SEND_FROM, TWILIO_TRUSTHUB_BUNDLE_SID.
Not present: OXYLABS_*, COMMAS_CHECKOUT_BASE_URL, META_APP_ID/SECRET, LINKEDIN_*, OPENAI_API_KEY, GOOGLE_DRIVE_DELEGATE_EMAIL, HUBSTAFF_*, CREATIVE_*.

| Id | Prior finding (short) | Prior status | Now | Evidence | Note |
|---|---|---|---|---|---|
| R0 | Doc header says "Updated 2026-08-06"; board says "updated 2026-08-06 / 2026-08-14" | — | still open (doc gap) | `git log -- docs/STILL-MISSING.md` → last commit `ea4c03f` 2026-08-06; no 08-14 commit touches the file | The 08-14 credential work landed in other docs (`docs/workflows/ghl-out-crs-today.md`, `live-send-cutover.md`, `.env.example` in `0d6c2ee`), not in STILL-MISSING.md. STILL-MISSING.md is stale vs. Netlify today. |
| C1 | Oxylabs creds — browser skipped, `OXYLABS_USERNAME/PASSWORD` missing; Residential Apply 503 | missing | still open | Netlify env:list: no OXYLABS_* name; `src/adapters/oxylabs.mjs` unchanged since `5de4b88` 2026-08-04 | Nothing landed after 08-06. |
| C2 | GHL contacts-scoped `GHL_API_KEY` (contacts probe 403) | missing | superseded (owner-set) | `docs/workflows/ghl-out-crs-today.md:3-4` "Owner laws (2026-08-14): GoHighLevel is OUT. Do not call or debug GHL."; commit `0d6c2ee` 2026-08-14 "Cut outbound off GHL"; GHL_* names still present on Netlify | Owner decision closes it; no new key needed. |
| C3 | `COMMAS_WEBHOOK_SECRET` missing (payment webhook verify) | missing | done (name set) | Netlify env:list shows COMMAS_WEBHOOK_SECRET and COMMAS_API_KEY; consumer `src/http/router.mjs` | Value not checked (never printed); live webhook round-trip not exercised. |
| C4 | `COMMAS_CHECKOUT_BASE_URL` missing (payment-link send 503) | missing | superseded | Netlify has FANBASIS_CHECKOUT_API_KEY (not COMMAS_CHECKOUT_BASE_URL); `src/payment-links/index.mjs:23-24` "Prefer FANBASIS_CHECKOUT_API_KEY → live checkout-session API. Fall back to COMMAS_CHECKOUT_BASE_URL … only for tests / legacy"; `src/payments/commas-api.mjs:189-192,212,255` POST /checkout-sessions → payment_link | Code moved to API session create, exactly the fix the doc anticipated. Live mint not proven here (POST). |
| C5 | `META_APP_ID` / `META_APP_SECRET` missing (social OAuth) | missing | still open | Netlify env:list: no META_* name; `src/social/oauth.mjs` last changed `766e8bc` 2026-08-04 | |
| C6 | Meta system-user token + `act_…` on `ad_platform_connections` (DB, not env) | missing | still open | read-only `select count(*), count(encrypted_access_token) from ad_platform_connections` → `0|0` (prod DB via netlify env:get, 2026-08-16) | Table empty; no connection stored. |
| C7 | `LINKEDIN_CLIENT_ID/SECRET/ORG_ID` missing | missing | still open | Netlify env:list: no LINKEDIN_* name | |
| C8 | `OPENAI_API_KEY` missing (Company Brain embeddings / creative) | missing | still open (narrowed) | Netlify env:list: no OPENAI_API_KEY; `src/company-brain/embed.mjs:1-4,14,18` (commit `8887286`) "Owner-set: embeddings stay on OpenAI. Completions moved to Claude" | Scope shrank to embeddings only (owner-set); key still absent. |
| C9 | `GOOGLE_DRIVE_SERVICE_ACCOUNT_JSON` / `GOOGLE_DRIVE_DELEGATE_EMAIL` missing (Drive sync) | missing | done (partial) | Netlify has GOOGLE_DRIVE_SERVICE_ACCOUNT_JSON; DELEGATE_EMAIL absent but now optional — commit `b6b83ca` 2026-08-15 "Allow Drive sync without Workspace admin delegation"; `src/company-brain/config.mjs:18,23` | Sync run not proven read-only. |
| C10 | `HUBSTAFF_TOKEN` / `HUBSTAFF_ORG_ID` missing (deep-monitoring poll) | missing | still open | Netlify env:list: no HUBSTAFF_* name; `src/adapters/hubstaff.mjs` unchanged since 08-04 | Doc itself says leave unset until cutover. |
| C11 | Creative provider keys `CREATIVE_*` missing | missing | still open | Netlify env:list: no CREATIVE_* name; consumers `src/creative/providers/{copy,static,ugc-video,product-video}.mjs` | |
| C12 | Social channel page tokens on `social_channels` (DB) | missing | still open | read-only `select count(*) from social_channels` → `0` (prod DB 2026-08-16) | No channel rows at all. |
| C13 | `INQUIRY_REMOVAL_WEBHOOK_SECRET` (IRA → platform webhook) | missing | done (name set) | Netlify env:list shows INQUIRY_REMOVAL_WEBHOOK_SECRET; consumers `src/adapters/inquiry-removal.mjs`, `src/http/router.mjs` | Value not checked; IRA runtime itself is on owner hold (`.serena/memories/inquiry-phone-remover-on-hold.md`, commit `fb53d01`). |
| C14 | `TWILIO_*` — do not set until A2P | missing (owner rule) | superseded (owner-set) | Netlify env:list shows TWILIO_ACCOUNT_SID, TWILIO_AUTH_TOKEN, TWILIO_API_KEY/SECRET, TWILIO_SEND_ACCOUNT_SID, TWILIO_SEND_AUTH_TOKEN, TWILIO_SEND_FROM, TWILIO_TRUSTHUB_BUNDLE_SID; `docs/workflows/ghl-out-crs-today.md:39-59` A2P brand + campaign submitted 2026-08-14; `live-send-cutover.md:42` "not approved until Monday" | Owner set the keys and submitted A2P; live SMS not approved / not proven. |
| C15 | `INNGEST_EVENT_KEY` deliberately unset (owner gate; "may already exist on Netlify") | not touched | superseded (owner-set) | Netlify env:list shows INNGEST_EVENT_KEY and INNGEST_SIGNING_KEY by name; consumers `src/workflows/index.mjs` | Owner-gated. Presence by name only; whether workflows are live was NOT checked and no change is suggested. |
| P1 | Already-present: ANTHROPIC_API_KEY, MAILGUN_SEND_*, GHL_API_KEY/GHL_RELAY_API_KEY, DATABASE_URL/MIGRATION_DATABASE_URL/enc keys | present | done (still present) | Netlify env:list: all names present (PII_ENC_KEY, AD_TOKEN_ENC_KEY, MAILGUN_SIGNING_KEY too) | Mailgun demoted: `live-send-cutover.md:73` "Mailgun is no longer the product email path" (owner law 08-14); RESEND_API_KEY / RESEND_FROM now present. |
| P2 | Local `.env` lacks Oxylabs/Commas/Meta/LinkedIn/OpenAI/Drive/Hubstaff | — | unverified | Not read — the local `.env` values are out of scope for this audit beyond the password tool; `.env.example` changed in `0d6c2ee` (RESEND_*, Twilio) | Cannot confirm local parity read-only without printing names from `.env`; not attempted. |
| K1 | Commas payment-link assumption 1: URL-query variable amount likely unsupported; fix = mint session via API | unverified | done (code) | `src/payments/commas-api.mjs:212-282` createCheckoutSession POST /checkout-sessions; `src/payment-links/index.mjs:23-24` prefers API key path; FANBASIS_CHECKOUT_API_KEY on Netlify | Live Commas response shape still not captured in repo evidence → live behaviour unverified. |
| K2 | Commas assumption 2: webhook echoes our reference — unverified until real payload | unverified | unverified | `src/adapters/commas.mjs:153-178,445` still stores commas_session_id at mint and reads ref/metadata; no captured `payment.succeeded` payload found in repo | Needs a real webhook capture; cannot verify read-only. |
| K3 | Owner action: log into Commas, create webhook, store secret, confirm checkout mode | open | done (partial) | COMMAS_WEBHOOK_SECRET + COMMAS_API_KEY + FANBASIS_CHECKOUT_API_KEY names on Netlify; checkout switched to API create (K1) | Webhook subscription on Commas side not observable from repo. |
| L1 | Lender + inquiry ops tables (migrations 138–140) ship empty; import from Airtable | unchanged by design | still open | `db/migrations/138_lenders.sql`, `139_funding_ops.sql`, `140_inquiry_ops.sql` present; no lender/inquiry seed in `db/seed` since `ea4c03f`; read-only `select count(*) from lenders` → `0` | Empty on purpose; no import landed. |
| X1 | "Closed earlier" list (partner funnels, social publish cron, creative runner, campaign write loop, agent runtime shadow, Oxylabs door 503, Hubstaff consent gate) | closed | superseded | Not re-tested here; covered by role journeys in the board (proxy/campaigns/creative/social rows are UNVERIFIED there for POST routes) | Out of scope for a credentials reconcile; see board Findings table. |

Counts: total 23 — done 6 (C3, C9, C13, P1, K1, K3) · still open 10 (R0, C1, C5, C6, C7, C8, C10, C11, C12, L1) · superseded 5 (C2, C4, C14, C15, X1) · unverified 2 (P2, K2).
