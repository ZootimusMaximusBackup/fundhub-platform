# Reconcile — docs/JOURNEY-RUNNER-RESULTS.md (dated 2026-08-02)

Re-verified 2026-08-16 by claude-fable-5, read-only, on `main` @ `8887286`. Board: `docs/workflows/fable-audit-2026-08-16.md` ("Reconcile (prior docs)" row 4).

Method: code + git evidence only. The runner is reachable only via `POST /api/journeys/run` (`netlify/functions/api.mjs:223`), which fires workflows and drains the message queue inside a `BEGIN … ROLLBACK` (`api/journeys/run.mjs:95,107,147`). That is a side-effect POST against production, so it was **not** run. No DB reads were made. CI's real-Postgres job on `main` (`gh run 31989936887`) fails at "Apply every migration to an empty database", so the regression tests added by the fix commit are not proven green in CI either.

| Id | Prior finding (short) | Prior status | Now | Evidence | Note |
|----|----------------------|--------------|-----|----------|------|
| R1 | Runner crashes on every journey: virtual clock passes epoch-ms number to `::timestamptz` (`date/time field value out of range`) | open (blocking) | done | commit `1ae3eef` (2026-08-02); `src/messaging/dispatch.mjs:47-53` `resolveTimestampParam()`; applied at `dispatch.mjs:177` in `claimDue`; regression tests `src/messaging/dispatch.pg.test.mjs:98-119` (function / number / Date clock shapes) | Fixed in code. Not re-run live (POST with side effects). CI pg job fails before tests run, so "green against real Postgres" rests on the commit message only. |
| R2 | `facts.mjs` queries table `stages` (does not exist; real name `pipeline_stages`) | open | done | `src/journeys/runner/facts.mjs:169` now `FROM pipeline_stages`; commit `1ae3eef` | — |
| R2b | One bad query poisons the shared transaction; agents + templates come back "could not read" (24 of 46 unchecked) | open | done | `src/journeys/runner/facts.mjs:121-147` per-query SAVEPOINT/ROLLBACK TO; `gather()` runs the five queries sequentially (`facts.mjs:156-180`); commit `1ae3eef` | Collateral cause removed in code; the "24 of 46" count not re-measured. |
| R3 | Two cross-journey handoffs name journeys the runner does not know: "Optimization rounds" (closer) and "Owner revenue roll-up" (advisor) | open | still open | `src/journeys/seed-journeys.mjs:614` ("Optimization rounds"), `:703` ("Owner revenue roll-up") — unchanged since 2026-08-02 | Nothing in git touched these labels. |
| R3b | All handoffs are prose; tool flags all 4 as "could not check" | open (tool limit) | still open | `src/journeys/runner/diff.mjs:236-243` still emits `unverified` "handoff … prose and cannot be matched automatically" | Documented tool limit, not a bug. |
| R4 | Runner's 6 keys vs docs/journeys' 8 keys don't line up; `role-owner` and `role-inquiry-remover` can never be checked | open | still open (owner-set: documented, not merged) | `src/journeys/seed-journeys.mjs:4-13` header records the gap; `docs/journeys/README.md:27-35` "The join … was never finished"; commit `1ae3eef` msg item 3 | Fix commit chose to document rather than rename. No runner tree for role-owner / role-inquiry-remover today. |
| R4b | No `-intended.md` exists for any of the 8 journeys | open | done | `ls docs/journeys/*-intended.md` → 8 files, all dated Aug 2 10:29; added in commit `1ae3eef` | All 8 carry the "WRITTEN AFTER THE FACT" banner (e.g. `role-owner-intended.md:3-12`); they are generated from the same data as `-actual.md`, so intended-vs-actual comparison proves little until a human rewrites them. |
| O1 | `journeys` table has zero rows for the company (all trees come from `seed-journeys.mjs`) | note | unverified | no DB read made in this pass | Would need a production SELECT; not done. |
| O2 | `COMMAS_WEBHOOK_SECRET` not set → $32 diagnostic payment step fails closed | open | still open | `docs/STILL-MISSING.md:51` still lists it as uncollected (doc updated 2026-08-14); commit `1ae3eef` msg item 5 "stays unset … open follow-up per the owner's direction" | Owner-set follow-up. Netlify env not read (no `netlify env:list` attempted). |
| O3 | 27 of 31 staff screens named by no journey step | note (raw fact) | unverified | not re-run | Informational only. |
| O4 | `messages` table empty in production | note | unverified | no DB read made | — |
| C1 | Run connected as `postgres` superuser, so RLS was bypassed | caveat | superseded | `db/migrations/104_app_role.sql` (unprivileged `fundhub_app` role); CLAUDE.md §12 | Caveat about that run remains true; the app itself no longer runs as superuser. |
| M1-M4, M6-M8, M10 | 8 mismatches: no screen answers to "Setter queue", "Sales board" (x3), "Pre-call panel" (x2), "Closer result screen", "Client finance dashboard" | broken/mismatched | still open | labels unchanged at `src/journeys/seed-journeys.mjs:150,188,246,280,326,440,481,699`; matcher unchanged `src/journeys/runner/diff.mjs:248-266` (token overlap vs filename/title, needs >=0.5); `grep -il` of those phrases over `public/*.html` → no file | Not re-run; but neither the labels nor a matching screen changed, so the mismatch cannot have closed. |
| M5, M9 | client / closer "$32 diagnostic" — payment adapter secret unset | broken | still open | same as O2 | Duplicate of O2. |
| M11 | affiliate "Send their link" step names a screen that says "not built yet" | broken | still open | `src/journeys/seed-journeys.mjs:741` still reads "Affiliate portal — not built yet" | Label unchanged. |
| X1 | Doc's implicit claim: runner "has never successfully produced a real report against production data" | open | unverified | no run performed this pass; no evidence file of a post-fix run found under `docs/` | Absence is the finding: no recorded post-fix production run exists in the repo. |

Counts: total 17 · done 4 · still open 8 · superseded 1 · unverified 4.
