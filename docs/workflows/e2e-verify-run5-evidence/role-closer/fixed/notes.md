Expected: a closer session never calls GET /api/demo/mode, so the shell does not 403 on every screen. Observed after the gate in mountDemoBanner: signed in as closer@fundhub.ai on http://localhost:8888, landed on /dashboard.html (HOME), then opened /app/pipeline.html (a shell screen that used to fire the 403). Role stored as closer. Network capture listed zero /api/demo/mode requests (no 403). Landing and pipeline screenshots show the closer UI with no demo banner, which is correct because the closer is not owner/admin. Demo Mode was not hidden in the sidebar. The API was not changed.
