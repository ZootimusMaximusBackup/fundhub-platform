# Week of 2026-08-24 — to-do list

**Owner:** Chris  
**Status:** this week’s list. Not a build.  
**Ramp board:** [`employee-ramp-system-2026-08-24.md`](employee-ramp-system-2026-08-24.md)

This file exists because there was **no** week list. It only holds known ramp / class leftovers plus the new row Chris named. It does not invent extra chores.

---

## Pending

| ID | Item | Owner | Status |
|----|------|-------|--------|
| W1 | **Watch sales calls → train the closer AI on real data.** Meet words: pair Drive Transcript / Gemini-notes docs on Sync, Whisper short leftovers on a 10-min sweeper, stamp `call_outcomes.transcript`. Coach OP-06 reads those words. Sales floor shows Words ready / Waiting. No new tab. | Chris | `claimed` |
| W2 | Lock the AI / Sarah **PASS_BAR** number | Chris | `pending` |
| W3 | Say if people may **talk to the draft coach** (test chat / shadow). Not built. | Chris | `pending` |
| W4 | Clickable **20-box** camp tracker | — | `pending` |
| W5 | Two scrubbed sample drill reports | — | `pending` |
| W6 | Alec `ai-call-reviewer` scrape back on disk | — | `pending` |

**Not this week / later:** funding-advisor, inquiry, and repair AI coaches. Hermes. Phrase-flag scoring on `call_compliance_flags`.

---

## Done this week (ramp / class only)

Draft closer camp: packs loaded as **waiting**, draft coach **OP-06 Closer drill (beta)** (not live), Sarah gate text. See the ramp board.

---

## W1 law (owner-set 2026-08-24, **updated same day**)

Chris said these three are **LIVE** now. That overrides the earlier “do not promote / do not approve / do not wire” for quizzes, **OP-06**, and the smallest call watcher only.

**Found already (do not invent a dashboard):**

- Closers log outcomes in the call cockpit → `call_outcomes` (`deposit`, `downsell`, `callback`, `no_show`, `not_a_fit`).
- `fetchContext` already pulls recent `call_outcomes` for a client.
- Meet recordings land in Drive. Sales floor **Refresh from Drive** runs `POST /api/company-brain/sync` and stamps `recording_url` when the file name matches.
- Company Brain flags A/V as `needs_transcription` and **does not download** the video.

**Wired this pass:** OP-06 drill turns read recent non-demo `call_outcomes` and label easy vs hard for the coach.

**Words wire (2026-08-24, corrected):** The 576 Drive videos are a **COURSE**, not Google Meet call recordings (owner). Local Whisper keep-alive is **STOPPED** — do not start `scripts/transcribe-newest-meets.mjs` again. 17 course files already have words (~99k words). 0 real Meet recordings in `brain_files`. Live 10-min sweeper stays on but now skips names that are not Meet recordings. Do not put more API money on this pile.

**Wrap tonight:** leftover Whisper / scrape / keychain jobs confirmed dead. Live course-name skip still unshipped (4pm deploy was before the skip). Do not restart.
