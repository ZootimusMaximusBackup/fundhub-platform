# Closer Dashboard screen merge — 2026-08-20

## Shared context

- Owner-approved result: one screen named **Closer Dashboard** at `public/app/closer-dashboard.html`.
- `public/app/closer-call.html` is now a backward-compatible redirect. It carries `location.search` and `location.hash`, so `client_id`, `task_id`, and any other query value survive.
- The fixed `GET /api/read/closer-call` response is the one shared client-data source on the merged screen.
- Credit scores come from `triMerge` inside that response.
- UnderwriteIQ is computed while the same response is built. The screen reads `lite_banner_funding`, personal totals, and combined totals from that response. It no longer calls `GET /api/read/underwrite`.
- The dashboard-only reads remain for their own jobs: tradelines, deal math, and lender matches.
- The shared navigation has one Closer Dashboard row. The Call cockpit row is gone.
- The Present flow, contract send, outcome save, next-call handoff, credit facts, UnderwriteIQ bands, and compliance checklist remain.
- Both calculators now live inside one closed **Payment Calculator** section.

## Task status

- `done` — merge live-call tools into Closer Dashboard.
- `done` — preserve dashboard calculator, tradeline, and lender behavior.
- `done` — make Payment Calculator closed by default and open in one click.
- `done` — make closer-call.html redirect with the full query and hash.
- `done` — remove the Call cockpit row from the sidebar source, shell, and generated copies.
- `done` — update direct links from My Numbers, Present, and staff help.
- `done` — regenerate actual journeys. Output was byte-identical because no route or role gate changed.
- `done` — source tests, lint, and local Playwright.
- `blocked` — `npx tsc --noEmit` has no project to read because this repository has no `tsconfig.json`.
- `blocked` — full `npm test` still has seven unrelated baseline failures listed below.
- `blocked` — manual Cursor-browser smoke could not start a usable browser session. The local Playwright browser suite passed; the separate verifier still owns adversary and real-client proof.

## Change manifest

### Merged screen and data

- `public/app/closer-dashboard.html` — owns the live-call panel, client facts, credit scores, UnderwriteIQ bands, contract send, outcome bar, next calls, compliance checklist, and closed Payment Calculator.
- `public/app/closer-call.html` — redirect stub only.
- `public/app/closer-call.js` — paints the merged live-call area, reads shared client data once from `closer-call`, and sends Save · next call back to Closer Dashboard.
- `src/sales/cockpit.mjs` — adds full UnderwriteIQ output to the fixed closer-call response while retaining `triMerge` scores.
- `api/read/closer-call.mjs` — endpoint comment now names its Closer Dashboard purpose.

### Navigation and links

- `public/app/sidebar.fragment.html` and `public/app/shell.js` — remove the Call cockpit screen and row.
- All 30 sidebar-bearing HTML screens were regenerated by `node scripts/sync-sidebar.mjs`; each generated copy loses only that row.
- `public/app/my-numbers.js`, `public/app/present.js`, and `src/chat/platform-help.mjs` — direct users to Closer Dashboard.
- `public/app/contract-send.js` and `public/app/crm-sidebar.css` — screen-name comments match the merged result.

### Tests

- `src/http/crm-html.test.mjs`
- `src/http/closer-ui-honest.test.mjs`
- `src/http/closer-deck-present.test.mjs`
- `src/http/contract-send.test.mjs`
- `src/http/app-client-carry.test.mjs`
- `src/http/app-nav-reachability.test.mjs`
- `src/http/app-nav-matches-shell.test.mjs`
- `src/chat/staff-assistant.test.mjs`
- `e2e/sales-dashboards.spec.mjs`
- `e2e/sidebar-roles.spec.mjs`

## Verification

- Targeted source tests: **138 passed, 0 failed**.
- `npm run lint`: **passed** — 1,372 files and inline scripts parsed cleanly.
- `npx tsc --noEmit`: **not runnable** — TypeScript printed help and exited 1 because there is no `tsconfig.json`.
- Local Playwright: **50 passed, 0 failed** across merged dashboard behavior, all screen smoke checks, and sidebar roles.
- `npm run journeys`: **passed** — nine files generated; `role-closer-actual.md` and the other generated files were byte-identical.
- `npm test`: **6,280 passed, 7 failed, 3 skipped**. The seven failures are outside this change:
  - stale generated diagrams;
  - two already-untraceable journey gates;
  - `company-brain-affiliate.mjs` org-scope drift;
  - three journey-runner count checks expecting 53 instead of 54;
  - raw outbound fetch in `api/social/generate.mjs`.

## Remaining proof

- Separate verifier: adversary review of the committed diff.
- Separate verifier: real-client proof that scores, `lite_banner_funding`, UnderwriteIQ totals, contract send, and Save · next call all paint and act from the deployed fixed response.
- Separate verifier: backward-link proof with real `client_id` and `task_id`.

## Blockers

No implementation blocker remains. Deployment, merge, pull request creation, and live proof were outside this workflow.
