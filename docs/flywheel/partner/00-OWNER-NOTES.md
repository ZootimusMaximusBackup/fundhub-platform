# Owner notes — partner flywheel

Hand-authored. Agents **append one line, never rewrite**. Same rule as the
intended journey files.

Every correction Chris makes to a stage goes here as one line, and it is fed
back into that stage's prompt on every future re-run. Without this file, re-running
stage 1 loses every correction ever made to it, and the loop oscillates instead
of converging.

Format:

```
YYYY-MM-DD | stage N | the correction, in one line
```

## Notes

2026-08-31 | stage 1 | the avatar is assumed on purpose. Never add a "validate this before spending" rider to anything built on it. Spend at stage 6 is the validation.
2026-08-31 | all | no compliance checking in this pipeline. The product already screens ads before they send, in src/compliance/.
