2026-08-28 | optimize | **COMPLIANCE REVIEW REQUIRED** — `/optimize` cut down to ONE form. Owner: the page looked wrong and did too much. Now a single card — first name, last name, email, mobile, SMS consent — reading like the SmartCredit signup it leads to, then three short what-happens-next steps. REMOVED from the page: the whole Audit section, the Pay-for-Audit checkout button and the five-step roadmap render. The roadmap API (`?view=roadmap`) is untouched and still works; only the page stopped drawing it. Primary button is `Get My Credit Report` and goes to the SmartCredit partner link served by the API (PID 29056), or the Consumer Direct widget mounts in its place once a client key exists. Booking a call is now the secondary action, not the primary one. Copy follows the `/apply` fragment's voice (`clickfunnels-fragments/02a-apply-top.html`). Ghost wordmark no longer bleeds off the page — it was clipping at both edges and the bottom. `src/http/optimize-html.test.mjs` updated because the PRODUCT changed, not to hide a failure: the Audit-UI assertions are gone, and two new guards are added — the page must carry an `sms_consent` box and must keep the shipped STOP wording. Every compliance guard kept: no credit repair, no CRS, no Identity IQ, no score claim, entity is Fundhub Credit Solutions LLC. 10/10 tests pass, lint clean. | Owner: one box, first/last/email/phone, make it look like the SmartCredit signup | (this commit)
