2026-08-30 | role-funding-advisor (no journey file changed — the generator reads routes and gates, and this adds neither) | A ROUND CAN NO LONGER BE MARKED FUNDED WHILE A BANK APPROVAL ON IT HAS NO DOLLAR AMOUNT. Since 2026-08-30 (PR #304) the success fee is a percent of CONFIRMED APPROVALS — Approved applications carrying a recorded amount (docs/CLOSEOUT-FEE-BASIS.md) — so an approval nobody typed a number into is worth nothing on the bill, and once the round closed nobody ever went back for it. That was pure revenue leakage: the screen already warned "1 bank approval is still waiting on its dollar amount" but nothing stopped the round closing anyway. `guardFundedAmount` (src/funding/card-stacking-rounds.mjs), which already refused a funded round with no money at all, now refuses FIRST with `approval_amounts_missing` and NAMES THE BANKS still blank, so the advisor knows exactly what to go and fill in. Server-side only — the board's drag already calls it — and sending a funded amount does not override it, because the blank approval is the problem and a funded amount does not answer it. THE WAY OUT, AND WHY IT IS A FLAG AND NOT A STATUS: a bank can approve and the client never use it, or an approval can be withdrawn, so a hard block with no escape would deadlock a real round on one dead row and turn a revenue guard into a support ticket. New columns `applications.approval_excluded_at / _by / _reason` (db/migrations/272_approval_excluded.sql) record that an approval does not count, with WHO and WHEN, plus an `application_decisions` row exactly as every other decision writes one. `applications.status` is deliberately left at 'Approved': that column is what the BANK said, and src/plays/outcomes.mjs reads approval-vs-denial straight off it ('Approved' to yes, 'Denied' to no), so writing 'Denied' would invert the approval rate and inventing a third status would delete a real bank yes from that report. The audit row carries status NULL so an exclusion never shows up in the bank yes/no history as an outcome at all. An excluded approval also drops out of the fee basis even when it does carry an amount — an approval we are not billing for is not billed at any size. NULL still means unknown and still blocks (CLAUDE.md section 12); nothing defaults to 0, and an explicit zero is still refused as a denial rather than accepted as an approval. A round with no approvals behaves exactly as it did before, and an already-funded round is never re-blocked. Escape wired into the existing /api/applications (no new endpoint, no new screen) and onto the existing lender row in client-control-panel.html as a "Doesn't count" button that asks for a reason. 13 new tests in src/funding/approval-amount-guard.pg.test.mjs. | An approval left blank was money we never invoiced, and a guard with no way out would have deadlocked real rounds. | (this commit)
2026-08-30 | (no journey changed — this is CSS only, and recorded deliberately) | THE CONTRACT TEMPLATES SCREEN HAD 30 FONT SIZES WRITTEN INTO IT AND 29 OF THEM PAINTED NOTHING. `public/app/fundhub-brand.css:184-186` sets `font-size:inherit !important` on every element inside `.app`, and an important author rule beats a normal one no matter which file loads last — so every plain `font-size:<n>px` in this screen's own `<style>` was discarded by the browser before it ever reached the page. The screen rendered flat at 16px body, which reads as a design problem, which is how a file ends up with thirty sizes that have never once had an effect (UI-STANDARDS §12.7). 28 of them were in the `<style>` block and 2 were inline `style="font-size:…"` in the markup, which the same rule also eats. WHAT WAS DELETED AND WHY IT IS SAFE: 23 declarations sat on names the brand file ALREADY sizes — `h1` and `h2` (`--fs-title`, 20px), and `.badge`, `.note`, `.hint`, `.mono`, `label`, `th` (`--fs-caption`, 13px) — or on names that correctly take body size (`.btn`, `td`, form inputs, `.msg`, `.chip2`'s siblings). Removing a declaration the browser was already throwing away cannot change a pixel, and that is the property that was measured rather than assumed. ONE ESCAPE HATCH, NOT TWO: six of this screen's own class names are captions by job and had no way to say so, so they now sit in a single `!important` rule with the reason written above it, copying the shape of `client-control-panel.html:100-106` — `.crumb` and `.foot` (the mono-caps eyebrow in the top bar and the meta strip in the footer, the `.eyebrow` role under a different name), `.tools h3` (a section heading; §12.7 says those are 13px caps-mono and an `h3` is not on the title whitelist), `.chip2` (`.chip` is on the brand's caption list, this is the same control under a second name), `.pg .pn` (the page label, positioned at `top:-11px`, which at 16px was taller than its own offset), and `.box` / `.box .bl` (a field marker sized to a real PDF field rectangle, often under 20px tall — a 24px-tall label did not fit inside an 18px-tall box, measured). `body{font-size:13px}` is the ONE size on this screen that was NOT dead, because `body` is not inside `.app`: it reached the search dialog and the data banner that `shell.js` injects outside the screen. It is now `var(--fs-body)`, which is what 16 of the 19 CRM screens already use, so those shared pieces render here at the same size they render everywhere else. PROVEN, NOT ASSERTED: a local copy of the screen was served against the live backend by Playwright route interception, before and after, at 1440x900 and 390x844, with the wording editor and the PDF field editor both open, and the computed font size and box of all 293 elements inside `.app` compared element by element. THE ONLY DIFFERENCES ARE THE EIGHT THE ESCAPE HATCH RECLAIMS — `.crumb`, the two `.tools h3`, `.foot` and its three spans — plus two knock-on boxes (the top bar's spacer grows 11px because the eyebrow got narrower, and the page is 5px shorter because the footer text did). Every other element is byte-identical in size and position. No horizontal scroll at 390 either way. The three PDF-editor selectors are unreachable in the live data (no PDF-backed template exists) and were proven separately by injecting the markup and reading the computed size: 16px before, 13px after, on all of `.pn`, `.box`, `.bl` and `.chip2`. NO WORDING CHANGED — not one user-visible string, empty state included; `src/http/crm-html.test.mjs` pins that and passes. NO PAGE, SCREEN, TAB OR MENU ROW ADDED, no `--fh-maxw` touched, no width cap added, no migration, no route. The LIVE indicator already matched the house shape (`<span class="chip on" id="liveChip">` with `<span class="cd">`, the same as `agent-editor.html` and `content-admin.html`) and there is no org pill on this screen, so neither consistency item needed a change. MEASURED on macOS, worktree `wf_0508cbcf-e62-4`, branch `fix/contracts-dead-type`: lint clean, 1602 files. Unit phase 7218 tests / 7209 pass / 6 fail / 3 skipped with no database, 7220 / 7213 / 7 fail / 0 skipped against a scratch Postgres 16.14 database built from zero (217 migrations) — THE SAME FAILURES BY NAME AS THE BRANCH BASELINE, all pre-existing, and the extra one with a database connected is the documented superuser artifact from CLAUDE.md §12. The 5 files holding those failures were run against HEAD and against this branch: 78 pass / 5 fail both times, identical. The 8 tests that actually read `contracts.html` or the shell — `contracts-screen`, `crm-html`, `shell-is-fluid`, `app-nav-matches-shell`, `app-nav-reachability`, `mobile-shell`, `contract-send`, `portal-contracts` — are 205 tests, 205 pass, 0 fail. The pg phase does NOT run under `npm test` because the runner exits after the unit phase when it is red, so it was run by hand, serially, against the scratch database: 134 files, 1924 tests, 1895 pass, 28 fail, 1 skipped — every one of the 28 a partner-isolation / row-level-security test, which is the class CLAUDE.md §12 says fails when the connection role owns the database, and no `.pg.test.mjs` file reads `contracts.html` at all. `src/ui/screen-standard.test.mjs` (workflow 1's, run from its worktree copy) names ZERO offenders in `contracts.html`; the screens it still names belong to other workflows. | Thirty font sizes in one screen, twenty-nine of which the browser had always thrown away | (this commit)
2026-08-30 | role-owner (galaxy.html — no journey diagram changed) | TWENTY-FOUR FONT SIZES IN public/app/galaxy.html WERE PAINTING NOTHING AT ALL. fundhub-brand.css:184-186 sets `font-size:inherit !important` on every element inside .app, so a plain px size written in a screen's own <style> is discarded by the browser before it ever reaches the page. Every one of galaxy's 24 was plain. Removing them is therefore a no-op, and it was measured as one: the screen was rendered against the live app twice in the same session with only this diff swapped in, and .brand .sub, footer.statusbar, .av, .clock, .topbar-right, .census, .live-pill and .p-name all computed to the identical size before and after. Two groups genuinely had to be captions and are galaxy's own class names, so they were handed --fs-caption in ONE !important rule with the reason written above it, per UI-STANDARDS §12.7 — the reading-key strip (.legend) and the worker panel (.p-*). Those two moved 16px -> 13px, which is the only visible change and is what stops the 22px-tall key bar clipping 763px of its own legend at 1440. | The screen looked flat and half-clipped, someone reads that as a design problem and writes more px sizes that also do nothing — UI-STANDARDS §12.7 calls that loop most of Fundhub's screen drift. src/ui/screen-standard.test.mjs names galaxy.html until the dead sizes are gone. | (this commit)
2026-08-30 | (no journey file changed — recorded deliberately, and the reason matters) | THE SCREEN STANDARD IS NOW WRITTEN DOWN, AND A TEST KEEPS IT TRUE. THE ROOT CAUSE: no settled standard for what a Fundhub screen looks like existed in writing anywhere an agent could see it. On 2026-08-19 two screens were reworked on the same day with OPPOSITE instructions and BOTH agents cited `docs/UI-STANDARDS.md` as their authority — neither was lying, because that document said nothing about what a container looks like at rest, what a topbar must carry, or that a font size written in a screen's own `<style>` is thrown away by the brand file. Each agent filled the gap with a reasonable guess and the guesses did not match. Worse, `docs/UI-STANDARDS.md` was reachable ONLY through a Cursor rule set to `alwaysApply:false`, which Claude Code never loads, so the document an agent was supposed to follow was invisible to it. THREE THINGS, and the smallest is the most important. (1) `docs/UI-STANDARDS.md` gains **§12 THE SCREEN FRAME**, written from what `public/app/pipeline.html` actually does (owner-set: pipeline is the reference for good): the resting container is `#fff` + `1px solid var(--line)` + a radius + `box-shadow:var(--panel-shadow)`; the shadow is NEVER written on a screen because `fundhub-brand.css:130-137` already gives it app-wide to `.card .stat .panel .tabs .editor .fos-panel .kpi-tile .big-tile .calc-panel .compose .ctx-card`, so a new container takes one of those names or joins that list; every OTHER shadow in pipeline.html is an interaction state, not decoration (hover :242, drop target :225, drag ghost :308-309, menu :315-316) — resting elevation is the token and anything stronger is an event; `border:0` on a shadow-list class is forbidden because it paints a floating shadow with no edge; a box that HOLDS boxes takes a flat tint and a hairline, never a shadow (`.col`, pipeline :215); status is never colour alone, because `paintBrand()` in `shell.js:2277-2286` overwrites `--alert/--warn/--ok/--info` from the tenant's own colour ramp and a single-hue white-label brand therefore paints every status identically — pipeline :243-244 survives that with a LEFT STRIPE and a tint, and :263-268 keeps literal amber with the reason written above the rule; the type trap and its ONE escape hatch per screen; and the topbar contract read off `client-control-panel.html:64-84`, including the note that `partner-galaxy.html:79-85` sets `flex-wrap:wrap` AND `overflow:hidden` on the same bar, so nobody copies it. (2) ONE LINE IN `CLAUDE.md` §3 — "Touching anything under `public/app/`? `docs/UI-STANDARDS.md` is law. Read it first." — which is the highest-leverage edit here, because it is what makes the standard reachable at all. (3) NEW `src/ui/screen-standard.test.mjs`, modelled on `src/ui/shell-is-fluid.test.mjs` (same screen discovery, same hand-written CSS walker, same house rule that a failure message must TEACH): no px font-size without `!important` in a shell screen's `<style>`; no topbar that wraps and then clips or pins a height; no shadow-list class dropping its border; and `CLAUDE.md` must still name `docs/UI-STANDARDS.md`. **IT LANDS RED ON PURPOSE — 5 tests, 2 pass, 3 fail — and the failure list IS the burndown for the screens phase.** MEASURED, NOT REPEATED: 210 dead px font-sizes across 10 of 29 shell screens — calendar 58, my-numbers 30, contracts 28, partner-galaxy 28, sales-floor 27, galaxy 24, closer-dashboard 9, consent-capture 4, client-control-panel 1, pipeline 1. The brief's prior figures for my-numbers (31) and contracts (30) counted inline `style=` attributes as well as `<style>` blocks; both are dead, and the inline ones are counted separately (closer-dashboard 6, contracts 2, my-numbers 1) plus `font:` shorthand (consent-capture 21, inquiry-remover 7, pipeline 2). ONE OFFENDER THE BRIEF DID NOT NAME: `ops-admin.html:128` `.aff-body .card{border:0}` keeps the app-wide shadow with no edge, the same fault as `closer-dashboard.html:102` and `:111`. `.gitattributes` is new and carries `docs/journeys/CHANGELOG.md merge=union`, because every branch in this batch appends to this file and would otherwise collide on the first line — "keep every entry" is exactly Git's built-in union driver. NO SCREEN WAS EDITED, no page, screen, tab or menu row was added, no copy was reworded, `--fh-maxw` was not touched, and no migration was added. | Two agents redesigned two screens on the same day from the same rulebook and got opposite answers, because the rulebook did not say what a screen looks like | (this commit)
2026-08-30 | role-funding-advisor (no journey file changed — this is a value on an existing step, not a new step; `npm run journeys:check` reports docs/journeys up to date) | THE BANK'S EMAIL STATES THE APPROVED AMOUNT, THE SYSTEM ALREADY FOUND IT, AND THEN THREW IT AWAY — so a funding advisor retyped by hand a number Fundhub already had, and when they forgot, the approval carried no dollar amount at all. Since PR #304 the success fee is a percent of CONFIRMED approvals — approvals with a recorded amount — so a forgotten box is a bill that never goes out. `src/adapters/mailgun.mjs:96` has always run a dollar pattern over every inbound bank email; it used the result only to nudge classification confidence and discarded the figure. Two things upstream destroyed the evidence as well: the event carried no body text at all, and `src/handlers/comms.mjs` bound the SUBJECT LINE to both the `subject` column and the `body_preview` column of every `bank_inbox` row — so each row showed the same sentence twice, the paragraph stating the amount was gone forever, and because `body_preview` was always populated the screen's `body_preview || classification || received_at` fallback could never reach its second or third option. FIXED IN FOUR PLACES. (1) The adapter now carries the figures: `findDollarAmounts` gathers every DISTINCT dollar figure from the subject and body, as fixed 2dp dollar strings, cents read as whole numbers so $450.10 never becomes $450.09. (2) `bodyPreviewOf` stores a real preview — whitespace flattened, capped at 500 characters with the cut marked, NULL when the email has no text. The WHOLE body is deliberately still not stored: `src/http/router.mjs:110` records what an uncapped body did to a shared production Postgres, and the back half of a forwarded bank statement is account detail nobody asked us to keep. (3) `onMailResponse` writes the preview into `body_preview` instead of a second copy of the subject, and NULL when there is none. (4) `GET /api/read/bank-inbox` returns two named keys out of `raw` — `amount_candidates` and `amount_candidates_found` — and still never returns the `raw` column itself; the endpoint's staff role gate, org gate and client gate are unchanged. ON SCREEN: the figures appear as one-click buttons immediately beside the Approved $ box on each lender row of the client control panel, plus one sentence above the list saying which email they came from. THREE RULES, DELIBERATE. It is a SUGGESTION — clicking fills the box and saves nothing; only Bank yes writes, and only after FHMoneyInput.parseAmount accepts what is in the box. It NEVER PICKS — a bank email routinely states a credit limit, an APR, an annual fee and a minimum payment in one paragraph, so when there is more than one figure the screen says outright "we cannot tell which one is the approval" and offers all of them. And NO FIGURE MEANS NO SUGGESTION, never a zero (CLAUDE.md §12). A DENIED email is not scanned at all — the number in a denial letter is the limit being refused, the most dangerous figure in the mailbox — and the classifier itself is untouched: the negation work from PR #298 that makes "not approved" stay DENIED was not edited, and a test pins its decisions. MEASURED on branch feat/bank-email-amount-suggestion, macOS, worktree agent-a1fdd568f7c9b2724. Unit phase 7295 tests / 7286 pass / 6 fail / 3 skipped, against clean main at 7e8f0da7 7259 / 7250 / 6 fail / 3 skipped — THE SAME 6 FAILURES BY FILE, none new; 36 unit tests added, plus 2 database tests and 9 browser tests. Database phase run separately (the runner exits after a red unit phase, so pg files skip silently) against a throwaway local Postgres 16.14 with all 218 migrations applied to it empty: 1942 tests / 1913 pass / 28 fail / 1 skipped as the database owner. 26 of those 28 are the partner-isolation suites, which bypass row-level security entirely when the connection is an owner — re-run as `fundhub_app` exactly as CI does, they are 109 tests / 109 pass / 0 fail. The other 2 are in `src/affiliates/economics.pg.test.mjs`, which imports only db.mjs and economics.mjs, fails on its own with nothing else running, and cannot be reached by this change. Playwright 9 new browser tests pass, and funded-amount + proxy-apply + screens-smoke (54 tests) stay green. Lint clean. | An approval a funding advisor forgot to type an amount into is worth nothing on the bill, and the bank had already told us the amount | (this commit)
2026-08-30 | (no journey changed by this work — recorded deliberately) | **COMPLIANCE REVIEW REQUIRED** — consent capture. The label is on the file, not on a behaviour change: this edit touched `public/app/consent-capture.html` and changed no logic, no wording and no pixel. TWENTY-FOUR OF THE SCREEN'S TWENTY-FIVE FONT SIZES WERE PAINTING NOTHING — AND THE TWENTY-FIFTH WAS NOT, WHICH IS THE CORRECTION THIS ENTRY RECORDS. `public/app/fundhub-brand.css:184-186` sets `font-size:inherit !important` on every element INSIDE `.app`, so a plain px size written in a screen's own `<style>` is thrown away before it reaches the page. That is a descendant rule, and `body` is not a descendant of `.app`. The first version of this change converted `body{font-size:13px}` to `var(--fs-body)` along with the other 24, on the premise that everything was dead. It was not. `shell.js:1644` appends the CRM's global search dialog to `document.body`, outside every shell wrapper, and the magnifier glyph inside it (`shell.js:1638`) is the one child of that panel with no font of its own — so it inherits straight from `body`. `chat-widget.js` hangs five more containers off `body` the same way. Measured in real Chrome against the live backend at 1440x900 and 390x844, with the dialog actually opened: the glyph went 13px to 16px (7.83x19.5 to 9.64x24), the search bar 49.80px to 53.00px tall, the panel 126.64px to 129.84px, and 47,571 pixels moved at 1440 and 31,871 at 390 — against a same-file noise floor of 441 and 181. So `body` is left at `13px`, with the reason written above it in the file, and only the 24 genuinely dead declarations are converted to the tokens that name what the brand file actually paints (`--fs-title` for the h1 and h2, `--fs-caption` for the rest). NO ESCAPE HATCH WAS ADDED — no new selector, no new rule, 24 straight substitutions. PROVEN INVISIBLE RATHER THAN ASSUMED: 261 elements measured before and after at both widths, with the search dialog closed AND open — 1,044 element comparisons, ZERO differences in font-size, line-height, weight, family or bounding box, and 0 differing pixels at 390 with the dialog open. `src/ui/screen-standard.test.mjs` still names this screen once, for the `body` line; that is a blind spot in the test, which scans every px font-size in a `<style>` block without checking whether its selector is inside one of the five wrappers, and a body-level rule is genuinely live. Suite unchanged against `main`: 7300 tests, 7287 pass, 10 fail, 3 skipped, same ten failures by name on both. All 120 tests that actually read this file pass, including the database one. | Verifier proved one of the 25 declarations was live; corrected in place on PR #310 rather than shipping a visible change | bcf106b1
2026-08-30 | (no journey file changed — the flow is unchanged; what a funded round PRODUCES changed) | EVERY FUNDED ROUND MADE A TASK INSTEAD OF A BILL, so no client has ever been invoiced automatically. F-07 (`src/workflows/f-07-funding-locked.mjs`) refused to raise the success-fee invoice unless the `round.funded` event carried a fee percent, and nothing ever put one there — not the Card Stacking emitter (`src/funding/card-stacking-rounds.mjs`), not the Lendflow adapter. Every funded round fell into the not-ready branch: a tag, a task reading "Fix fee lock/percent before invoicing", and no invoice, no email, no SMS. Verified against a scratch Postgres on `main` at 3939d23d before the fix: payload feePercent undefined, 0 invoices, 0 messages, that exact task. Both existing test files were green throughout — the emitter's test never ran F-07, and F-07's own test handed itself a feePercent no real event carried. THE FIX: the event now carries the fee percent, the confirmed approved total, and the sale + round ids (the ids are the idempotency key that stops a replay billing twice). THE BASIS ALSO CHANGED, owner-set 2026-08-30, reversing 2026-08-04: the fee is a percent of CONFIRMED APPROVALS — the sum of `applications.approved_amount` on Approved rows that carry a recorded amount — not `funding_rounds.funded_amount`. Chris: "we are getting people approved, NOT funding the actual credit cards... make sure we bill based on confirmed approvals." Defined once in the new `src/funding/success-fee.mjs`, which both the invoice and the closeout record read, so they cannot disagree. The rate is `sales.agreed_success_fee_percent` frozen on the client's sale, never a hardcoded 10. A round with nothing confirmed, or a sale with no agreed rate, now REFUSES with a named reason (`no_confirmed_approvals` / `no_agreed_fee_percent`): no invoice, no closeout row, and a task in plain English — never a $0 invoice. `money(null)` in `src/funding/closeout.mjs` returned 0 and would have written a silent $0 fee row; it returns null now and every caller treats null as a refusal. COMPLIANCE REVIEW REQUIRED (fee basis). | The owner settled the fee basis and the invoicing path was dead in a way no test could see | fix/success-fee-confirmed-approvals
2026-08-30 | role-inquiry-remover | THE DESK COULD NOT BE TRUSTED AFTER A WRITE, AND TWO OF ITS BUTTONS LIED IN DIFFERENT WAYS. Three fixes in `public/app/inquiry-remover.html` (the Specialist desk — Inquiry queue and Repair desk on one page). (1) COLUMNS. `paintRow()` indexed `r.cells[3]` and `r.cells[4]` — leftovers from a five-column Work Queue that no longer exists — so pressing "Mark confirmed" put the attempt count in the EXPECTED column and the status pill in CALL STATE, while the real Attempts and Status columns never moved: the row read "Removed" in the wrong place and still read open in the right one. Now `COL_ATTEMPTS = 6, COL_STATUS = 7`, derived from and pinned against the table's own header, and the same stale pair is fixed in `rowState()`'s DOM fallback. The comment above `paintRow` claimed the cell held a "2d 14h" stuck age; that column and its sample rows were deleted long ago and the sentence was pointing the next reader at the wrong two cells. (2) LISTENERS. `loadRepairDesk()` runs again after every Send, Stage, Enroll and drawer Send, and it re-bound the letter drawer, the PULL modal, a `document` keydown and all five filter tiles on each pass without removing the old ones. Two handlers on one tile ran the toggle twice — the first set `aria-pressed="true"`, the second read that and turned the filter straight back off — so one Send killed the desk's only way to sort itself, and the Escape listener accumulated one copy per reload for the life of the page. The chrome and the state it touches now live in a `repairDesk` IIFE and are bound once; `loadRepairDesk` binds only to rows it has just created and is published as `window.loadRepairDesk`. (3) SOFT PULL. `runSoftPull()` fired three POSTs at `/api/finance/crs-pull` and discarded every answer (`.then(next).catch(next)`), closing the box and reporting success by saying nothing. It now goes through `FHData.write`, stops at the first failure, and says so in `FHData.explain`'s house wording — mirrored into the desk's own error line because explain()'s "sample"-tone cases paint nothing on the bottom strip. OWNER-SET DECISION, `inquiry_specialist` DOES NOT PULL CREDIT: a soft pull is a real paid three-bureau order and an erasure cannot be undone; this desk disputes what is already on a report rather than ordering one, and `role-inquiry-remover-intended.md` already lists Finance and privacy among the routes this role stays blocked from. So the role was NOT added to `CRS_PULL_ROLES`; instead "Soft pull" and "Clean personal info" no longer render for roles their endpoints refuse (docs/UI-STANDARDS.md §5 — no control renders for a role that lacks permission), and both still render and work for owner and admin. No journey file changed and `npm run journeys:check` is clean: the journeys are generated from the routing table and the gate on each handler, and no route and no gate moved — the screen moved TO the journey, not away from it. Proof: `e2e/specialist-desk.spec.mjs` (5 browser tests; the four bug cases were confirmed to fail against the pre-fix file and pass after) plus source pins in `src/http/inquiry-remover-view.test.mjs` and `src/http/repair-desk-screen.test.mjs`, the latter now comparing the screen's role lists against `api/finance/crs-pull.mjs` and `api/privacy/erasure.mjs` so the two halves of each gate cannot drift. | A credit-report action that appeared to have happened and had not — the file's own header calls that the worst thing this page can do to somebody — plus a filter that died after one Send and three permission refusals that vanished into an empty catch | (this commit)
2026-08-30 | affiliate (no journey file changed — recorded deliberately) | THE AFFILIATE SCREEN HAD A 12% COMMISSION RATE HARDCODED IN IT WHILE THE REAL TIER 1 RATE IS 20%. `public/app/affiliate.html` carried `var RATE=0.12` and multiplied each referral's basis by it to fill the Commission column. Tier 1 is 20% and Tier 2 is 5%, owner-set 2026-08-24 and shipped in `db/migrations/261_affiliate_tier1_20pct_20260824.sql`, so every affiliate would have been shown a number 40% short of what they are owed the moment that table had a row in it. It was dormant only because `var LEADS = []` is fetched by nothing — a live bug waiting on a data source, not a harmless mock. THE FIX: the constant and the browser-side `commission()` are deleted. The Commission cell prints `affiliate_referrals.commission_due` — the figure `convert()` in `src/affiliates/economics.mjs` already worked out once and stamped with the exact rule and rate that produced it in `rule_snapshot`. Recomputing it here would have let the screen and the ledger disagree with nothing to say which was right, and a single constant cannot express a rate that moves by tier, by product and by date anyway. Nothing in that script multiplies money now, and a test fails if a multiply comes back. NULL SURVIVES: a conversion with no rule in force has `commission_due` NULL, which is UNKNOWN — the cell prints a dash, never $0 and never a figure the browser invented. "Total shown" adds only the rows it knows and names how many it left out rather than counting an unknown as zero, and that cell now ships as a dash instead of `$0`. COVERED BY `src/http/affiliate-screen.test.mjs` (14 tests through the new FH-AFFILIATE-COMMISSION block, the repo's existing vm pattern) and `e2e/affiliate-commission.spec.mjs` (5 tests in a real browser); putting the old rate back fails 7 of them. NOT REGENERATED, AND WHY: `npm run journeys` was run and produced no change — the extractor reads the routing table and the gate on each handler, and this adds neither. ALSO CHECKED, DELIBERATELY LEFT: the "Business" column header. `clients` has no business-name column, confirmed against `db/schema/001_init.sql` and every later ALTER; the only business name in the schema is `client_custom_fields.business_name`, and the affiliate contract in the Terms tab on this same page does grant the affiliate a business name. So the header is not wrong and was not changed — it is documented in place instead, so that nobody filling this table later substitutes a client's first and last name, which that contract does not allow. | A commission 40% short is the kind of mistake only the person it shorted ever finds | (this commit)
2026-08-30 | (no journey file changed — the generator reads routes and gates, and this adds neither) | "DAYS IN STAGE" ON THE PIPELINE BOARD WAS ACTUALLY THE CARD'S AGE. `cards.entered_at` is stamped once by `DEFAULT now()` at insert (`db/schema/001_init.sql:215`) and NOTHING HAS EVER UPDATED IT — verified across all 185 migration files and the whole schema: there was no trigger on `cards` touching it, and both production writers run a bare `UPDATE cards SET stage_id` and leave it alone. So `public/app/pipeline.html:1424` printed a number labelled "in stage" that meant "since this card was created": a card moved into Funded five minutes ago read `20d in stage`, byte-identical to one that had genuinely sat untouched for twenty days. The single number that tells a funding advisor which round has gone quiet could not tell them anything. TWO MORE CONSUMERS RODE THE SAME COLUMN: the "Over 3d in stage" filter (`pipeline.html:573-575`) selected OLD cards, not STALLED ones — it parses the minutes back out of the text the board printed, so it inherited the fault exactly; and `src/dashboard/kpis.mjs:125-133`, the `pipeline_movement` KPI, counted cards CREATED in the window while its own comment called them cards that changed stage. A TRIGGER, NOT APPLICATION CODE, AND THE REASON IS COUNTABLE: two production writers already move a card, in two different files, neither aware of the other — `moveCardToStage` (`src/workflows/cards.mjs:98`) and `placeWhiteLabelRailCard` (`api/public/partner-apply.mjs:87`) — and a third path moves them in raw SQL, `161_optimization_repair_pipeline.sql` having bulk-repointed every optimization card at a new stage. Application code would have to be right in all three and in every future one; `db/migrations/271_cards_entered_at_on_stage_change.sql` is right once and cannot be forgotten by the next person who types `UPDATE cards SET stage_id`. ONLY ON A REAL CHANGE: the `WHEN` clause gates on `IS DISTINCT FROM`, so re-writing a card's CURRENT stage does not reset the clock — and that matters, because both writers are find-or-create and documented as safe to run twice, so a retried webhook would otherwise have made a stalled card look fresh every time it fired. A second half of the same clause skips the trigger when the statement also set `entered_at` itself, so a fixture can still backdate a card on purpose rather than waiting four days for a genuinely stale one. `kpis.mjs` SQL IS UNCHANGED and only its comment moved: the query always asked the right question and the column can finally answer it; a newly created card still counts, correctly, because it did just enter its first stage. NO BACKFILL, AND `cards.updated_at` IS THE NEAR MISS THAT WAS CONSIDERED AND REJECTED: every `UPDATE cards` in the codebase happens to be a stage write, so 001_init's generic `trg_cards_updated` superficially looks like a stage-change log — but it carries no `WHEN` clause, so it also fires on the idempotent re-write of a card's current stage, which happens constantly, and it fired for every optimization card at once when 161 ran. Backfilling from it would stamp "moved recently" onto cards that were merely touched, which is this exact bug pointed the other way and strictly more dangerous, because it HIDES a quiet round instead of over-reporting one. There is no card-history table, no audit row, and `events` holds `round.*` payloads for one pipeline only, keyed by client, naming no card and no stage. So existing rows keep their creation time, read too STALE rather than too fresh, and become correct forever on their next real move. PROVEN BOTH DIRECTIONS: 10 new tests in `src/workflows/cards-entered-at.pg.test.mjs` against real Postgres, and they were checked backwards — with the trigger DROPPED, 6 of the 10 go red (the ones asserting the clock moves) and 4 stay green (the ones asserting it must NOT move on a no-op, an owner change, or an explicit backdate), so neither half can pass vacuously. Both production writers are covered by name, including the partner rail that a one-file application fix would have missed. Plus 3 new Playwright tests in `e2e/pipeline.spec.mjs` driving the real board in a real browser: a card seven minutes into its stage prints `7m in stage`, one five days in prints `5d in stage`, and "Over 3d in stage" keeps the stalled card while dropping the just-moved one — 6/6 pass in that file. MEASURED, macOS, worktree `agent-a7f8f459364b28688`, local Postgres 16.14 (Homebrew) on a scratch database created for this run with all 218 migrations applied to it empty. Unit phase, DATABASE_URL unset: 7218 tests, 7209 pass, 6 fail, 3 skipped — and clean `origin/main` at `3939d23d`, measured the same way in the same worktree, gives 7218 / 7209 / 6 / 3, THE SAME SIX FAILURES BY NAME, none new and none mine. Database phase run separately because `scripts/run-suite.mjs` exits after the unit phase when it is red and it is red on main: 135 files, concurrency 1, 1934 tests, 1905 pass, 28 fail, 1 skipped as the database OWNER — all 28 are the partner-isolation assertions CLAUDE.md §12 describes, which the owner role bypasses RLS for. A/B'd to settle it: the six failing files re-run with the trigger DROPPED and again with it PRESENT give 129 / 103 / 26 both times, failure names byte-identical. As the unprivileged `fundhub_app` role — the way production connects — the 10 new tests are 10/10, `npm run guard:db` 3/3, `npm run guard:rls` 4/4. Lint clean, 1603 files. `npx tsc --noEmit` is a no-op in this repo: §6 lists it but no tsconfig exists, so it checks nothing. `npm run journeys:check` reports all 9 files up to date. NO new endpoint, screen, tab, nav row, table or column, and the way the age is DISPLAYED is untouched — only the underlying number is now true. | Every "days in stage" number on the board was the card's age, so a card that moved five minutes ago looked as stale as one stuck for three weeks | (this commit)
2026-08-30 | (no journey file changed — recorded deliberately, and the reason matters) | A CLIENT'S PAYMENT NOW MOVES THEM ONTO THE BOARD THAT DOES THE WORK. Fundhub is event-based: landing a card on `funding_card_stacking / apply_now` already fires `round.started`, which assigns the funding advisor, provisions the inbox and messages the client. That cascade worked. NOTHING LISTENED FOR THE PAYMENT AND PERFORMED THE MOVE, so a person had to drag the card, and when nobody did there was no advisor, the client's portal never moved, and the 24-hour guarantee ran out — the first automatic warning being "deals going cold" seven days later, on a screen only the sales manager opens. `src/handlers/purchase-routing.mjs` is the missing link: a new bus handler on `deposit.paid` / `sale.closed` / `payment.received`, registered in `src/register-all.mjs` AFTER `registerMoneyChain()` so the sale exists before it is read. It routes on the owner's rule — funding purchase -> `funding_card_stacking/apply_now`; repair purchase -> `optimization/intake`; both purchased -> both boards with the funding card held on the EXISTING hold (`round_hold_reason = "Funding Paused"` + the `funding:paused` tag, cleared by the existing clean-snapshot path in `src/crs/snapshot-negatives.mjs`); repair bought with funding only signed for -> repair board plus the `client:funding` tag and NO funding card. Never `funding_altfin` (Lendflow only) and never `inquiry_removal` (it starts itself). WHICH SIGNAL: `products.category` on the client's active sales, not the product-code lists in `src/affiliates/economics.mjs` — those name only 'repair-bundle', so the $200 'repair-trial' buyer would have got no repair card at all; category is also what `money-chain.mjs:818` already routes on. `clients.outcome_tier` via `isFundingPath()` is the second signal and does one job only: separating "bought both" from "repair now, funding later". SAFE TO FIRE TWICE: state is re-derived from the sales table every time and a card is placed only when that board has none for the client, so a replay, a duplicated webhook or a second payment moves nothing twice and can never drag a `funded` client back to `apply_now`. Also fixed in the same commit: s-06's "Funding intake — pull CRS" task was assigned to `closer` with no due date. It is fulfilment work, not sales work, and `public/app/calendar.html` drops every task with no due date — so the task existed and reached nobody. Now `funding_advisor`, due in 24 hours, dedupe unchanged. WHY NO JOURNEY FILE MOVED: every `-actual.md` in this folder is generated by `scripts/journeys/generate.mjs` from the ROUTE table and the gate on each handler — they document who can open which URL, not what happens when an event fires. This change adds no route, so `npm run journeys` reproduces all nine files byte-for-byte. The event chain has no journey file to update, and that gap is the finding, not an omission. | 0b96686a
2026-08-29 | client | **COMPLIANCE REVIEW REQUIRED** — credit-repair messaging, customer-facing copy. THE "READY LETTERS" EMAIL NOW POINTS AT THE PORTAL ITSELF, AND THE PORTAL CARRIES A SIGNED-OUT CLIENT'S EMAIL INTO SIGN-IN. Owner, same day, third call: "just send it to the portal." The earlier attempt pointed the link at `/portal-login.html`, so EVERY client — including one already signed in — was handed a sign-in form before they could see anything. The link is now `{{portal_url}}`, a NEW merge tag resolving to `/app/client-portal.html?email=<theirs>`, added to `clientContext()` in `src/workflows/messaging.mjs` beside the existing `portal_login_url`. Chris's two sentences are still untouched — not one word has moved across any of the three passes. A NEW TAG, NOT A REPOINT: `portal_login_url` still means the sign-in form and is still what `db/seed/009_u02_funding_delivery_template.sql` and `db/migrations/253_repair_email_templates.sql` use for six other emails, so changing its meaning would have silently moved all of them. `portal_url` was added to BOTH `RESOLVABLE_TAGS` and `AVAILABLE_TAGS` in `src/messaging/merge-tags-registry.mjs` in the same change, so the template editor accepts it and offers it — confirmed by running `classifyChange` over the real before/after bodies, which returns nothing blocking. That registration is the whole point: the tag the previous pass rejected (`custom_values.portal_link`) was rejected precisely because it was NOT registered, and adding an unregistered tag would have frozen this template against every future staff edit. **THE CORRECTION TO THE BRIEF, AND IT MATTERS: THE SIGNED-OUT REDIRECT ALREADY EXISTED.** The instruction said `public/app/client-portal.html` has no redirect and a signed-out client gets a half-dead page. It does have one. `public/app/shell.js` is loaded by that page (line 372) and its pass-2 session check already sent a signed-out visitor to `/portal-login.html` via `signInUrl()`. The "You are signed out. Sign in and try again." string at client-portal.html line ~2034 is the message shown when a SIGNATURE SAVE returns 401, not the page-load state — a different code path entirely. Proven, not assumed: the new test's first case passes against the UNMODIFIED `shell.js`. **WHAT WAS ACTUALLY BROKEN WAS THE EMAIL ADDRESS, AND THAT IS NOW FIXED.** The bounce threw the `?email=` away, so a signed-out client following the new link would have arrived at an EMPTY address box and had to type in the address the email already knew — strictly worse than the link it replaced. `signInUrl()` now reads `email` off the current URL and carries it: `/portal-login.html?email=<theirs>`, which `prefillEmailFromQuery()` on that page drops straight into the box. Carried only when present — a cold visit with no parameter still gets the bare page. `?next=` is still deliberately NOT added, unchanged, because that page reads only `email` and `t`. NO NEW PAGE, SCREEN, TAB, MENU ROW, ROUTE, GATE, TABLE OR MIGRATION, and no new sign-in mechanism — no magic code was invented, the existing emailed-link flow is untouched. PROVEN BOTH DIRECTIONS by 6 new tests in the new `src/http/portal-signed-out-bounce.test.mjs`, which EXECUTES `shell.js` against a stub browser rather than scanning it as text (the same harness `src/http/app-client-carry.test.mjs` uses, because a regex would pass on code that never runs): a signed-out visitor is bounced to the CLIENT sign-in page and not the staff one; the address is carried; an address needing encoding survives intact; no parameter in means no empty parameter out; the exact URL `{{portal_url}}` renders bounces correctly; and a signed-in client is NOT bounced. Three of those six FAIL against the unmodified `shell.js` and pass after — the test was checked both ways, so it cannot pass vacuously. ALSO PROVEN IN A REAL BROWSER, both paths, against the real `public/` tree with only `/api/auth/session` stubbed: signed out, `/app/client-portal.html?email=marcus%40example.com` ends on `/portal-login.html?email=marcus%40example.com` with the address box reading `marcus@example.com`; signed in, the same URL stays on `/app/client-portal.html` and paints the portal with its Account & history drawer and Documents tab present. MEASURED on macOS with DATABASE_URL unset, worktree `.claude/worktrees/agent-a8c643a2596a7306f`: 7148 tests, 7138 pass, 7 fail, 3 skipped — the same seven failures by name as the branch baseline, all pre-existing, none touching messaging, templates or the shell. The unit phase exits 1 after 499 files so the pg phase never runs and 0 `.pg.test.mjs` files ran. Lint clean, 1595 files. NOT REGENERATED, AND WHY: `npm run journeys` was run and produced no change — the extractor reads the routing table and the gate on each handler, and this adds neither. | Owner: "just fucking send it to the portal" — and the bounce had to carry the email or the new link was worse than the old one | (this commit)
2026-08-29 | (no journey changed by this work — recorded deliberately) | **COMPLIANCE REVIEW REQUIRED** — credit-repair messaging, customer-facing copy. THE "READY LETTERS" EMAIL NOW CARRIES A CLICKABLE PORTAL LINK. Owner answered the open question on PR #296: yes, put the link in. The two sentences he wrote are UNCHANGED — not one word moved — and `{{portal_login_url}}` sits on its own line directly under them, the same shape the `{{custom_values.booking_link}}` line further down the same email already uses. WHICH TAG, AND WHY IT MATTERED. Three merge tags resolve to the identical URL, all filled by `clientContext()` in `src/workflows/messaging.mjs`: `{{portal_login_url}}`, `{{CLIENT_PORTAL_URL}}` and `{{custom_values.portal_link}}`. The previous agent proposed the third. It would have worked at send time and BROKEN THE TEMPLATE EDITOR: `src/messaging/merge-tags-registry.mjs` classifies `custom_values.*` as UNKNOWN, and `classifyChange()` refuses a save that INTRODUCES an unknown tag, so no staff member could ever have saved an edit to this template again without deleting the link first. `CLIENT_PORTAL_URL` is warn-only and the editor tells a person it "will send as blank". Only `portal_login_url` is classified `resolvable` and listed in `AVAILABLE_TAGS`, and it is what `db/seed/009_u02_funding_delivery_template.sql` and `db/migrations/253_repair_email_templates.sql` already use. Verified by running `classifyChange` against the real before/after bodies: `ok:true`, nothing blocking. NO ANCHOR TAG, DELIBERATELY: this body is plain text, and `src/messaging/providers/resend.mjs` only sends `html` when the body matches `<!DOCTYPE html|<html|<table`, so an `<a href>` here would have reached the client as visible angle brackets. A bare URL on its own line is what mail clients auto-link. WHERE IT ACTUALLY LANDS A CLIENT, STATED PLAINLY BECAUSE IT IS NOT ONE CLICK: `/portal-login.html?email=<their address>`, the portal sign-in page with their email pre-filled. They press "Email me a sign-in link", get a SECOND email, click that, and `api/auth/magic-link-verify.mjs` sends them to `PORTAL_PATH` = `/app/client-portal.html`. Their letters are inside the CLOSED "Account & history" drawer on that page, third tab, and `public/app/client-portal.html` supports no `?tab=` or `#docs` deep link — the tab switch is click-only and "Payments" is the tab that opens. So it is: click → request link → second email → click → portal home → open drawer → Documents tab. That is the furthest any existing mechanism reaches with no new code, and it is a real improvement on naming no address at all, but it is not a link to the letters. NO SMS VERSION EXISTS AND NONE WAS CREATED. There is no `SMS-DS02-*` key anywhere in the repository; `src/workflows/ds-02-diy-letters.mjs:179` and `src/sales/closer-deck.mjs:744` both send this beat on `channel: "email"` only. Inventing an SMS twin nothing sends would be dead copy. THE SEED FILE WAS EDITED IN PLACE, AND THE §12 TRAP WAS CHECKED FIRST: `db/seed/023_ds02_letters_portal_copy.sql` is new on this unmerged branch and absent from `origin/main`, migrations run on the production deploy only, so it has never been applied anywhere and there is nothing to supersede. `db/expected-migrations.mjs` lists filenames and carries no content hash, so it did not need regenerating. `fundhub-docs/sources/EMAIL-TEMPLATES-SOURCE-OF-TRUTH.md` carries the identical line, because `src/messaging/seed/seed.mjs` upserts bodies straight from that document and the next hand-run of it would otherwise strip the link back out. Both bodies confirmed byte-identical through the real parser. NOT REGENERATED, AND WHY: `npm run journeys` produced no change — the extractor reads the routing table and the gate on each handler, and this adds no route and no gate. | Chris: yes, put the clickable portal link in the email | (this commit)
2026-08-29 | (no journey changed by this work — recorded deliberately) | **COMPLIANCE REVIEW REQUIRED** — credit-repair messaging. THE CORRECTION LETTERS ARE NOW SAVED, AND THE EMAIL STOPS SAYING THEY ARE ATTACHED. THE HOLE: two paths generated a client's repair letter pack, mapped every PDF down to a filename and a byte COUNT, and threw the bytes away — `src/workflows/ds-02-diy-letters.mjs` (the fallback taken when the Metro 2 engine finds no violations but the vendor letter generator does) and `src/sales/closer-deck.mjs generateDeckLetters` (the closer's "generate letters" button). Both then emailed the client `EMAIL-DS02-DIY-LETTERS-READY`, whose seeded body opened "your correction letters are attached and ready to send" and went on to tell them how to print and sign letters they had never received. Nothing was ever attached and nothing could be: `sendTemplated` has no attachments parameter, `src/messaging/compose.mjs` states outright that attachments are asset keys and never raw bytes, and `src/messaging/assets.mjs` is a frozen map with one entry pointing at a file inside the repo. NOTHING NEW WAS BUILT TO STORE THEM. Both call sites now use `persistDiyPackageFiles` (`src/metro2/diy/persist.mjs`) — the same helper the Metro 2 engine path has always used — writing through `storeAndRegister` into the existing documents registry as `kind='deliverable'`, stamped with the caller's own `org_id` and `client_id`, one document per file so a regeneration versions that letter instead of collapsing the pack. No new kind, no new table, no new storage path, no new route, no new screen. THE HELPER LEARNED THE SECOND PACK'S SHAPE, and that is the only change to it: the repair letter pack's files are `{filename, contentType, content, type}` where the engine pack's are `{path, pdf, text}`, so (1) a file's own `type` now names it — "State-Attorney-General-Complaint.pdf" contains no "state-ag-complaint" and was being filed as a Metro 2 dispute letter — and (2) a `.txt` cover sheet arriving as a Buffer in `content` is no longer eligible to be stored as `application/pdf`. An untyped file still falls through to the filename classifier, so the engine pack behaves exactly as before. THE COPY, OWNER-SET 2026-08-29: no attachments and no download links in the email at all; clients sign in to the portal. Two sentences changed in `EMAIL-DS02-DIY-LETTERS-READY` — the opening became Chris's exact wording, "Your correction letters are ready. Log into your client portal to view and download them.", and "Print and sign each one." became "Download each one from your portal, then print and sign it." Everything else in the body is untouched and still true. `db/seed/015_live_template_backfill.sql` is already applied on live and editing an applied file is a silent no-op, so the new copy ships as `db/seed/023_ds02_letters_portal_copy.sql`, which supersedes it and deliberately leaves `compliance_passed` alone on an existing row. `fundhub-docs/sources/EMAIL-TEMPLATES-SOURCE-OF-TRUTH.md` carries the same two sentences, because `src/messaging/seed/seed.mjs` upserts bodies straight from that document and would otherwise revert the copy on its next run. MERGE ORDER, STATED PLAINLY: this copy should land with or after the portal-summary signing change being built separately. `public/app/client-portal.html paintDocs()` already renders a download link when a row carries `download.url` and `src/documents/retrieve.mjs` already produces that shape; `api/read/portal-summary.mjs` simply does not sign its rows yet, so until it does a client following this email sees their letters listed as On file and cannot open them. PROVEN by 2 new tests in `src/sales/closer-deck.test.mjs` that run the real `generateDeckLetters` against the real repair pack built from the sandbox credit pull — the dispute letter, the CFPB complaint and the state AG complaint all reach the registry, every row is stamped with the right org and client, the text cover sheet is not stored, the bytes read back out of the store starting `%PDF`, and a storage failure is reported rather than swallowed or thrown — plus 3 in `src/metro2/diy/deliver.test.mjs` pinning the classification and the text-file refusal. NO TEST WAS WEAKENED, SKIPPED OR DELETED. NOT REGENERATED, AND WHY: `npm run journeys` was run and produced no change — the extractor reads the routing table and the gate on each handler, and this work adds no route and no gate. | The client was told their letters were attached, nothing was attached, and the letters themselves were being thrown away | (this commit)
2026-08-29 | client (+ all six role journeys, regenerated) | SAVED FILES CAN BE OPENED AGAIN, ON BOTH SIDES. A file used to get ONE working link, minted in the reply to its own upload and dead 15 minutes later (`DEFAULT_TTL_SECONDS = 900`), so an uploaded photo ID or a generated letter saved correctly and could never be opened by anybody again. MOST OF THE PATH WAS ALREADY BUILT AND PAID FOR — recorded because it changes what this cost: the client portal's `paintDocs()` has always rendered `d.download.url` as a link and falls back to plain text without one; `shapeDocument()` in `src/documents/retrieve.mjs` has always attached exactly that shape; and `read/portal-summary` was already routed. CLIENT SIDE — ONE FILE, NO FRONT-END CHANGE AT ALL: `api/read/portal-summary.mjs` wrote its own SELECT over `documents` and signed nothing, so `download` was undefined on every row and the link the portal was built to draw never appeared. It now calls `listClientLibrary()` — one of the four readers in `retrieve.mjs` that had ZERO callers anywhere in `src/`, `api/` or `scripts/` — with signing on. `public/app/client-portal.html` is byte-for-byte unchanged. THE ROWS ARE NARROWED BACK DOWN AND THAT IS THE SECURITY-RELEVANT HALF, not tidying: `listClientLibrary` selects the registry's full public column set, which carries `metadata` (for an upload that holds the original filename and an `uploaded_by` object naming the STAFF MEMBER's id), plus `checksum`, `generated_by`, `signature_ref` and `org_id`. None of that was in this response before and none of it belongs to a consumer, so an explicit allow-list projects each row back to exactly the fields it returned yesterday plus `download`. Sort order (`created_at DESC`) and `LIMIT 50` are preserved, so the screen changes in exactly one way: the titles are now links. An EXPIRED document gets no link, because the download route refuses one anyway and a control that cannot finish is worse than none. STAFF SIDE — the Documents desk (`public/app/documents.html`) could NOT download: it reads `/api/read/documents`, which signs nothing. New route `/api/documents-download?id=` mints one fresh link per CLICK, and a Download button was added inside the existing Document cell — no new page, screen, tab or menu row. Click-time rather than list-time DELIBERATELY: that screen asks for 200 rows, and signing the list would mint up to 200 live bearer credentials to credit reports and photo IDs on every page load whether or not anyone clicked. `api/read/documents.mjs` and its `redact()` are untouched. ROUTED IN THE SAME COMMIT as the handler — `netlify/functions/api.mjs` holds a hardcoded ROUTES map and a handler missing from it 404s locally and deployed (CLAUDE.md §12) — and registered in `src/pulse/registry.mjs`. The key is `documents-download`, NOT `documents/download`, because `src/http/routes.test.mjs` refuses any exact key sitting under the `documents/` prefix branch. GATES: `requirePrincipal(['staff','client'])` first, then a SEPARATE `requireRole(res, staff, ROLE_SETS.STAFF)` — the set that already lists these rows — because `requireAuth` silently drops a `roles` key. The session's org is passed to the reader, never a query parameter; a client principal must additionally OWN the document, because every client of a company shares its org and the org clause alone would let any client open any other client's credit report. Unknown id, wrong company and wrong client are ONE identical 404, so neither endpoint is an inventory oracle. BOTH LINKS ARE SAME-ORIGIN PATHS, not absolute URLs: deriving an origin from request headers means guessing a protocol, and with no `x-forwarded-proto` the helper must assume https — right behind Netlify, wrong against a plain-http dev server, where the link then will not load at all. A browser already on the origin needs none; absolute URLs are for links that LEAVE the page. TTL is 15 minutes on both and the query string cannot ask for longer; deliberately NOT the 7-day maximum, which is for a link inside an email. Fails closed with no `DOCUMENT_URL_SECRET` — and on the portal it fails closed on the LINK, not the PAGE, so a missing secret no longer takes the client's scores, pre-qual and upload doors down with it. `storage_key` is never returned. Nothing is logged — no filename, no key, no URL. NO SECOND SIGNING IMPLEMENTATION: both paths reach the one `signDocumentUrl()`; `baseUrlFrom` was lifted out of `api/documents-upload.mjs` into shared `baseUrlFromRequest` rather than copied. PROVEN by 27 new tests in `src/http/documents-download.pg.test.mjs` against real Postgres, 27/27 pass, SEVEN of them isolation: staff at company B refused company A's document; a client refused a SIBLING client's document inside their own company; a client refused another company's document; the cross-company refusal byte-identical to an unknown id; the reverse direction; a client's portal summary never listing a sibling's document; and cross-company portal preview refused. Also pinned: an expired link is dead and a fresh one works, and BOTH answers are narrowed by an explicit allow-list and the suite fails if anybody stops narrowing either one. NOT DONE, recorded as a finding rather than a gap that was filled: `getByClientAndKind`, `getHistory` and `getVersion` in `retrieve.mjs` still have zero callers — no shipped screen asks for a document by kind or for its version history, and inventing a caller to retire a warning would be building a surface nobody asked for. | Uploaded IDs and generated documents saved correctly and then could never be opened again | (this commit)
2026-08-29 | role-funding-advisor (no diagram changed — the classifier is a value on an existing step, not a new step) | A REJECTION EMAIL FROM A BANK WAS BEING READ AS AN APPROVAL. `src/adapters/mailgun.mjs` matched its keywords with a plain substring test (`lower.includes(kw)`), and the string "not approved" contains the string "approved". APPROVED is the first rule in the list and the first match won, so "Unfortunately, you were not approved for the requested credit limit" classified as APPROVED. That label is not decoration: `src/workflows/f-11-bank-email-event-router.mjs` reads it, and on APPROVED it moves the client's funding card into the `approved` stage of `funding_card_stacking`, sets `employee_next_action` to "Prepare Next Funding Round", and files the task "APPROVAL: log amount + update round". So a client who had just been turned down was recorded as approved, staff were told to prepare the next round on money that did not exist, and the denial follow-up (F-09) never ran because the event never said DENIED. Three more phrasings hit the same fault — "your request was not approved", "we have not approved your application", and any denial whose "not approved" fell across a line break — and the same substring test read "funded" out of "refunded" and "approved" out of "unapproved", so a fee refund also classified as APPROVED. FIXED THREE WAYS, all in the classifier, nothing downstream touched. (1) Keywords now match on WORD BOUNDARIES, and a space inside a keyword matches any run of whitespace so a hard-wrapped denial still matches. (2) NEGATION IS READ BEFORE THE KEYWORD CONTEST: "not approved", "unable to approve", "cannot approve", "will not be approved", "could not fund" and the like drop the APPROVED rule outright and record DENIED — because the leftover APPROVED keywords in a denial letter ("credit limit", "approval") are the letter describing what it is refusing. The gap between the negative and the approval word is a closed list of function words, not a wildcard, so "this does not affect your approved limit" and a "do not reply" footer stay APPROVED. (3) A shorter phrase sitting inside a longer phrase from another rule is discarded as the same words read twice. RULE ORDER WAS DELIBERATELY NOT CHANGED — putting DENIED first only swaps which case breaks, and outcome-beats-process is what the order encodes: an approval that also says "verify your identity" or "please upload your ID" is still an approval, and those are pinned. OWNER-VISIBLE JUDGEMENT CALL: "We approved you for a reduced amount of $2,000" is now COUNTEROFFER, not APPROVED. Both move the card to the same stage, so the stage is not what is at stake — the task is. APPROVED files "log amount + update round", which reads as done; COUNTEROFFER files "review + log offer + next step", which puts a person on the shortfall. A cut-down approval needs the second one. A reduced RATE ("your APR has been reduced") is still APPROVED — it has to be a reduced amount. `classifyBankEmail` and `classifyFull` held two byte-identical copies of this loop; they are now one function with two entry points, so the fix cannot be applied to one and missed on the other. MEASURED: 9 of the assertions in the new tests FAIL on main and PASS on this branch, proven by running both classifiers side by side. 24 new tests, every phrasing one banks actually send. Unit suite 7161 tests / 7151 pass / 7 fail / 3 skipped on this branch vs 7137 / 7127 / 7 fail / 3 skipped on clean main at 4f551ff1 — THE SAME 7 FAILURES BY NAME, none new. The 131 database test files did not run in either measurement: `scripts/run-suite.mjs` exits after the unit phase when it is red, and it is red on main. Run on their own with DATABASE_URL unset they are 675 tests / 57 pass / 0 fail / 618 skipped. macOS, worktree agent-aff7ff77095f78d1a, no database connected. Lint clean. SEPARATE FINDING, NOT FIXED HERE: a stray `<<<<<<< HEAD` merge-conflict marker is committed on main at line 3 of this file, with no matching `=======` or `>>>>>>>`. | A client who was declined was recorded as approved, and nobody was told they had been declined | (this commit)
2026-08-29 | role-funding-advisor | Owner correction to the line below it, same day. THE DOLLAR AMOUNT ON A BANK APPROVAL IS NOW OPTIONAL, BECAUSE APPROVAL AND AMOUNT ARE TWO SEPARATE MOMENTS. "Bank yes" on the client control panel used to refuse to save with an empty "Approved $" box. That was wrong: when a bank comes back, the fulfillment team (the funding advisor who submits the applications — a closer never touches this) very often does not know the limit yet, and has to ask the client or wait for the bank's approval email to arrive through the mail routing that surfaces keyword matches in that client's inbox. "Approved, amount unknown" is a real, valid state and now saves cleanly. AN EMPTY BOX SENDS NO AMOUNT AT ALL, so `applications.approved_amount` keeps its NULL — it is never turned into a 0, because a zero is a claim that the bank approved nothing and would flow into a success-fee invoice looking legitimate. A box with something WRONG in it is still refused, because a typo is not an unknown. THE AMOUNT IS ADDED LATER IN THE SAME BOX: the client control panel now reads the saved applications back (an added `applications` key on the existing `GET /api/applications?client_id=` — no new route), paints the saved dollars into the box they were typed into, and pressing "Bank yes" again patches the SAME application row rather than minting a second approval. A MISSING AMOUNT IS NOW VISIBLE IN TWO PLACES THE FUNDING ADVISOR ALREADY LOOKS, so it cannot rot unbilled: an amber "Amount needed" chip on the lender's own row plus a one-line count at the top of the Funding block ("N bank approvals are still waiting on their dollar amount"), counted across the whole file and not just the lenders showing; and the same fact across every client as an "Amount needed" chip on the Pipeline board card, from a new read-only `approval_amount_missing` flag on `/api/dashboard/pipeline` that is true only when an application is `Approved` with a NULL amount — a recorded 0 is a fact somebody entered and is not flagged. NO NEW PAGE, SCREEN, TAB OR MENU ROW, and no new route. THE FUNDED GUARD IS UNTOUCHED AND STILL STRICT: `guardFundedAmount` in `src/workflows/cards.mjs`/`src/funding/card-stacking-rounds.mjs` still refuses to park a card on Funded without a dollar amount, and a round whose only approval has no amount still cannot be marked funded. Two moments, two rules — the approval may be incomplete, the number a client is billed from may not. VERIFIED that a NULL approved amount is already tolerated downstream: `src/funding/closeout.mjs` filters `COALESCE(approved_amount, 0) > 0` on the lender breakdown, so an unpriced approval is left OUT of the invoice's line items rather than billed as zero, and the fee itself is computed from `funding_rounds.funded_amount` either way. That omission from the invoice is exactly why the missing amount had to be made visible. 4 new database tests (`src/applications/approved-amount-optional.pg.test.mjs`) prove the amount stays NULL and never 0, that it can be filled in later onto the same row, that Funded is still refused without it, and that the closeout skips the unpriced approval; 1 new database test on the board endpoint; 6 new screen tests; 8 new browser tests. Measured on a scratch Postgres created for the run: unit 7183 tests / 7175 pass / 7 fail / 1 skipped and database 1882 / 1853 / 28 / 1, against a baseline at this branch's HEAD of 7177 / 7169 / 7 / 1 and 1877 / 1847 / 29 / 1 — +11 tests, all passing, zero new failures, and `src/funding/card-stacking-rounds.pg.test.mjs` still green. The generated `role-funding-advisor-actual.md` is UNCHANGED and that is correct — `npm run journeys` was run and produced no diff, because that file is a map of which routes the role can reach and this adds no route and no permission gate. | Owner, 2026-08-29: "We should be able to say yes, it's approved, and then also enter in the dollar amount. So yes, technically it's a two-step thing, because sometimes we know it's approved, we don't know what the dollar amount is." | (this commit)
2026-08-29 | role-funding-advisor | Staff can now type a dollar amount in the two places a funding decision is recorded: an "Approved $" box beside "Bank yes" on the client control panel, and a box that asks what actually funded when a card is dragged onto the Pipeline board's Funded column. Both send dollars (fixed 2dp) on the existing status update — `approved_amount` to `/api/applications`, `funded_amount` to `/api/pipeline-cards`. A BLANK BOX REFUSES AND SENDS NOTHING; it is never turned into a zero, because a zero would claim the bank approved nothing and would flow into a success-fee invoice looking legitimate. Negatives, words and more than two decimal places are refused on the screen and again on the server. The generated `role-funding-advisor-actual.md` is UNCHANGED and that is correct: this adds no route and no permission gate, and that file is a map of which routes the role can reach. The behaviour change is inside two screens that already called two already-reachable routes. | The move onto Funded was guarded (`src/workflows/cards.mjs guardFundedAmount`) because `funding_rounds.funded_amount` is the basis a client is billed from, but nothing in the product could record an amount — so every Funded move was refused, no round was ever marked funded, the round-funded event never fired, and no success-fee invoice was ever raised. | (this commit)
2026-08-28 | optimize | **COMPLIANCE REVIEW REQUIRED** — ConsumerDirect's SmartCredit(R) sign-up box is now built into `/optimize`, and their twelve-item compliance wording is on the page around it. The box mounts with the container id, all three required attributes (`data-clientkey`, `data-memberurl`, `data-productname`), their `type=module` file, the hidden `PID` input for credit, a `data-theme` of `sc` (their own look), and a prefill block carrying the first name, last name and email the person already typed, so nobody types it twice. Their `cd-marketing-message-widget` (company name `Fundhub Credit Solutions LLC`), `cd-memberplans-widget` and `cd-progress-widget` mount with it. `cd-cobrand-widget` and the Spanish switcher are deliberately left out. The page now listens for `cd-signup-next-step`: `ConfirmationStep` is the only success and shows a real success state; all THREE failure signals (`SignupError`, `SignupErrorOutsideUS`, `SignupErrorBlackbox`) are told apart and each shows a real, plain-English next step — the fraud-screen one deliberately offers no restart. `BillingStep` is no longer mistakable for a sale. Compliance: SmartCredit(R) is now named and described ONLY as a credit monitoring product, in its own bordered card separate from Fundhub's; the FCRA rights ticker, the 'mark what is being reported wrong' lede and the old dispute-flavoured step 02 were DELETED because the checklist bans wording that suggests credit repair; pricing now says $29.99 a month and $19.99 a month, recurring, no trial, charged until cancelled; enrollment start, independent cancellation and the trademark attribution are stated; an agreement checkbox sits by the control and now GENUINELY refuses the click when unticked (it was decorative before). Two real bugs fixed on the way: `sms_consent` was written down as `true` no matter what was ticked, and `hidden` did not hide, so the off-site tracking link stayed on screen underneath the mounted box. NOT INVENTED and still missing: SmartCredit's four policy addresses and its cancellation route — those are read from env BY NAME (`CONSUMER_DIRECT_SERVICE_AGREEMENT_URL`, `_PRIVACY_POLICY_URL`, `_TERMS_OF_USE_URL`, `_CONSUMER_RIGHTS_URL`, `CONSUMER_DIRECT_CANCEL_URL`), https-only, and the page prints the document name as plain text until they are set. The no-key path is unchanged: production holds no key on purpose, so the tracking link still works and ConsumerDirect's file is never fetched. 20 new guards added, none weakened or removed; lint clean. | ConsumerDirect will not issue a production key until they pass this page at compliance review | (this commit)
2026-08-28 | (no journey changed by this work — recorded deliberately) | **COMPLIANCE REVIEW REQUIRED** — dispute logic. THE ROUND 5 STATE ATTORNEY GENERAL COMPLAINT CAN NOW ACTUALLY BE MAILED — FOR 38 STATES. PR #282 built the whole Round 5 send path and then correctly refused to use it, because this repository held no state attorney general mailing address: `agPostalAddress()` returned null for every state, `complaintDestination` refused with `ag_postal_address_unknown`, no filing row was written, and Round 6 stayed silent. WHAT CHANGED IS THE DATA, NOT THE PATH. New `AG_MAIL_BY_STATE` in `src/metro2/letters/ag-statutes.mjs` carries a confirmed consumer-complaint mailing address for 38 states in EXACTLY the shape `CFPB_MAIL_ADDRESS` already used — same file, same object shape, no new config format, no new table, no new route, no migration. `agPostalAddress` now reads that table and returns a copy; every other line of the send path is untouched. EACH ADDRESS WAS READ OFF THE STATE'S OWN SITE OR ITS OWN PRINTABLE COMPLAINT FORM AND RE-CHECKED BY A SECOND READER, and the source URL sits in the comment directly above the entry so the next person can re-verify one state without redoing the batch. Nothing came from a third-party directory and nothing was inferred from a pattern. TWELVE STATES ARE STILL NOT MAILABLE, AND THAT IS THE FINDING, NOT A GAP: AK, CO, CT, KS, ME, MS, NV, NY, OK, OR, RI, SC. For them Round 5 refuses exactly as it did yesterday. FOUR OF THOSE TWELVE — NV, NY, OK, OR — DO PUBLISH AN OFFICE ADDRESS AND IT IS DELIBERATELY LEFT OUT: each of those states publishes no postal route for a consumer complaint at all, and the only address on the site is the general office. The sourcing note for New York says in terms not to present it to a consumer as a confirmed filing address. Mailing a document the client signed UNDER PENALTY OF PERJURY to a general office address is the exact failure this refusal exists to prevent, so a blank was kept. `AG_MAIL_UNRESOLVED` records all twelve with the reason and the URL that was checked. ONE CORRECTNESS FIX CAME WITH IT: the office named INSIDE the complaint is now the office on the envelope. In Hawaii and Wisconsin the attorney general does not take consumer complaints — Hawaii's DCCA Office of Consumer Protection and Wisconsin's DATCP Bureau of Consumer Protection do — so the letter used to name an office that would never have received it. PROVEN BOTH DIRECTIONS by 23 new tests in the new `src/metro2/letters/ag-statutes.test.mjs` and 2 rewritten ones in `src/metro2/rounds/complaint-filing.test.mjs`: every address present has a street with a number in it, a city, a two-letter state that matches the key it is filed under, a ZIP that looks like a ZIP, and a source comment — a half-filled address fails the suite; no two states share an address; the table is frozen and the caller gets a copy; and every one of the twelve unresolved states, plus DC, PR, GU and junk codes, still returns null and still refuses with `ag_postal_address_unknown` and no destination. The four general office addresses that were deliberately not recorded are pinned as absent by string. NO TEST WAS WEAKENED, SKIPPED OR DELETED. Two assertions changed because the behaviour they pinned was the thing this work was asked to change, and each was replaced with a stronger set: `complaint-filing.test.mjs`'s "NO STATE ATTORNEY GENERAL CAN BE MAILED" became one test that every unconfirmed state is still refused with nothing substituted and a second that every confirmed state mails to its own address and no other; `letter-pack-escalation.test.mjs`'s single `/Attorney General of Texas/` match became four Texas-specific pins. MEASURED: 7113 tests, 7103 pass, 7 fail, 3 skipped, on macOS with DATABASE_URL unset, worktree /tmp/ag-addr on branch feat/state-ag-addresses off c9fef24b. The same 7 failures, by name, fail on the branch this sits on. Lint clean. NOT REGENERATED, AND WHY: `npm run journeys` was run and produced no change — the extractor reads only `netlify/functions/api.mjs`, `src/http/read-api.mjs`, `src/http/partner-read-api.mjs` and `src/http/dashboard-auth.mjs`, and this change touches none of them and adds no route and no gate. | The Round 5 complaint had nowhere to be posted, and a wrong address is worse than no address | (this commit)
2026-08-28 | (no journey changed by this work — recorded deliberately) | **COMPLIANCE REVIEW REQUIRED** — dispute logic. A MACHINE MAY NO LONGER CARRY A CLIENT INTO ROUND 4. THE HOLE: a client can upload the bureau's reply from the portal, an AI reads it, and when the reading is confident `src/repair/parse-loop.mjs runParseAdvanceLoop` advances the round WITH NOBODY IN THE LOOP — `src/repair/response-agent.test.mjs` "C1 clear letter auto-advances" pins that and it is INTENDED BEHAVIOUR, not a bug, and it was not changed. But that same confident read on a Round 3 item would cross into R4, and R4 is where this branch's own gate releases the CFPB and state attorney general complaints into the client's pack — two documents the consumer signs UNDER PENALTY OF PERJURY. Nothing auto-mails them (`api/repair/send.mjs` requires a staff login) so no paper left the building on its own, but the documents would have been built and handed over on a machine's say-so. OWNER-SET 2026-08-28: auto-advance STAYS for R1→R2 and R2→R3 — those produce another bureau letter, a wrong move costs a letter and is recoverable, and the speed is worth it — and crossing INTO the escalation rounds requires a human confirmation, because a wrong move there produces sworn complaints, which is not the same class of mistake. ONE CHOKE POINT, NOT THREE. `src/metro2/rounds/state.mjs applyItemOutcome` is the only thing in the repository that moves an item up a round; the rule lives there and nowhere else. It gained one option, `humanConfirmed`, which `advanceAfterParse` passes straight through and `confirmParse` computes. On a "verified" answer whose next round is an escalation round, without that flag, the item KEEPS ITS OWN ROUND with status `verified` and `awaiting_human_confirmation: true`. It is not lost: `itemsNeedingEscalation` still reports it, and the parse that produced it is held unconfirmed so a person can act on it. WHAT COUNTS AS A HUMAN: only a real staff id — the same confirmation `src/metro2/inbound/confirm.mjs` already represents. New `isRealStaffId` there is now the single definition, and `src/repair/parse-loop.mjs staffIdOrNull` was a second copy of the same regex and now calls it. The auto path passes `null` or the sentinel `"system_high_confidence"`, and NEITHER BUYS AN ESCALATION — pinned, including through the staff confirm path. `humanConfirmed` must be the literal `true`; `"true"`, `1`, `{}` and every other truthy value are refused. It defaults false, so a caller that says nothing gets the safe answer. THE HELD PARSE GOES TO THE EXISTING QUEUE, AND A REAL BUG WAS FOUND GETTING IT THERE. `confirmParse` now returns `{ok:false, reason:"escalation_needs_human", hold:true}` in exactly the shape the low-confidence hold already used, so `runParseAdvanceLoop`'s existing hold path stores it unconfirmed with no new branch. But `api/repair/exceptions.mjs` — the Inquiry Remover's exceptions screen — listed only parses with `confidence < 0.85`, and an escalation hold is a CONFIDENT read, so it would have sat in the table where NOBODY COULD EVER SEE IT. The parse is now stamped `heldForEscalation` in the existing `parse_json` column (no migration, no new table, no new queue) and that one query was widened to show both kinds. NO NEW QUEUE, NO NEW SCREEN, NO NEW ROUTE. PROVEN BOTH WAYS by 10 new tests in `src/metro2/rounds/rounds.test.mjs`, 5 in `src/repair/response-agent.test.mjs` and 3 in `src/underwrite/prior-outcome.test.mjs`: a confident machine read of an R3 verify is HELD, writes no item row, and does NOT release the complaints; the same answer confirmed by a staff id reaches R4 and does release them; a held item is still reported as needing escalation so nothing is dropped; the round cap still wins over the human gate; and — the regression most likely to break by accident — R1→R2 and R2→R3 still advance with nobody in the loop, pinned at both the state-machine level and end to end through `runParseAdvanceLoop`. | Owner: auto-advance is fine up the bureau ladder, but a machine must not decide that somebody swears a federal complaint | (this commit)
2026-08-28 | (no journey changed by this work — recorded deliberately) | **COMPLIANCE REVIEW REQUIRED** — dispute logic. ROUND 6 MAY NOW NAME THE COMPLAINTS — BUT ONLY FROM A RECORDED MAILING. Owner correction, same day: "WE MAIL THEM." Fundhub mails the Round 4 CFPB complaint and the Round 5 state attorney general complaint on the client's behalf, so unlike the DIY package the filing IS knowable, and the earlier conclusion in this changelog — that R6 must stay silent because nothing records a filing — was based on the wrong premise. THE RULE DID NOT CHANGE, THE SOURCE OF TRUTH DID: **a claim must be backed by a row, not by an expectation.** NOTHING NEW WAS BUILT TO SEND OR TO STORE. `dispute_letters` (`db/migrations/160_metro2_dispute_engine.sql`) already carries `round` as free text so R4 and R5 already fit, already flips to `status='sent'` when the mail provider accepts a letter (`src/repair/send.mjs`), and `src/metro2/rounds/store.mjs saveLetter` already takes a `target`. The send path is the existing human gate `sendRepairLetters` → `mailBureauLetter` → the PostGrid provider; no second delivery path and no new route were created, and `src/repair/` still contains no outbound `fetch`. ONE MIGRATION, WIDENING ONLY: `db/migrations/270_dispute_letter_complaint_targets.sql` widens `dispute_letters_target_check` from `('bureau','furnisher')` to `('bureau','furnisher','cfpb','state_ag')`, because a complaint is neither a bureau nor a furnisher and recording one as `'bureau'` would be a FALSE ROW — worse than no row, since Round 6 reads these rows to decide what it may say. 252 is already applied and editing an applied migration is a silent no-op, so this supersedes it; every existing row stays valid. NEW `src/metro2/rounds/complaint-filing.mjs` holds the two pieces that genuinely did not exist: WHERE a complaint is mailed, and HOW the record is read back. `loadComplaintFilings` is read-only and filters in SQL on `target IN ('cfpb','state_ag')` AND `status IN ('sent','delivered')` AND the round matching the target, so a row that comes back is by construction a complaint a mail provider accepted. A GENERATED COMPLAINT IS NOT A FILED ONE — `'generated'`, `'ready'`, `'variance_failed'` and `'failed'` all count as not mailed. A corrupt row whose round and target disagree is discarded, not trusted. A database failure returns ZERO filings and is reported, never thrown, so a hiccup makes Round 6 say LESS, never more. `src/metro2/letters/generate.mjs buildLetterText` gained one optional `priorFilings` input, rendered through the same evidence-block mechanism the existing PRIOR BUREAU RESPONSE facts already use — no new letter shape — as `COMPLAINTS ALREADY FILED (evidence):` with one plain sentence per recorded mailing. It is gated to R6 alone, and to a non-empty record: with no record the R6 letter renders EXACTLY as it did before this path existed. `src/repair/analyze.mjs` loads the record only when it is building an R6 letter. NO STATUTE WAS INVENTED; `roundInstructions` is untouched. **THE STATE ATTORNEY GENERAL STILL CANNOT BE MAILED, AND THAT IS A FINDING, NOT A GAP THAT WAS FILLED.** The CFPB has a published mailing address and it was already in this repository (`CFPB_FILING.mail`); the structured form added next to it is asserted by test to be the same address, not a second one. The fifty state attorneys general have NO street address here: `AG_BY_STATE` carries an office name and a web portal for five states (TX, CA, FL, NY, IL) and a generated placeholder for the other forty-five, and never a street. **Fifty addresses were not invented** — a wrong address means a complaint the client signed under penalty of perjury is mailed into a void while everyone believes it was filed. So `agPostalAddress()` returns null for every state, `complaintDestination` refuses that send with `ag_postal_address_unknown`, `sendRepairLetters` stops before mailing so NO filing row is written, and Round 6 stays silent about a state AG filing exactly as it does today. Fill `agPostalAddress` in per state from each office's own published mailing address and the rest of the path works unchanged. PROVEN BOTH DIRECTIONS by 23 tests in `src/metro2/rounds/complaint-filing.test.mjs`: with a recorded CFPB and state AG mailing R6 names both; with no record R6 says nothing and the string `COMPLAINTS ALREADY FILED` never appears; with only the CFPB on record — today's real case — R6 names the CFPB and never the attorney general; a written-but-unmailed complaint earns no sentence; and rounds 1 through 5 stay silent even with a full record. The second direction is the one that matters. | Owner: "WE MAIL THEM" — the filing is knowable, so R6 may stand on it, but only from the record | (this commit)
2026-08-28 | (no journey changed by this work — recorded deliberately) | **COMPLIANCE REVIEW REQUIRED** — dispute logic. THE ESCALATION LADDER NOW ESCALATES. Two faults, both above Round 3. FAULT 1 — ROUNDS 4, 5 AND 6 RECYCLED THE BUREAU LETTERS FOREVER. `promptPoolRound` in `src/metro2/letters/prompts.mjs` mapped R4→R2, R5→R3, R6→R2, and `src/repair/round-plan.mjs` was reading that function AS the round ladder. So a client who finished Round 3 was handed the Round 2 method-of-verification letter again, then Round 3 again, then Round 2 again, with no end — while `CFPB_COMPLAINT` and `STATE_AG_COMPLAINT` sat in `LETTER_TYPES` the whole time and were never reachable through the ladder. The owner set this ladder on 2026-08-15 ("full path after Round 3 is CFPB + state AG", recorded on line 2 of `src/metro2/letters/catalog.mjs`) and restated it 2026-08-28 as "1, 2, 3, escalation, escalation, escalation final notice" and "We just use more aggressive law as we go!" — the ladder climbs by AUTHORITY, not by rewording. FIXED by separating two questions that were tangled into one function. WHAT DOCUMENT IS ROUND N is now `ROUND_LADDER` in `catalog.mjs`: R1 Metro 2 dispute, R2 method of verification, R3 final notice, **R4 CFPB complaint**, **R5 state attorney general complaint**, R6 final notice reissued. WHICH PROSE SHAPE A BUREAU LETTER USES stays `promptPoolRound`, now R4/R5/R6 → R3, because after Round 3 every bureau letter is a final notice and dropping back to the Round 2 question is a step DOWN in authority. R6 — WHAT WAS REUSED AND WHY, STATED PLAINLY: there is no eighth letter type called "escalation final notice" and NONE WAS INVENTED. R6 reuses `R3_FINAL_NOTICE`, because a final notice to the bureau is exactly what R6 is — the last bureau letter, sent when the two regulator complaints have not moved the item either. It is labelled "Final notice, reissued" so it never prints Round 3's title or Round 3's timing. **R6 DOES NOT AND MAY NOT SAY THE COMPLAINTS WERE FILED**, and that is a FINDING, not a gap that was filled: NOTHING IN THIS REPOSITORY RECORDS THAT A CLIENT EVER FILED ONE. The pack ships both complaints undated and unsigned inside `06-complaints-CONDITIONAL/` behind a cover sheet reading DO NOT FILE WITH ROUND 1; the client writes the date, hand-signs the perjury declaration and files them personally, and no table, column, endpoint or workflow here ever hears whether that happened — `dispute_items` records bureau rounds and nothing else. Writing "I have filed a CFPB complaint" into R6 would be the identical defect already fixed on main, where the Round 3 letter told the bureau a CFPB complaint was being filed and no such document had ever been produced. Pinned: no R4–R6 opening, closing, instruction or rendered letter claims a filing happened — every CFPB and attorney-general reference in them is future tense. NO NEW STATUTE WAS WRITTEN; every citation used already existed in `src/metro2/letters/`. FAULT 2 — THE SWORN COMPLAINTS RELEASED ON THE WRONG CONDITION. `src/underwrite/letter-pack.mjs` released the CFPB and state AG complaints whenever the pack had produced ANY dispute letter. A pack writes a Round 1 letter for a client on their first day, so both complaints — signed by the consumer UNDER PENALTY OF PERJURY and saying "I disputed inaccurate information with the consumer reporting agencies" — were handed to clients who had disputed nothing and heard back from nobody. That sworn sentence was false and the federal complaint was out of order. FIXED: the gate is now the round the client is ACTUALLY on, which became knowable when the recorded bureau answer was wired to the pack (`src/underwrite/prior-outcome.mjs`, PR #281). New `reachedEscalation()` requires a recorded answer at R4, R5 or R6. An item only sits at R4 because R1, R2 and R3 were EACH answered "verified" and a human confirmed EACH one — `loadPriorOutcomes` filters in SQL on `status = 'escalated' AND outcome = 'verified'`, a pair only `src/metro2/rounds/state.mjs applyItemOutcome` can set, and only behind the human gate in `src/metro2/inbound/confirm.mjs`. A CLIENT CAN NEVER REACH R4 BY DEFAULT, BY GUESS OR BY EMPTY DATA: an empty list, a missing argument, a null round, "R9", the number 4, "round 4" and a database error all refuse, and a database failure WITHHOLDS the complaints rather than releasing them. The old "dispute letters in the same pack" rule is KEPT as the pack-health check it always really was, and is reported first, so `no_dispute_letters` means the pack broke while `not_escalated` means the client is simply not there yet — the normal state for almost every client. The complaints still ship last, still inside `06-complaints-CONDITIONAL/`, still behind the same cover sheet, still built only by `src/metro2/diy/package.mjs maybeComplaintFiles` — no second copy. Proven by 26 tests in `src/underwrite/letter-pack-escalation.test.mjs`, 28 in `src/underwrite/prior-outcome.test.mjs`, 15 in `src/metro2/ws-b-engine.test.mjs` and 5 in `src/repair/round-plan.test.mjs`. Three baseline pins in `src/underwrite/output-baseline.test.mjs` were updated deliberately, in this commit, with the reason recorded on the line — a Round 1 client's pack is now pinned as its own baseline because it is the common case. NO TEST WAS WEAKENED, SKIPPED OR DELETED: the three assertions that changed (`promptPoolRound("R4")`, `promptPoolRound("R6")`, `priorOutcomeForRound`) pinned the bug itself, and each was replaced with a stronger assertion plus a new one forbidding any round from stepping back down to the Round 2 wording. NOT REGENERATED, AND WHY: the journey extractor reads only `netlify/functions/api.mjs`, `src/http/read-api.mjs`, `src/http/partner-read-api.mjs` and `src/http/dashboard-auth.mjs`; this change touches none of them, adds no route and no gate, and `docs/journeys` is stale by the same 9 files before and after it. | The client's ladder stopped at Round 3 and then repeated itself forever, and the two sworn complaints were being handed to clients on day one | (this commit)
2026-08-28 | (no journey changed by this work — recorded deliberately) | **COMPLIANCE REVIEW REQUIRED** — dispute logic. UnderwriteIQ could only ever write a Round 1 dispute letter. Rounds 2 and 3 were gated on one field on each account, `priorOutcome`, which `src/underwrite/letter-pack.mjs` read and **nothing in this repository ever wrote**. The vendor letter writer (`vendor/underwriteiq-full/api/lite/letter-generator.js`, `getAccountsForRound`) hands out the Round 2 letter only to accounts whose `priorOutcome` is exactly `"verified"` and the Round 3 letter only to `"verified_round2"`, so both matched zero accounts every time. That is why the escalation ladder's top rung was out of reach: the CFPB and state attorney general complaints ship inside `06-complaints-CONDITIONAL/` behind a cover sheet reading DO NOT FILE WITH ROUND 1 — send only after Round 3 is done, and Round 3 could not be reached. NOTHING NEW WAS BUILT TO CAPTURE A BUREAU ANSWER, because a capture path already existed and works: staff post a scanned bureau letter to `/api/repair/inbound-mail`, `src/metro2/inbound/parse-response.mjs` reads it, a human confirms it (`src/metro2/inbound/confirm.mjs`, or `/api/repair/exceptions` when confidence is low), `src/metro2/rounds/state.mjs applyItemOutcome` moves the account up a round, and `src/repair/parse-loop.mjs persistAdvancedItems` writes the answer to `dispute_items`. What was missing was one wire from that stored answer to the letter pack's gate. New file `src/underwrite/prior-outcome.mjs` reads it — read-only, never writes — and `buildLetterPackForClient` now makes a third read for it. THE GATE: an account advances only when all three of `status = 'escalated'`, `outcome = 'verified'` and `round = R2..R6` are true in the database at once, which only `applyItemOutcome` can produce and only from a confirmed answer. An unanswered account is `open` or `sent` and never advances; an account that hit the program's round cap is `closed` and never advances; a deleted, updated or unaddressed account is finished and never advances. Matching is strict on purpose — same bureau, then the last four digits of the account number; a creditor name alone is accepted only when the stored record carries no account number AND exactly one account at that bureau bears that name, so two cards from one bank can never both escalate off one answer. Rounds 4–6 reuse the Round 2 / Round 3 letter shapes through the repository's existing `promptPoolRound` rather than a second rule being invented. Proven by `src/underwrite/prior-outcome.test.mjs` (18 tests): with no recorded answer the pack still writes Round 1 and only Round 1 — that regression is pinned first — and with a recorded answer real `ex_round2.pdf` and `ex_round3.pdf` files come out. WHAT A HUMAN STILL DOES BY HAND: nothing in this change mails a bureau letter or reads a bureau reply. Someone still has to put the bureau's scanned reply into `/api/repair/inbound-mail`, and confirm it if the reader is unsure. LEFT AS IT WAS, NOT FIXED HERE: two accounts pre-date this work — the Round 3 letter text still tells the bureau a CFPB complaint is being filed, and the complaint pair is still gated on the pack containing at least one dispute letter rather than on Round 3 having actually been reached. NOT REGENERATED, AND WHY: `docs/journeys` is stale by the same 9 files before and after this change; the journey extractor reads only `netlify/functions/api.mjs`, `src/http/read-api.mjs`, `src/http/partner-read-api.mjs` and `src/http/dashboard-auth.mjs`, this change touches none of them and adds no route, so regenerating here would sweep in another change's undocumented `/api/public/optimize` routes. | Rounds 2 and 3 were dead code the owner had paid for, and the escalation ladder's last rung could not be reached in normal use | (this commit)
2026-08-28 | (changelog repair, no code) | Restored 212 lines of history this branch had truncated. Cause, recorded so it is not repeated: a changelog append written as `open(p,'w').write(new + open(p).read())` — `open(p,'w')` truncates the file BEFORE the read runs, so the read returns empty and only the new line survives. Read first, then open for write. | Self-inflicted, caught by an adversarial review before merge | (this commit)
2026-08-28 | optimize | **COMPLIANCE REVIEW REQUIRED** — `/optimize` now points at the REAL SmartCredit integration link. ConsumerDirect Partner Support gave three links on 2026-08-28 and they are not interchangeable: `smartcredit.com/Fundhub` (branded, no tracking), `smartcredit.com/cblp/?PID=29056` ("for link tracking or integration") and `smartcredito.com/cblp/?PID=29056` (same, Spanish). We were on `www.smartcredit.com/?PID=29056`, which is none of them — not the integration link and it does not track, so earlier enrollments may not have been attributed. Now on the cblp link; the Spanish one is exported alongside it. ALSO: the link has no trial (Build $29.99 / Protect $19.99), so the line under the button now reads "All three bureaus · membership from $19.99 · no free trial" rather than walking someone into an unannounced paywall. | Owner: SmartCredit partner links from Partner Support | (this commit)
2026-08-28 | optimize | **COMPLIANCE REVIEW REQUIRED** — `/optimize` cut down to ONE form. Owner: the page looked wrong and did too much. Now a single card — first name, last name, email, mobile, SMS consent — reading like the SmartCredit signup it leads to, then three short what-happens-next steps. REMOVED from the page: the whole Audit section, the Pay-for-Audit checkout button and the five-step roadmap render. The roadmap API (`?view=roadmap`) is untouched and still works; only the page stopped drawing it. Primary button is `Get My Credit Report` and goes to the SmartCredit partner link served by the API (PID 29056), or the Consumer Direct widget mounts in its place once a client key exists. Booking a call is now the secondary action, not the primary one. Copy follows the `/apply` fragment's voice (`clickfunnels-fragments/02a-apply-top.html`). Ghost wordmark no longer bleeds off the page — it was clipping at both edges and the bottom. `src/http/optimize-html.test.mjs` updated because the PRODUCT changed, not to hide a failure: the Audit-UI assertions are gone, and two new guards are added — the page must carry an `sms_consent` box and must keep the shipped STOP wording. Every compliance guard kept: no credit repair, no CRS, no Identity IQ, no score claim, entity is Fundhub Credit Solutions LLC. 10/10 tests pass, lint clean. | Owner: one box, first/last/email/phone, make it look like the SmartCredit signup | (this commit)
2026-08-28 | (no journey changed by this work — recorded deliberately) | **COMPLIANCE REVIEW REQUIRED** — the whole CRM moved from America/New_York to America/Phoenix: every clock and timestamp, the sales-floor day boundary in `src/sales/metrics.mjs`, and the SMS quiet-hours window in `src/messaging/gate.mjs` (now 20:00–08:00 Arizona; summer behaviour identical, winter one hour tighter). `shell.js` also mounts a clock on the fourteen staff screens that never had one and un-hides the ones pages hid below 1600px. No route, no gate, no migration. Open and NOT fixed: quiet hours are still measured in one fixed company zone for every recipient, so 8pm Arizona is 11pm on the east coast — see `docs/workflows/arizona-time-2026-08-28.md`. | Owner: make the CRM read Arizona time | (this commit)
2026-08-28 | optimize | **COMPLIANCE REVIEW REQUIRED** — Pull your file on `/optimize` now opens the official SmartCredit partner affiliate URL (`https://www.smartcredit.com/?PID=29056`) from the Sept 2025 Welcome to SmartCredit mail. Enrollment Widget stays dark (no client key). No new Commas product. Copy still says Audit. | Owner: Smart Credit on today | (this commit)
2026-08-28 | optimize | **COMPLIANCE REVIEW REQUIRED** — the Audit roadmap on `/optimize` is now the five-step black-report deliverable, not a bare numbered list. Step 1 the laws being broken (per finding: rule, observed vs required, the reason text, the FCRA citations and the Metro 2 reference), step 2 the letter ladder, step 3 a dated path, step 4 an ink verdict panel, step 5 the hand-back to the referring funder. `itemsFromFindings()` in `src/optimize-page/roadmap.mjs` STOPPED DISCARDING severity/field/observed/expected/reason/citations/metro2Ref/scope — the engine always computed them and the door threw them away; no new engine work. `buildOptimizeRoadmap` now also returns `findings`, and `today.preapprovalKnown` so the page can refuse to print the manufactured `$0` that black-report-client.mjs substitutes for a missing preapproval — NULL means unknown and must survive. Copy holds the line: no score claims, no removal promises, never funded-with-us, always we-talk-to-your-funder. R4-R6 are shown as repeats because that is what round-plan.mjs actually returns. 13/13 tests pass, lint clean. | Owner: same style as the UnderwriteIQ black deliverables | (this commit)
2026-08-28 | optimize | **COMPLIANCE REVIEW REQUIRED** — `/optimize` restyled onto the FUNNEL landing look (`clickfunnels-fragments/04a-book-top.html` + `04b`), not the main-site form look it shipped with this morning. Adds the 44px grid backdrop, centred logo, two-part headline ("You Were Referred." / "Book Your Call Below"), 900px wrap, soft floating cards, and the 04b footer furniture: trust marquee, disclaimer block, systems-nominal pulse, ghost wordmark. NEW COPY needing sign-off: eight FCRA marquee lines (rights and process only — no outcome or score claims), and a footer disclaimer stating the entity is not a law firm, bank or lender and no outcome is guaranteed. Funnel COPY was deliberately NOT carried over — those fragments are Fundhub LLC / funding / soft pull, this is Fundhub Credit Solutions LLC booking a call. Behaviour, element IDs, calendar URL and Audit checkout unchanged. 10/10 optimize tests pass, lint clean. | Owner: make the funnel form on brand, FCRA buzzwords in the marquee | (this commit)
2026-08-28 | (no journey changed by this work — recorded deliberately) | **COMPLIANCE REVIEW REQUIRED** — dispute logic and credit-repair messaging. Fixes three faults in the escalation merge recorded on the line below. ONE: the CFPB and state attorney general complaints could ship with ZERO dispute letters, and that failure was hidden. The complaint writer read the stored credit pull directly, so when the scoring engine could not read a client's pull the pack produced no dispute letters at all and still produced the two complaints — which made the file list non-empty, which erased the engine error from `reason`. Both callers decide Delivered vs Delivery Failed on nothing but "are there files?" (`src/workflows/ds-02-diy-letters.mjs`, `src/sales/closer-deck.mjs`), so a closer saw a green Delivered on a client who received two sworn complaints and not one dispute letter. That matters because both complaints say, in the client's own voice and signed UNDER PENALTY OF PERJURY, that the client already disputed with the credit bureaus — untrue if nothing was written — and filing a federal complaint before any dispute was mailed is out of order. Now: `buildEscalationComplaints` takes the dispute letters the pack actually produced and refuses without at least one (`complaintSkip: "no_dispute_letters"`), and the empty-pack `reason` is computed from the earned documents only, so a complaint file can never mask an engine fault. Verified by running: an unreadable stored pull now returns zero files with `reason: "engine_error: rawResponsesFromMerged: no bureau reports to score"`, ds-02 reports `delivered:false` and closer-deck writes `diy_status: "Delivery Failed — Retry"`, both as they did before the merge. TWO: the warning was left behind. In the $1,000 DIY pack the same two complaints ship inside `06-complaints-CONDITIONAL/` behind a cover sheet that says SEND THESE ONLY IF Round 3 is done, DO NOT FILE WITH ROUND 1, and sign the perjury declaration by hand; the repair pack dropped both and shipped the PDFs loose beside the Round 1 letter. The folder and the cover sheet now travel with the documents. THREE: the code was copied, not reused. `maybeComplaintFiles` had been retyped into `src/underwrite/letter-pack.mjs` with the cover sheet omitted, which is exactly how the warning went missing; it is now exported from `src/metro2/diy/package.mjs` and called, and `accountsFromPack` / `blankTimeline` are no longer exported since nothing outside needs them. A test fails if the repair pack ever builds its own complaints again. The regression coverage was blind — it built the repair pack with NO stored credit pull, so the new code exited on its first line and the baseline never saw a complaint. `src/underwrite/output-baseline.test.mjs` now runs `buildLetterPackForClient` — the function the app actually calls — against a readable pull, an unreadable pull and a clean pull, and pins the escalation tail exactly. NOT REGENERATED, AND WHY: `docs/journeys` is stale on this branch by 9 files, but every one of those is a new `/api/public/optimize` route from other work; the journey extractor reads only `netlify/functions/api.mjs`, `src/http/read-api.mjs`, `src/http/partner-read-api.mjs` and `src/http/dashboard-auth.mjs`, none of which this change touches, so regenerating here would have swept in another change's undocumented route. TWO PRE-EXISTING FAULTS RECORDED, NOT FIXED, both live on main today: the Round 3 letter text still tells the bureau a CFPB complaint is being filed, and UnderwriteIQ can still only ever produce a Round 1 letter because Rounds 2 and 3 need a `priorOutcome` field nothing in this repository ever fills in. | Three independent reviews found the same break: a sworn complaint could ship with no dispute behind it, and the failure read as success | (this commit)
2026-08-28 | (no journey changed by this work — recorded deliberately) | **COMPLIANCE REVIEW REQUIRED** — dispute logic and credit-repair messaging. The UnderwriteIQ repair pack (`src/underwrite/letter-pack.mjs`, `pack: "repair"`) now also produces the two escalation documents: a CFPB complaint and a state attorney general complaint. Until now that pack was Metro 2 bureau letters only, to Experian / TransUnion / Equifax and nobody else — while the Round 3 letter text it ships already tells the bureau a CFPB complaint is being filed. Nothing new was written: the wording, the statute cites and the sworn perjury declaration come from the repair brain that already builds them for the $1,000 DIY pack (`src/metro2/letters/complaints.mjs`), the findings behind the claims from `src/metro2/diy/from-crs.mjs`, and the account list plus the round timeline from `src/metro2/diy/package.mjs` (`accountsFromPack` and `blankTimeline` are now exported so there is one implementation, not two). THREE REFUSALS ARE PART OF THE CHANGE: no Metro 2 findings means no complaint at all, because a complaint naming no account is a claim about nothing; no stored credit pull means no complaint; and every complaint is UNDATED with the timeline reading "[DATE — not mailed yet]", so no mail date is ever invented and the client signs the declaration by hand. Reported as `complaintCount` / `complaintSkip` on the pack. Affects the two callers that ask for a repair pack: the closer's Send letters button (`src/sales/closer-deck.mjs`) and the DIY fallback (`src/workflows/ds-02-diy-letters.mjs`). The funding pack is untouched and can never carry a complaint. NO JOURNEY MOVED: no route, gate, screen or role changed — the generated journeys were regenerated and came out byte-identical to origin/main. RECORDED, NOT FIXED: on the closer path the pack's files are counted and then dropped, never saved to the client's documents — that was already true of the bureau letters and is unchanged here. | Owner-set 2026-08-28: UnderwriteIQ's credit-repair section is outdated, merge in the deeper repair brain so it also carries the escalation ladder | (this commit)
2026-08-28 | optimize | **COMPLIANCE REVIEW REQUIRED** — `/optimize` rebuilt on the funnel form system (the `.appform` / `.field` / `.consent` / `.btn` / `.disclaim` parts from the homepage survey), not the main-site look. Adds the Fundhub logo, the Credit Solutions tag, the spectrum eyebrow, mono uppercase labels, the black-edged card, a full-width button and a real footer; drops the 520px stub layout and the 999px pill. NEW: an `sms_consent` checkbox under the phone field carrying the shipped consent wording (msg & data rates, frequency varies, STOP, HELP, Privacy, Terms), reworded for this entity and this action — it was collecting a mobile number with no consent at all. Copy changed: headline, lede, assurances, disclaimer. TWO GAPS RECORDED, NOT FIXED: the handler never reads `sms_consent` so a ticked box is not stored, and Book a call is a plain redirect that sends nothing, so consent is dropped on both paths. Behaviour, IDs, calendar URL and Audit checkout unchanged. | Owner: make the funnel form on brand, and add SMS consent like the main site | (this commit)
2026-08-28 | optimize | **COMPLIANCE REVIEW REQUIRED** — Book a call on `/optimize` now opens `https://apply.fundhub.ai/schedule/phonecall` (Meeting with Chris, 30 min). Removed `funding-book-call` from this page only. Public copy still says Audit, not credit repair. | Owner: credit-repair phonecall calendar | (this commit)
2026-08-28 | optimize | **COMPLIANCE REVIEW REQUIRED** — Same hidden page. See Audit roadmap now runs the existing repair brain (`src/metro2/diy/from-crs.mjs` + `src/repair/round-plan.mjs`) and the UnderwriteIQ client map (`src/underwrite/black-report-client.mjs`) on a stored sample file. Shows on the same page. Then book a call. | Owner: use the credit repair brain for the roadmap | (this commit)
2026-08-28 | optimize (new) | **COMPLIANCE REVIEW REQUIRED** — Hidden referral page at `/optimize` and `/optimize.com`. Entity is Fundhub Credit Solutions LLC. Book a call on `apply.fundhub.ai/funding-book-call`. Optional Audit checkout on keep title Consulting Services Assessment. Smart Credit Enrollment Widget gated on CONSUMER_DIRECT / SMART_CREDIT client key + PID (dark until those names exist). No xyl.in. No Identity IQ. No CRS. Page copy does not say credit repair. | Owner: COMMIT AND LIVE AFTER — Audit + gated Smart Credit | (this commit)
2026-08-28 | all actuals regen (no route/gate change) | Regenerated after restoring two imports the 2026-08-27 merge dropped: `logoPathOrPlaceholder` in src/lenders/resolve-logo.mjs and `linesForEngine` in api/read/underwrite.mjs. The second meant the UnderwriteIQ read endpoint threw on every request, so the generated actuals had drifted from what the code really does. | Owner: remove sim data / fix what the merge broke | (this commit)
2026-08-27 | (no journey changed by this work — recorded deliberately) | Kills the last sideways page scroll in the CRM and makes Pipeline's board fit the owner's 2560 monitor. TWO SCREENS, ONE BUG: campaign-manager (381px of sideways scroll at 1440) and social-studio (195px) were not two problems. shell.js mountChip builds the session chip with its geometry as an INLINE style including flex-shrink:0, measured 558px wide, and placeInHeader drops it into whatever top bar the screen already has. In a bar that is already full, an unshrinkable 558px child pushes the whole PAGE sideways. The four declarations that let it shrink already existed but sat inside the 860px phone block, so they never applied on a desktop; nothing about the problem is phone-shaped, so they are hoisted to apply at every width. That alone also fixed creative-factory, which was scrolling 144px at 1280 and had not been reported. SHRINKING ALONE WAS NOT ENOUGH, and the second half matters: at 1280 the chip obediently collapsed to 26px while its own children kept their intrinsic widths, so a 106px label still hung 101px off the page AND the chip had become unreadable. Squeezing harder makes both worse. So a bar that holds the chip may now WRAP, letting the chip drop to its own row at a readable width. Scoped with :has() to bars that actually contain it, and covering BOTH containers shell.js can choose — `.topbar` and `.topbar-right` (11 screens have the latter; they measure clean today, and covering only the broken one is the half-fix that leaves the other as a landmine). PIPELINE AT 2560: ten columns at the 238px minimum need 2498px and only 2332px exists, so the board scrolled 166px on the owner's monitor at 100% zoom. From 2560 up the minimum may relax to 216px, and flex-grow then pushes the columns back out to fill what is actually there — 220px at 2560, 308px at 3440. Deliberately NOT lowered below 2560: there the board scrolls either way, so narrowing the cards would cost laptop users width and buy nothing. Verified live on all 29 shell screens at 1280 / 1440 / 2560 / 3440: 29/29 clean at every width, zero page-level sideways scroll anywhere, and pipeline board scroll 0 at both 2560 and 3440. Before this change: 26/29 at 1280 and 27/29 at 1440. | Owner-set 2026-08-27: every tab must look the same and none of them may scroll sideways | (this commit)
2026-08-27 | role-sales-manager, role-closer (no route/gate change) | Pipeline's board columns now STRETCH to fill a wide window instead of stopping short of the right edge (owner-set). Was `.col{width:238px;flex-shrink:0}`, which pinned the board at 2498px however wide the monitor got — measured 714px of empty board to the right of the last column at 3440. Now `flex:1 1 238px;min-width:238px`: columns share the free space when there is any and never shrink below the width a card was designed for. `width:max-content` REMOVED from `.board` and it must not come back — with a growable column it stops meaning 'as wide as the columns', because intrinsic sizing resolves each item at its CONTENT max-content; tried first and measured a 3501px board with 338px columns at every viewport, scrolling even at 3440. The board now takes its normal auto width (the scroll wrap's), which is what gives the columns space to grow into. A 4px `::after` spacer restores the trailing gap after the last column that the old max-content used to give via padding. Measured live at four widths: 3440 board 3212 / columns 308 / no scroll and no empty board; 2560 columns 238 and 166px of board scroll; 1440 columns 238 and 1286px of board scroll; 1280 columns 238 and 1446px — the last three identical to before, so nothing regressed on a narrower screen. Page-level sideways scroll stayed 0 at every width. NOTE for the owner: at 2560 at 100% zoom ten 238px columns need 2498px and only 2332px exists, so the board still scrolls 166px there — exactly as it did before. It fills once the window is wider than that, which is what zooming out does. The 238px minimum was NOT lowered to force a fit, because that is a card-design decision, not a layout one. | Owner-set 2026-08-27: the page was fluid but the board was not, so Pipeline alone looked unfinished on a wide monitor | (this commit)
2026-08-27 | (no journey changed by this work — recorded deliberately) | Locks the fluid layout in so it cannot drift back. New guard `src/ui/shell-is-fluid.test.mjs` (7 tests) fails if any screen re-caps its own page column, if the crm-sidebar.css shell rule stops saying `max-width:none !important`, if `.fh-maxw` regains a cap, if the `--fh-maxw` token is deleted (client-portal.html reads it for its 780px reading column), or if the 860px phone pin is removed. The guard immediately caught two real landmines and they are removed in the same commit: `my-numbers.html` `.shell{max-width:1240px;margin:0 auto}` and `sales-floor.html` `.shell{max-width:1280px;margin:0 auto}`. Both were already DEAD — crm-sidebar.css overrode them with !important — but anyone deleting the shell rule believing it to be 'the cap' would have got those two numbers back and left those screens narrow while every other tab filled the monitor. Audited all 36 pages in public/app: 29 carry the shell and every one is fluid; of the remaining 7, only `client-portal.html` is referenced by the staff nav and its 780px reading column is deliberate. `soft-pull-approve.html` keeps its 560px form width — a client approval form, not a tab. Verified on production after the merge of #248: all five sampled screens 0 dead margin at 1440/2560/3440, and every major layout block on all 29 screens grows when the window widens. KNOWN AND NOT CHANGED: the Pipeline board's columns are a fixed 238px each (10 columns = 2498px), so on a 3440 window the board stops 714px short of the right edge — the page is fluid, the board's columns are not. That is a design decision for the owner, not a bug in the shell. | Owner-set 2026-08-27: every tab must look the same when switching between them, and must stay that way without anyone policing it by hand | (this commit)
2026-08-27 | (no journey changed by this work — recorded deliberately) | Follow-on to the full-width change, same day. THREE screens mount the shell's account row (Journeys, My Numbers, Sales Floor) and on Journeys it was stealing 718px of the window. Root cause in shell.js placeInHeader: the host was picked with one grouped `querySelector(".shell, .main, .content, main, .app-shell, .app")`, and a selector list returns the first match in DOCUMENT order, not list order. `.app` is an ANCESTOR of `.shell`, so the grouped form always resolved to `.app` and never to the content column it was written to prefer. Harmless while `.app` is display:block (My Numbers and Sales Floor just get a full-width block row), but Journeys sets `.app{display:flex}`, so the row became a flex ITEM sitting between the rail and the content. Measured live: Journeys painted its content in a 494px column at 1440 and left a 1065px gap at 3440. Fixed by trying the hosts in order, innermost first. After: all three screens 0 gap on both sides, content 1212px at 1440 and 3212px at 3440, no sideways scroll. Also in this change, unrelated to layout: tsconfig.json ADDED — CLAUDE.md §6 requires `npx tsc --noEmit` to pass and there was no tsconfig at all, so tsc found no inputs, printed its help and exited 0; the gate had been passing by checking nothing for its whole life. checkJs is off on purpose (measured 5393 errors if enabled wholesale), files opt in with `// @ts-check`, and money.mjs and chunk.mjs are opted in as the first two. CLAUDE.md §12 updated with the first real database-suite measurement: 6867 tests, 6866 pass, 0 skipped, and the one failure is a connection-role artifact that passes as fundhub_app. | Journeys was unusable wide — content in a 494px column — and two 'done' gates were not checking anything | (this commit)
2026-08-27 | (no journey changed by this work — recorded deliberately) | CRM content now fills the browser window at any zoom instead of stopping at 1800px and centring. Three rules applied the same cap and all three had to go: `.fh-maxw` in fundhub-brand.css (max-width and the auto inline margin dropped, width:100% kept), the shell rule at crm-sidebar.css that made the cap global on 2026-08-18 (inverted to `max-width:none !important` rather than deleted — deleting it would have handed sales-floor.html its own hardcoded 1280px and my-numbers.html its 1240px back, leaving those two narrow while every other screen filled the monitor), and `#fosWrap` in finance-os.html, an ID selector the shell rule cannot reach, which would otherwise have been the single screen still capped. margin-inline is pinned to 0 rather than removed because those same screens set `margin:0 auto` and an auto cross-axis margin cancels flex stretch, which shrink-wraps a wrapper to its widest child. The `--fh-maxw` token is deliberately KEPT at 1800px though nothing caps with it any more: client-portal.html reads it as `min(780px, var(--fh-maxw))` to hold the client's 780px reading column, and deleting the token makes that min() invalid, which drops max-width to none and blows the client portal full width. The phone pin (`max-width:100%` under @media max-width:860px, UI-STANDARDS §11 no sideways scroll) and the 228px rail offset (`padding-left:var(--fh-side-w)`) are both untouched. docs/UI-STANDARDS.md §1 updated in the same commit: "Max content width 1800px, centered" is replaced by "Content fills the window. No max width.", keeping the note on why 1280 was abandoned. | Owner-set 2026-08-27: zooming out on a 2560px monitor to get more room returned none — content stopped growing and every extra pixel became empty side margin (532px dead at 2560, 1412px at 3440). Raising the cap 1280->1800 on 2026-08-17 had not fixed that; removing it does. | (this commit)
2026-08-27 | role-funding-advisor, role-owner (no route/gate change) | The Fulfillment "Needs Pull" and "Needs Consent" tiles were silently dropping almost every client. The fraud check compared TRIM(custom_fields->>'round_hold_reason') to 'Fraud Alert' with no COALESCE, so a client with no hold reason - the common case - produced NULL, which poisoned the whole OR, made NOT NULL null, and COUNT(*) FILTER skipped the row. Both tiles read 0 with a client that qualified on every other term. Adding COALESCE fixes it; a test now pins that a qualifying client must land in one of the two counts. | Owner: Needs Pull tile undercounted | (this commit)
2026-08-27 | role-closer, role-sales-manager (no route/gate change) | **COMPLIANCE REVIEW REQUIRED** — Adds the call_outcomes.transcript column that src/sales/recordings.mjs has been writing to since 2026-08-26 and which never existed. The Meet transcript sweeper runs on cron */10 and failed on every transcript it matched to a client, so no spoken words were ever saved and every logged call read as "no recording link and no transcript" on the unrecorded-calls report. Column is plain nullable text; NULL deliberately means no transcript, because src/sales/unrecorded.mjs distinguishes NULL from empty. Found by a test that had not run since ~2026-08-21. | Owner: Meet transcript sweeper shipped without its column | (this commit)
2026-08-27 | client, role-inquiry-remover (no route/gate change) | Closes the gap hole 17 left. The identity packet now counts a photo ID sent through the inquiry upload door, and every portal door asks what the file is so the subtype it is matched on is real. Before this NO door sent a subtype, so every portal upload landed as 'other' and the packet could not be completed from the portal through any door. An unanswered picker still defaults to 'other', so there is no regression path. | Owner: hole 17 gap — clients uploaded and it did not count | (this commit)
2026-08-27 | client, role-owner (no route/gate change) | Corrects the record on hole 19: repair.enrolled runs onRepairEvent TWICE on the deployed site, not once. The old comment said this HTTP path does not load the workflow registry; netlify/functions/api.mjs does load it. No behaviour change — both runs were already idempotent (an UPDATE, and an email keyed on a provider_ref both runs build identically). Tests now pin that, so a non-idempotent side effect added later fails instead of double-sending. | Owner: hole 19 gap — false comment about a double run | (this commit)
2026-08-27 | role-closer, client (no route/gate change) | Closes the gap hole 10 left. The navigation shell reads ?client= and ?contact= as well as ?client_id=, so the rail no longer points at a different file than the page has open. ?id= stays gated to the Control Panel on purpose — on the Agent Editor it means an agent, not a person, and the suite caught an attempt to widen it. | Owner: hole 10 gap — page and menu disagreed | (this commit)
2026-08-27 | role-closer (no route/gate change) | **COMPLIANCE REVIEW REQUIRED** — Closes the gap hole 16 left. Saving a company's incorporation month and year now re-reads the deck, so the pre-approval figure the closer reads aloud updates instead of staying on the old number until a reload. If that re-read fails the screen says the number above may be stale rather than showing it as current. | Owner: hole 16 gap — saved date left a stale price on a live call | (this commit)
2026-08-27 | role-sales-manager, client (no route/gate change) | **COMPLIANCE REVIEW REQUIRED** — Every recorded robot call now speaks "Just so you know, this call is recorded." before anything else it says. Hole 12 switched recording on with no disclosure anywhere in the system, and src/workflows/ai-set-01-josh-setter.mjs fires that path automatically on booking.created, so the first undisclosed tape would have been of a consumer. The notice is bound to the same request body that carries record:true and cannot be turned off separately; it prepends the Agent Editor prompt rather than replacing it. The bureau path (placeConfiguredCall) sets no record flag and is untouched. | Owner: hole 12 follow-up — taped with no notice | (this commit)
2026-08-27 | client, role-inquiry-remover (no route/gate change) | The client portal shows the upload box to anyone with an open inquiry case, not only clients who bought the repair product. KNOWN GAP: files sent through this door are stored as kind 'inquiry_doc'/subtype 'other', and src/inquiry-ops/doc-gate.mjs still only counts 'client_upload' — so the packet gate does not yet see them. | Owner: hole 17 — inquiry clients were asked for ID with nowhere to send it | (this commit)
2026-08-27 | role-closer, role-funding-advisor (no route/gate change) | **COMPLIANCE REVIEW REQUIRED** — Present and the funding read no longer say "thin file / $0" when the tradelines sit inside the saved credit pull but were never written to the accounts table. Both now read the accounts out of the stored report. KNOWN GAP: api/read/underwrite.mjs walks every pull newest-first for one carrying accounts, while src/sales/closer-deck.mjs still reads only the single newest pull, so the two screens can still disagree. | Owner: hole 11 — Present read $0 over three 700s | (this commit)
2026-08-27 | role-sales-manager, client (no route/gate change) | **COMPLIANCE REVIEW REQUIRED** — Outbound robot calls are now recorded (every tape came back blank before) and dial from a number matching the callee's area code. NOT YET ADDRESSED: no recording notice is played or spoken to the person called, and src/workflows/ai-set-01-josh-setter.mjs fires placeCall automatically on booking.created, so recording is not limited to manually-placed calls. | Owner: hole 12 — Josh's call tapes were empty | (this commit)
2026-08-27 | role-closer, client (no route/gate change) | Client Control Panel, Closer Dashboard and Present all open a file from any spelling of the client id in the web address, instead of one exact spelling. KNOWN GAP: public/app/shell.js urlClient() was not updated, so the page and its own menu can disagree about who is open. | Owner: holes 10 and 18 — links opened a blank file | (this commit)
2026-08-27 | role-owner, role-sales-manager (no route/gate change) | Every message the company sends is filed on the person's thread, so Messaging lists and finds people the company contacted first — previously only people who wrote in appeared. Adds db/migrations/266_backfill_message_threads.sql to backfill existing sent messages. | Owner: hole 15 — Messaging could not find the person | (this commit)
2026-08-27 | white-label, role-owner (no route/gate change) | CRM search finds a white-label partner by the contact person's name, not only by company name or email. KNOWN GAP: the result row is still titled with the company name, so the match looks like a different record. | Owner: hole 20 — partner search returned nothing | (this commit)
2026-08-27 | client, role-owner (no route/gate change) | Enrolling a client in credit repair now writes a repair.enrolled row in the events table so later reporting can see the sign-up happened. KNOWN GAP: the new code comment claims this HTTP path does not dispatch registered handlers; netlify/functions/api.mjs statically imports api/read/workflows.mjs, so on the deployed site the event can reach handlers and run twice. | Owner: hole 19 — repair sign-ups left no trace | (this commit)
2026-08-27 | white-label, affiliate (no route/gate change) | **COMPLIANCE REVIEW REQUIRED** — The partner welcome TEXT is allowed past the send gate on the same consent tick that already released the welcome email; the text was silently dropped every time before. Still holds until 11am Eastern. KNOWN GAP: the partner welcome text carries no client id, so opt_outs (keyed on client_id, channel) is never read for it, and there is no phone-number suppression list — a person who is both a client and a partner can receive it after saying STOP. | Owner: hole 7 — partner welcome text never arrived | (this commit)
2026-08-27 | client, role-funding-advisor (no route/gate change) | Client Control Panel and the Fulfillment desk stop showing stale "Awaiting CRS" and "Pull CRS" blockers once tri-merge scores are on the file, and show Apply Now instead of New Lead. | Owner: hole 1 — CCP contradicted the scores on screen | (this commit)
2026-08-27 | client (no route/gate change; no `-actual.md` moved) | **COMPLIANCE REVIEW REQUIRED** — Document Check (GHL-DOC) is live again on Inngest, so a client file upload is read and answered: DOC-02 asks for a better copy, DOC-03 says approved. It was retired in error by 168, which switched off every GHL-* row when GoHighLevel was cancelled; this one runs on Inngest in this repo, not on GoHighLevel. The other seven GHL-* rows stay retired. Template copy unchanged. | Owner: hole 13 — doc chase dead after upload | (this commit)
2026-08-27 | role-closer (no route/gate change) | **COMPLIANCE REVIEW REQUIRED** — Present asks for the month and year each company on the file was incorporated, and lists every company on the client slide. Saving a date updates the business age, which the UnderwriteIQ stack prices off. Reuses the Control Panel's staff save. No new route. No live pull. | Owner: hole 16 — Present never asked the incorporation date | (this commit)
2026-08-27 | client (no route/gate change; `npm run journeys` byte-identical, measured) | An education enrollment at `/education/enroll/` now places a Sales card on `sales` / `new_lead`, the same board place a homepage lead lands on. Placement is forward-only, so someone already further along is not dragged back, and no `entry.captured` event is emitted, so the funding nurture workflows (s-01, s-02, n-01, at-01) do not chase an education buyer. The generated `*-actual.md` files do not move because the generator maps routes and gates, not card placement — that gap is a finding, not a silent reconcile. No `*-intended.md` rewritten; the intended client journey does not mention education enrollment at all. | Owner: hole 6 — education enroll never hits Sales | (this commit)
2026-08-27 | role-sales-manager, role-funding-advisor, role-closer (no route/gate change) | Pipeline MOVE to another rail hands the client over: the rail the card left no longer keeps an open card for the same person. Same-rail drag is unchanged. | Owner: hole 9 — MOVE left two cards | (this commit)
2026-08-27 | role-inquiry-remover (no route/gate change) | **COMPLIANCE REVIEW REQUIRED** — Specialist Inquiry Generate writes inquiry items from the credit file onto the case. Does not mail. Does not call Repair Generate. | Owner: hole 5 — Inquiry Generate | (this commit)
2026-08-26 | role-funding-advisor, role-closer, client (no route/gate change; `npm run journeys` expected byte-identical) | **COMPLIANCE REVIEW REQUIRED** — CRS funding pack prints the four black reports from this file’s UnderwriteIQ numbers (not Jordan Sample, not Claude JSON) and stores them on Documents. Live site uses the same pdf-lib stack letters already use. Mac can still use the Python printer. | Owner: black-report printer | (this commit)
2026-08-26 | slo-connections (new) | **COMPLIANCE REVIEW REQUIRED** — Owner SLO Connections on Products & Commissions. ClickFunnels funnel + product ID maps to a Fundhub product and can be turned off. A signed paid webhook writes one purchase on the named client. Does not guess by email or price. No new checkout. | Owner: SLO first slice | (this commit)
2026-08-26 | role-closer (no route/gate change) | Closer Dashboard Up next shows the booked call on the open file (today, even after the start time). It no longer hides that person. | Owner: hole 18 — Up next missed the booked call | (this commit)
2026-08-26 | (no `-intended.md` edited; no `-actual.md` moved) | Inquiry files that still need the identity packet get DOC-01 once on `inquiry.docs.needed` or `docs.received`. Retired/draft GHL-DOC still does not text; it writes an honest run row. Course with no case and no deposit is not chased. | Owner: hole 17 — AI doc chase | (this commit)
2026-08-26 | role-funding-advisor (no route/gate change) | Later plays can read saved bank yes/no from `application_decisions` (`src/plays/outcomes.mjs`). No model. No training job. Play-name stamp stays B1. | Owner: named-build item 1 — save outcomes for later plays | (this commit)
2026-08-26 | role-closer (no route/gate change) | **COMPLIANCE REVIEW REQUIRED** — Present Mastery now has Funding Mastery program agreement wording. Closer disposition does not send “you're enrolled” while Mastery is unpaid. | Owner: hole 14 — enrolled email while unpaid | (this commit)
2026-08-26 | role-closer (no route/gate change) | **COMPLIANCE REVIEW REQUIRED** — Present “Invoice this client” writes an `invoices` row (draft, not sent). It no longer mints a pay link for purpose invoice. | Owner: hole 12 — Invoice this client made pay links | (this commit)
2026-08-26 | role-owner, role-funding-advisor, role-inquiry-remover (no route/gate change) | Fulfillment and Client Control Panel next job says Send Letters when repair letters are written and not sent. A leftover inquiry case no longer hides that job. Same words the Specialist repair desk already uses. No paper mail. | Owner: hole 6 — desks said Remove Inquiries when letters were next | (this commit)
2026-08-26 | role-inquiry-remover (no route/gate change) | **COMPLIANCE REVIEW REQUIRED** — confirming a bureau answer now reads `repair_programs.rounds_cap`. Trial stays at 2 rounds: no R3, status `upsell_pending`, trial-done email can fire. | Owner: hole 7 trial cap | (this commit)
2026-08-26 | affiliate, white-label (no new route) | Forgot password on `/api/auth/reset` now mails affiliate and partner logins with the same staff token path. Affiliate apply queues catalog AF1. White-label drip stays not-live — no partner templates. | Owner: launch AFF/WL half-wires | (this commit)
2026-08-26 | role-inquiry-remover (no route/gate change) | **COMPLIANCE REVIEW REQUIRED** — Specialist Repair row now shows bureau + round on each attack, and a six-round “What is next” card (held until a bureau answer; trial blocks R3–R6). New letters name the creditor. No paper mail. | Owner: launch letter attack visibility | (this commit)
2026-08-26 | role-closer, role-sales-manager, role-owner | **COMPLIANCE REVIEW REQUIRED** — logged sales calls with no recording and no transcript after 20 minutes (or after Drive sync) flag as unrecorded on My Numbers, Sales Floor, and the 7am pulse count. No auto-record. No per-miss text. | Owner: missing-record tripwire | (this commit)
2026-08-26 | role-closer (voice, no route/gate change) | AI closer / doc-chase Bland calls now send a first sentence. The agent prove line does not wait for a greeting (that hang-up was 0.13s). | Owner: leftover Bland talk-time | (this commit)
2026-08-26 | role-funding-advisor (no route/gate change) | Staff can type or pick a play name when they log bank yes/no. That name is stamped on the existing application_decisions row. No learner. | Owner: four-builds B1 | (this commit)
2026-08-26 | role-funding-advisor (no route/gate change) | Apply shows the client’s own email for the bank form and warns if the file email is a Fundhub address. Monitor inbox is not the bank-visible email. | Owner: four-builds B4 | (this commit)
2026-08-26 | role-inquiry-remover (no new removal path) | Staff can type the expected creditor name on an inquiry. The desk shows actual (bureau string) next to expected. Does not open removal. Does not feed C-02. | Owner: four-builds B2 | (this commit)
2026-08-26 | role-funding-advisor (receive only) | Bank monitor inbox is minted on the Mailgun domain already in the house (`mg.fundhub.ai`) so a signed inbound can hit F-11. `@fundhub.ai` DNS was not changed. | Owner: four-builds B3 | (this commit)
2026-08-25 | role-funding-advisor, client (no route/gate change) | Underwrite suggestions now read the company on the file (`businesses`) so a saved company is not told “you don’t have an LLC.” Stored `age_months` is passed through. No UnderwriteIQ dollar math change. | Owner: four-plus-pulse P1 LLC suggestion | (this commit)
2026-08-25 | role-owner (ops, no screen) | **COMPLIANCE REVIEW REQUIRED** — pulse SMS dest is `PULSE_SMS_TO` (or `CHRIS_PULSE_SMS`). No number in the repo. Darwin WhatsApp still skipped until `DARWIN_WHATSAPP`. | Owner: pulse texts Chris personal cell | (this commit)
2026-08-25 | role-owner (ops, no screen) | Daily pulse reads a live-path registry and writes uptime/down on the board. Audit only. No auto-fix. | Owner: every new thing on the pulse | (this commit)
2026-08-25 | role-funding-advisor (no route/gate change) | Apply Chrome add-on now wakes on fundhub.ai. Desk shows one line when the add-on is off. A bank link cannot turn the home-city proxy on by itself. | Owner: Oxylabs extension on live CRM | (this commit)
2026-08-25 | role-owner (ops, no screen) | **COMPLIANCE REVIEW REQUIRED** — daily pulse cron at 7:00 a.m. America/Denver (`0 13 * * *` UTC while daylight time). Audit only. Writes `docs/workflows/pulse-YYYY-MM-DD.md`. Texts Chris on the prove number. Darwin WhatsApp waits on `DARWIN_WHATSAPP`. Recon AG-07 + gate-relay stay the tripwire. No auto-fix. | Owner: morning check + existing tripwire | (this commit)
2026-08-25 | role-funding-advisor (no route/gate change) | **COMPLIANCE REVIEW REQUIRED** — Pipeline rail, MOVE dest, and Galaxy no longer show Alt-Fin (Lendflow). Adapter and webhook stay. Card Stacking Apply Now stays. | Owner: park Lendflow desk | (this commit)
2026-08-25 | role-funding-advisor (no route/gate change) | Lenders desk with a client open shows the live match list (company state, then person fields). Full book only when no client is open. No new match engine. | Owner: staff must see fit, not the whole book | (this commit)
2026-08-26 | (no `-intended.md` edited; no `-actual.md` moved) | Inquiry-only files no longer get Apply for Funding after inquiry.removed. Funding product_path still does. Retired/draft GHL-DOC still does not send. | Owner: simple holes — inquiry next job + leftover doc texts | (this commit)
2026-08-25 | role-funding-advisor (no route/gate change) | MOVE to Card Stacking · Apply Now saves the card on that column and opens that board. Next step says Apply for Funding when the card is on Apply Now. | Owner: eight-fix T4 MOVE + next step | (this commit)
2026-08-25 | role-closer (no route/gate change; `npm run journeys` expected byte-identical) | Live closer pack (`GET /api/read/agent-context`) now includes Meet / tape words already stored on the call row as `said:`. Saving a company birth month/year on the file writes Age so it is not a dash. No UnderwriteIQ math change. | Owner: spoken words + company age in closer pack | (this commit)
2026-08-25 | role-funding-advisor (no route/gate change) | Apply uses the company city and state on the file when the person row has none. No intended journey rewrite. | Owner: Apply on Fund Horse must open the bank page | (this commit)
2026-08-25 | role-inquiry-remover | Specialist Stage / Generate letters treats a signed repair contract or an active repair program as the agreement, uses the company street when the person row has none, and restages letters already on file. Honest “needs agreement” only when none of those exist. Send stays off. | Eight-fix T5 | (this commit)
2026-08-25 | role-closer (no route/gate change; `npm run journeys` expected byte-identical) | **COMPLIANCE REVIEW REQUIRED** — Present close row now has Invoice this client (mints a pay link, does not charge). Create-invoice accepts purpose invoice and stores it as custom. Closer Dashboard Send fills company / days / email the way Present does. No `*-intended.md` rewritten. | Owner: eight-fix T7 invoice + Send blanks | (this commit)
2026-08-25 | role-funding-advisor, role-closer, role-owner (no route/gate change) | Documents lists that person's bank / FTC / portal uploads as their own Uploads class. The list asks for the client on the URL (or the last-open file). No intended journey rewrite. | Owner: Documents page must show the files and stay signed in | (this commit)
2026-08-25 | client | **COMPLIANCE REVIEW REQUIRED** — Retired Document Check (GHL-DOC) no longer queues or sends the “got your upload — one thing needs fixing” text (`SMS-DOC-02`). Staff upload, invoice, and Issue Inquiry Removal keep only the text that event asked for. No intended journey rewrite. | Eight-fix T1 — dead Document Check texts | (this commit)
2026-08-25 | role-owner (no route/gate change) | Live /api zip keeps the pg package so health, login, and closer drill Start D1 do not 502 when the bundler leaves the driver external. | Owner: eight-fix T2 live 502 | (this commit)
2026-08-25 | role-owner, affiliate, white-label (+ all actuals regen) | Staff, affiliate, and white-label partners may download Darwin's Message Blaster (`GET /api/gifts/message-blaster`). Clients and unsigned callers stay refused. | Owner: fix staff + partner download FAILs | (this commit)
2026-08-25 | role-closer (no route/gate change; `npm run journeys` expected byte-identical) | **COMPLIANCE REVIEW REQUIRED** — Present now shows the same stacked UnderwriteIQ pre-approval as the UnderwriteIQ read (engine + one business slice per saved company). It no longer uses the canned CRS $125,000. No new formula. No `*-intended.md` rewritten. | Owner: staff must see one money number | (this commit)
2026-08-25 | client (no route/gate change; `npm run journeys` expected byte-identical) | **COMPLIANCE REVIEW REQUIRED** — homepage survey (`fundhub.ai` `#apply`) now asks “Any negatives on your credit report?” (Yes/No → `cf_svy_has_negatives`). 750+ or 700-749 plus No books a funding call. Yes or a low score still goes thank-you. Missing answer still MANUAL_REVIEW. ClickFunnels apply still has no negatives question. No `*-intended.md` rewritten. | Owner: live must hash; homepage 750+ must book | (this commit)
2026-08-25 | role-funding-advisor (no route/gate change; `npm run journeys` expected byte-identical) | Lenders list default is 500 so the full book can paint. Banks with no mark show the dark/gold placeholder tile, not an empty box. No intended journey rewrite. | Owner: ship logos | (this commit)
2026-08-25 | role-closer (no route/gate change; `npm run journeys` expected byte-identical) | **COMPLIANCE REVIEW REQUIRED** — Present first-sale pay links for Funding DFY, Repair DFY, Repair trial, and Funding Mastery skip the upsell/downsell ask. UnderwriteIQ deliverables still require an explicit sale motion. No new route. No `*-intended.md` rewritten. | Owner: primary offers mint like Funding | (this commit)
2026-08-25 | role-closer, role-owner (no route/gate change; `npm run journeys` expected byte-identical) | Staff can start the closer drill on Agent Editor. The existing `POST /api/agents` door now accepts `action=run` for the closer-drill coach only. It does not text a client. No new route. No `*-intended.md` rewritten. | Owner: OP-06 closer drill on Agent Editor | (this commit)
2026-08-25 | client (no route/gate change; `npm run journeys` expected byte-identical) | **COMPLIANCE REVIEW REQUIRED** — each added soft-pull business now asks when it was incorporated (month/year). That date is saved as `businesses.age_months` plus `entity_data.incorporated_date`. Live CRS / Experian Business in this repo does not return the date, so the form is the source. Closers must verify the date (playbook + call-card box). No `*-intended.md` rewritten. | Owner: need incorporation date per company for pre-approval age | (this commit)
2026-08-25 | role-closer, role-funding-advisor (no route/gate change; `npm run journeys` expected byte-identical) | **COMPLIANCE REVIEW REQUIRED** — UnderwriteIQ / closer pre-approval now applies the existing Lite business slice once per saved company (under 12 months = half the card number, under 24 = the full card number, 24+ = twice the card number). Two companies with the same known age raise the shown total vs one. Unknown age still adds $0 — no new rate was invented. No `*-intended.md` rewritten. | Owner: extra businesses must raise the pre-approval | (this commit)
2026-08-25 | client (no route/gate change; `npm run journeys` expected byte-identical) | **COMPLIANCE REVIEW REQUIRED** — soft-pull authorize-then-pay now captures N businesses (name, address, EIN, optional extra owner) on the same page. Price stays $32 + $10 per business. Product cap of 5 is gone; safety ceiling is 20 and is not marketed. Each business is saved on the client and listed as its own fundable file on Pipeline and the Client Control Panel, with state, EIN, and a visible extra-owner warning. Draft intended: `soft-pull-multi-biz-intended-draft.md`. No `*-intended.md` rewritten. | Owner: one flow, many businesses, push live | (this commit)
2026-08-24 | ops-pulse | Pulse gained measured_minutes (Hubstaff + CRM, never locks MODEL) and marketing (spend + cost per booked + fail-closed special ad category). LinkedIn hire stays the existing postJob path. | Owner: implement conveyor KPI plan | (this commit)
2026-08-24 | ops-pulse | Pulse learns from real counts. Rates need 10. Call minutes need 20 timed logs. Below that it says not enough data. No invented averages. | Owner: use data to learn and make discoveries | (this commit)
2026-08-24 | ops-pulse | Closer and funding advisor are one pod. Bar is 27 deposits and 27 funded files per pod. Uneven seats hire the missing half. Packed calendar hires the pair. | Owner: they work in tandem | (this commit)
2026-08-24 | ops-pulse | Starting bars are 30 closer deposits and 27 funded files (desk-time). Spoken 20 is not the bar. | AI-set after owner said 20 was not a lock | (this commit)
2026-08-24 | client | Portal shows Experian, Equifax, TransUnion, and Experian business (1–100) from the real CRS / business file. Missing stays a dash. No new route. | Owner: show 3b score and EX business score on the portal | (this commit)
2026-08-24 | ops-pulse | Pulse grew: gap diagnosis from 20/20 + bookings, hire profile (closer LinkedIn / FA text), ads spend read + review task when spend > 0, raise/bonus task shapes. Fire/raise/bonus still no auto-enqueue. | Owner: Build the rest of the Ops AI brain | (this commit)
2026-08-24 | ops-pulse (new pair; no existing intended edited) | Daily pulse + two briefs + packed-calendar hire task and LinkedIn job post on the existing `postJob` path. Fire is a C-suite task shape only — no auto-enqueue, no revoke. Routes: `GET /api/read/ops-pulse`, `POST /api/ops/hire-closer` (owner, admin). | Owner: Ops / AI COO v1; brain writes tasks, humans act | (this commit)
2026-08-23 | client | Cal.com adapter and webhook door removed. Books, moves, and cancels come from ClickFunnels. A missed call still starts no-show recovery from DPC-02. | Owner: Cal.com is dead; delete it | (this commit)
2026-08-23 | role-closer, role-funding-advisor | After a new-negative pause, the sales task names dirty vs clean bureaus. A later clean CRS snapshot reopens the gate. Staff can release the gate when at least one bureau is clean (who / when / route stored). Repair done asks for a new credit pull and a new funding round. | Pause stopped at the task; recovery was missing | (this commit)
2026-08-23 | client | BS-01 pre-call texts and emails now wait until 48 hours before the call. If the call is sooner than that, that touch is skipped. The 2-hour and 15-minute soon texts stay. | Precall was tied to book time and piled up with the 24-hour reminder | (this commit)
2026-08-23 | client | At book, only EMAIL-S04-01-CONFIRM sends. It carries a 365-day single-use portal link. EMAIL-PORTAL-MAGIC-LINK and the BS-01 kickoff email no longer fire at book. Self-service login links stay 15 minutes. | Three emails at book was too many; a 15-minute login link dies before they open the confirm | b8000636
2026-08-23 | client | welcome, portal invite, offer email, and doc request now take a one-shot database lock so a double form ping cannot send twice | form pinged new lead repeatedly and welcome ran more than once | (this commit)
2026-08-23 | role-closer | closer deposit_collected rate superseded to one sixth | owner chose one sixth not 16.67% | (this commit)
2026-08-22 | role-owner, role-sales-manager (+ every `-actual.md` route table) | **COMPLIANCE REVIEW REQUIRED** — commission payout tracking. One route added: `POST /api/commissions` (owner, admin, sales_manager) to Approve (`earned→approved`) and Mark paid (`approved→paid` + `payout_ref`). Does not send money. Products & Commissions ledger gains select + those two actions. Regenerated `-actual.md` from code. | Owner asked for CRM commission tracking (approve / mark paid) | this commit
2026-08-22 | messaging/templates | seed 015 live template backfill of 41 keys so scratch matches production. | live-only rows made funding queue 0 emails look like a code bug | (this commit)
2026-08-22 | (no `-intended.md` edited; no `-actual.md` moved) | Dragging a card to Closed still emits `round.closeout` for N-04, but inquiry-gate now skips that payload (`stage=closed` / `engagementComplete`) so finishing an engagement does not open a new inquiry case. Money-chain per-round closeout still raises the gate. | Closed-column closeout must not start a new inquiry | (this commit)
2026-08-22 | (no `-intended.md` edited; no `-actual.md` moved) | Section 4 comms wiring. Welcome, portal invite, nobook emails, noshow sequence, offer-bucket email, document request/gate/GHL-DOC routing, AR chase, Closed-column `round.closeout`, N-04 moved behind staff closeout, reschedule restarts S-04B/BS-01, AX-07 new-negative pause. Portal shows the document-agent note when GHL-DOC asks for more. No new route. | Build spec 2026-08-22 section 4 | (this commit)
2026-08-22 | role-owner, role-sales-manager (+ every `-actual.md` route table) | **COMPLIANCE REVIEW REQUIRED** — commission payout tracking. One route added: `POST /api/commissions` (owner, admin, sales_manager) to Approve (`earned→approved`) and Mark paid (`approved→paid` + `payout_ref`). Does not send money. Products & Commissions ledger gains select + those two actions. Regenerated `-actual.md` from code. | Owner asked for CRM commission tracking (approve / mark paid) | uncommitted
2026-08-21 | role-inquiry-remover (no `-intended.md` edited; no new route) | WS-E Repair desk tab. Specialist Repair tab now matches the wireframe: five filter tiles including Trial ending, chip + due columns, row expand with letter drawer and soft-pull PULL confirm, four states (em-dash / empty / error+Retry / loaded). List/detail reads gather program/auth/address/due/parse/SLA facts without money fields; rollups add `trial_ending`. Inline `FHRepairLens` mirrors `src/repair/lens.mjs`. | Repair build batch WS-E dashboard tab | (this commit)
2026-08-21 | client, role-owner, role-sales-manager, role-closer, role-funding-advisor, role-inquiry-remover, affiliate, white-label | One route added: `POST /api/auth/staff-update` (owner, admin). Staff & Teams Edit Person can save name, email, phone, and start date. Monitoring consent still flips on Save for owners. Hubstaff ingest stays dark until that API is on. Regenerated `-actual.md` pages from code. | Owner asked to make Edit Person profile fields work and ship live | 2774da43 / follow-up CI green
2026-08-21 | (no `-actual.md` moved — `npm run journeys` byte-identical) | Overnight W1 regen. Code still writes the same eight role pages and README already on disk. Every journey still diverges from its `-intended.md`: client, role-owner, role-sales-manager, role-closer, role-funding-advisor, role-inquiry-remover, affiliate, white-label (intended is the 2026-08-02 snapshot; actual is 188 routes), plus gate-relay (independently authored, not a copy). No `-intended.md` edited. | W1 overnight launch-fixes regen | uncommitted (`overnight/launch-fixes-2026-08-21`)
2026-08-20 | role-closer (no API route, API gate, or generated `-actual.md` moved) | Owner-approved screen merge. `closer-dashboard.html` now owns the former Call Cockpit live-call panel, client facts, contract send, outcome logging with Save · next call, credit scores, UnderwriteIQ bands, next calls, and compliance checklist while keeping tradeline math, deal math, and lender matches. The fixed `GET /api/read/closer-call` response is the single shared client-data source: its credit scores come from `triMerge`, and it now carries the UnderwriteIQ engine output including `lite_banner_funding`; the browser no longer makes a second UnderwriteIQ read. Both calculators sit in one closed Payment Calculator section. `closer-call.html` redirects to Closer Dashboard with the full query and hash, direct links now target Closer Dashboard, and the duplicate Call cockpit navigation row was removed from the sidebar source and generated copies. `npm run journeys` regenerated all nine files byte-identically because routes and role gates did not change. No `-intended.md` was edited. | Put the closer's context, presentation handoff, call actions, and payment math on the one approved screen without blending two client reads | (this commit)
2026-08-20 | client, role-owner, role-sales-manager, role-closer, role-funding-advisor, role-inquiry-remover, affiliate, white-label | **COMPLIANCE REVIEW REQUIRED** — approved simplify implementation. Three routes were added: staff-only Bank Inbox and client-note save paths, plus owner/admin finance access to effective-dated commission rules; generated actual journeys and the route index were rebuilt from code. Approved duplicate controls and repeated counts were removed from Pipeline, Messaging, Documents, Specialist, Calendar, Products, Content, Staff, Sales Floor, Client Control Panel, and Client Portal without changing Present, Call cockpit, closer context, context fetchers, or contract/pay-link/credit/offer/disposition actions. The Specialist dispute-letter body is now read-only; generation and sending remain unchanged. Client Portal keeps the client document list: client sessions receive their own file metadata from the session-bound Portal Summary read and never call the staff-only Documents read, while staff get a filtered link to the Documents home. Owner-set commission rows start 2026-08-20 for supported bases: closer 16.67% of collected funding deposit and 0.25% of funded amount; manager 5% of collected funding deposit and 0.25% of funded amount. The named downsell/upsell rates remain pending until a durable sale-motion and product identity source exists, and manager upsell was not invented. | Remove approved duplicate displays, make named dead controls perform their real jobs, and store the supported owner-set commission schedule as dated configuration | uncommitted (`fix/simplify-before-polish`)
2026-08-20 | role-closer (no API route, API gate, or generated `-actual.md` moved) | **OWNER DECISION:** the Contract templates screen and its navigation row are owner/admin only. A closer who types `/app/contracts.html` is routed back to the closer dashboard before the protected wording panel is revealed, and Contracts is absent from the closer menu. The page stays hidden until the resolved session passes the shell gate, so a stale cached role cannot flash the wording, Upload, or New controls. Owner and admin keep the screen. The server already refuses every wording write, PDF upload, and field-placement action from a closer, so no endpoint changed. Closers still read and send contracts through the existing client and Documents flows, so the generated route page correctly remains byte-identical. **GAP, recorded not reconciled:** `role-closer-intended.md` still says the contracts route is reachable and does not distinguish that staff send route from this owner/admin setup screen; this owner decision supersedes it for screen access, and the intended file was not edited. | Named contracts permission leak: closers could open an owner/admin wording panel and see its Upload and New controls | uncommitted (`fix/contracts-permission-leak`)
2026-08-19 | role-owner, role-sales-manager, role-closer, role-funding-advisor, role-inquiry-remover (no route, no gate, and no `-actual.md` moved) | **COMPLIANCE REVIEW REQUIRED** — four numbers on the Fulfillment lens were telling lies, found by an adversary pass over the entries below. Same two screens, nothing else touched. **1. The tile disagreed with the row beneath it.** The "Needs Pull" tile counted a client as waiting on a credit pull whenever they had paid and had no credit report row on file. The chip on the row used one more test that the tile did not: a client whose credit status is already recorded as Complete is not waiting on anything. Measured on real Postgres, the tile said 4 while only 2 of those clients got the chip. The tile now uses the same rule as the chip, and the "waiting on written permission" sentence under it uses it too, so no client falls out of both counts. **2. Demo rows counted as real ones everywhere except credit.** Practice data is marked with a demo flag. The credit read always skipped it; the other reads did not. So with Demo Mode OFF, a practice task added a real client to "Action Needed" and painted a warning pill on their row, and a practice funding round could put a funding step on their file. Every read that can skip practice rows now does — tasks, funding rounds, funding cards, documents and inquiry cases. Two cannot: the dispute tables have no demo flag, and the invoice balance view does not carry the one on invoices; both would need a migration and this layer writes no schema, so both are recorded in the code rather than left silent. **Trade-off, recorded not hidden:** with Demo Mode ON, a practice client's own practice rows are now skipped too, so their file shows fewer warnings than before. That is the same behaviour the credit read has always had. **3. A failed read could read as "nothing is wrong with this file".** The warning list is built from three reads. If any one of them refused, the list came back empty and nothing was marked as unread — and an empty warning list is the whole test for "Ready to Fund". So an unreachable table could turn into a step saying the file is clear to fund. A read that fails now says so, that one step answers "we do not know", and the screen falls back to today's display. It does not take away an answer that never needed the list: a client whose step is "Get Consent" still gets it, and a repair-only client still correctly shows no funding step. **4. A tile counting everybody sat over a list that stops short.** "Total clients" counts the whole book while the list below it asks for one page. At 47 clients they match; above the page size the screen made a claim it could not back up. The tile now carries one sentence — "The list below shows 200 of these 431." — and only when the two numbers really disagree. No seventh tile, no paging control. **Read and display only:** no write, no migration, no column, no route, no screen, no tab, no menu row, no workflow touched. **Proof:** every fix has a test that fails against the code as it was — checked by putting each defect back and watching the test fail. `src/fulfillment/read-signals.pg.test.mjs` gains a fixture with a practice task and practice funding round on a REAL client, and a third org proving the tile and the chip agree on "Complete"; `src/fulfillment/read-signals.test.mjs` gains four tests that force the warning read to refuse; `src/fulfillment/next-action.test.mjs` gains five more on the same rule; `src/http/pipeline-screen.test.mjs` gains four on the new sentence. No test weakened, none deleted. | A tile that disagrees with the row under it is worse than no tile, practice data was being counted as real with Demo Mode off, and a failed read was coming out as a clean bill of health. | uncommitted (worktree `feat/fulfillment-layer-phase1`)
2026-08-19 | role-owner, role-sales-manager, role-closer, role-funding-advisor, role-inquiry-remover (no route, no gate, and no `-actual.md` moved) | **COMPLIANCE REVIEW REQUIRED** — three display defects fixed on the same two screens as the two entries directly below, and nothing else touched. **1. The big Next Action line no longer goes blank.** When the system worked a file out and found no step that applies, the line went empty and the page painted a bare em dash — with the grey "Saved on the record: Pull CRS" line sitting right underneath it. A heading, a dash, then the forbidden words: it read as though the dash were hiding the real instruction. The line now reads "No step applies right now." All three answers are plain sentences and no two read the same: the step itself, "No step applies right now.", or "Not worked out yet." The saved value stays exactly where Chris approved it — a quiet line underneath, labelled as what is on the record, never as an instruction. **2. Four money displays that were already on the screen are back to what they printed on `main`.** Prequal, Total Approved and the two income-per-year lines had been switched onto the new shared money rule, which changed them: a recorded zero became "$0" where it had been a dash, and a negative became "$-500". Nobody asked for either. They are back on `main`'s rule byte for byte. The one shared whole-dollars rule stays for the two NEW amounts — this page's funding-round approved amount and the pipeline lens's — so the $50,000-reading-as-$500 defect the entry below fixed stays fixed. **Whether a recorded zero on those old tiles should read "$0" or a dash is Chris's call and was deliberately not made here**; it reads as a dash, which is what it read before this work. **3. Funding money could reach a client who only bought credit repair.** A `funding_rounds` row carries an approved amount, and both screens painted the newest one for anybody who had one, so a REPAIR_ONLY client could read "Round 1 · approved · approved $75,000". No chip is involved, so Gate B's chip guard never saw it. **Gate B now covers the money as well as the chip, through the same helper and the same whitelist** — `fundingChipsAllowed()` in `src/fulfillment/next-action.mjs`, reading `src/config/product-path.mjs`. A round comes back only on the three funding tiers; repair-only, an unrecognised tier and no tier at all get nothing, and that refusal does NOT mark the file as unread — it is the right answer, not a failure. The Client Control Panel's round block also stopped reading the raw newest row as a fallback, which was the way around the gate; it now shows the worked-out round or dashes, and the round number, status and hold reason are still on the page in the tiles that always showed them. **Read and display only:** no write, no migration, no column, no route, no screen, no tab, no menu row, no workflow touched. `npm run journeys` still generates every page byte-identical, the correct result for a change that adds no route and no gate. **Proof:** `src/fulfillment/next-action.test.mjs` gains five tests that fail if a repair-only client is handed a funding round, one of them asserting the round and the chip agree tier for tier so there can only ever be one whitelist; `src/http/client-panel-screen.test.mjs` gains three tests pinning the old tiles to `main`'s rule and three that fail if either screen reads a raw funding-round row again, and the one test that asserted the old blank slot now asserts the plain words. No test weakened, none deleted. Both screens were also loaded in a real browser: a repair-only client carrying a $75,000 round row shows dashes in the round block and no round line on the pipeline lens, while a full-funding client still shows Round 1 · approved · $75,000. | A bare dash above the words "Pull CRS" is the worst way that line could read, tiles nobody asked to change had changed, and funding money was reaching people who never bought funding. | uncommitted (worktree `feat/fulfillment-layer-phase1`)
2026-08-19 | role-owner, role-sales-manager, role-closer, role-funding-advisor, role-inquiry-remover (no route, no gate, and no `-actual.md` moved) | **COMPLIANCE REVIEW REQUIRED** — fixes two defects an adversary pass found in the Fulfillment Layer Phase 1 entry directly below, both on the same two screens and nowhere else. **1. The big Next Action line no longer shows the value saved on the record.** It used to, whenever the answer could not be worked out — and that is exactly the case where consent could not be established, while the saved value is written with no consent check at all and is the literal words "Pull CRS" in `src/workflows/s-06-post-call-funding-purchased.mjs` and `src/workflows/c-05-pre-funding-review.mjs`. A client with no recorded consent could therefore be shown "Pull CRS" on screen, which is the compliance gate Chris called non-negotiable. The big line now only ever shows a step the system worked out; when nothing was worked out it says "Not worked out yet." in plain words. **Nothing is lost:** the saved value moves to a quiet grey line underneath reading "Saved on the record: Pull CRS", so staff keep the information and simply stop being told to act on it. This corrects the sentence in the entry below that says the panel shows the value last saved on the record — it does not any more, and that was the leak. **2. The two screens agreed to disagree about money.** `public/app/pipeline.html` divided a number by a hundred and `public/app/client-control-panel.html` did not, so the same amount could print as $50,000 on one screen and $500 on the other. The real unit was traced, not guessed: `funding_rounds.approved_amount` is a `numeric(14,2)` column and reaches the browser as the text "50000.00", meaning fifty thousand dollars, and `custom_fields.total_funding_estimate` is saved in whole dollars as a plain number. **Both are dollars.** They are exceptions to the integer-cents rule in `CLAUDE.md` §12, which describes `src/commissions/money.mjs`; that module converts into cents at its own door (`src/commissions/calculate.mjs` calls `toCents()` on a funded amount), so cents never leave the commission maths. Both screens now use the one dollars rule, written in a comment beside each formatter. Nothing on file still prints as an em dash and never as $0; a zero that really is recorded prints as $0 on both, because hiding a recorded zero behind a dash calls a known fact unknown. **Read and display only, same as the entry below:** no write, no migration, no column, no route, no screen, no tab, no menu row, no workflow touched. Two screens and two test files, nothing else. `npm run journeys` still generates every page byte-identical, which is the correct result for a change that adds no route and no gate. **Proof:** `src/http/client-panel-screen.test.mjs` is new and fails if the saved text reaches the big line again or if the two screens ever format the same amount differently; `src/http/pipeline-screen.test.mjs` had its money tests corrected from cents to dollars, none removed or weakened. Both screens were also loaded in a real browser against a degraded reply carrying "Pull CRS" and the big line read "Not worked out yet." | The saved text on the loudest line broke compliance gate A on screen, and one amount rendering two ways on two screens is a number nobody can trust. | uncommitted (worktree `feat/fulfillment-layer-phase1`)
2026-08-19 | role-owner, role-sales-manager, role-closer, role-funding-advisor, role-inquiry-remover (no route, no gate, and no `-actual.md` moved) | **COMPLIANCE REVIEW REQUIRED** — the Fulfillment Layer, Phase 1, built on the Phase 0 mapping at `docs/workflows/fulfillment-layer-2026-08-19.md` and Chris's option A. **Read and display only: no INSERT, UPDATE or DELETE anywhere, no migration, no new table, no new column, no new route, no new screen, no new tab, no new menu row, and no workflow or job touched.** **`npm run journeys` was re-run and every generated page came back byte-identical, which is the correct result** — these pages track routes and the gate on each one, and this work adds neither; it adds derived fields to two read endpoints that already existed and paints them on two screens that already existed. That is the same outcome the 2026-08-19 role-inquiry-remover entry below recorded for the same reason. **What a staff member now sees.** Chris's Employee Next Action field is blank on 39 of 47 client records, so the two screens stop showing that saved stamp on its own and instead work the answer out from the client's own records: `src/fulfillment/read-signals.mjs` gathers the evidence with SELECTs only and `src/fulfillment/next-action.mjs` decides, with no database access of its own so the same answer reaches every screen from one rule instead of three copies. The Client Control Panel (`public/app/client-control-panel.html`) gains one read-only block — next action with the sentence explaining why, the active blockers, and where the funding round stands with its finalized state — and its existing "Next action" line is now driven by the same worked-out answer so the two lines on one page cannot disagree. The pipeline board (`public/app/pipeline.html`) gains a **Board / Fulfillment switch on the filter bar it already had**; Board is the default and is byte-for-byte the screen that shipped before, and turning the lens on only sets `hidden` on the board row — no card, column, listener, filter or scroll position is rebuilt, re-queried or re-sorted, and turning it off restores every one of them, including the previous hidden state of the search box, the filter button, its chips and the summary. The lens lists the clients with a next-action chip on each row, the six rollup tiles, and click-a-chip-to-filter. **The order is Chris's, first match wins, and nothing in the code implies it** (Phase 0 §4 says so plainly): Clear Fraud Alert, Get Consent, Pull CRS, Remove Inquiries, Collect Documents, Review Disputes, Review Funding File, Prepare Next Round, Apply for Funding, Ready to Fund. "Lock Fee" is deliberately not a queue chip — money-side signal only — and "File Prep" is deliberately dropped. **TWO COMPLIANCE GATES, EACH BUILT TWICE.** GATE A: a client with no live written permission is never shown "Pull CRS"; their step is "Get Consent", which is ranked above the pull so first-match-wins refuses it, AND `guardConsentBeforePull()` refuses it again on the final answer while knowing nothing about the order, so reordering the list cannot defeat it. This closes Phase 0 defect 2, where the pull button was correctly greyed out but the words above it were not checked at all, so staff read an instruction the system would then refuse — the screen and the button now read the same consent through the same `consentStatus()` call `src/finance/soft-pulls.mjs` gates the pull on, and `src/consent/index.mjs` exports its validity predicate (additive, behaviour unchanged) so a 500-client list cannot fork the rule into a second hand-typed copy. GATE B: a repair-only client is never shown a funding chip — Review Funding File, Prepare Next Round, Apply for Funding, Ready to Fund — enforced by the chip's own predicate AND again by `guardFundingProduct()`, which reads its own separately written list of the four and fails closed on an unknown, missing or unrecognised product type. That closes Phase 0 defect 1 for the display, where a repair-only client is being told to "Apply for Funding" today. Both gates, both halves, have tests that fail if the guard is removed (`src/fulfillment/next-action.test.mjs`, 78 tests). **When a signal cannot be read the screen falls back to today's display — never a blank, never a crash, never a guess.** Every derivation is wrapped; the four new fields are absent from the reply rather than blank; an unknown answer sets `next_action_degraded` and the panel says in words that this is the value last saved on the record. Blank survives as blank: the derivation reads the raw `custom_fields` blob and the raw tag array rather than the response mapper, because that mapper turns a missing `crs_paid` into `false` (Phase 0 defect 7) and "we do not know" is not "no". Money is passed through exactly as Postgres hands it over and a NULL approved amount stays a dash — never $0. **Two of the six tiles are `null` on purpose, and that is the answer, not a gap**: "Ready" and "Total Approved" have no honest source (Phase 0 §5), so each carries a sentence saying so rather than a zero, which would be a claim nobody can stand behind; "Total Prequal" is shown beside the number of clients it came from, because Phase 0 measured the whole company total coming from one client out of 47. **GAPS RECORDED, NOT RECONCILED, and no `-intended.md` was edited:** (1) **no `-intended.md` file names either screen at all** — the Client Control Panel and the pipeline board appear in none of the eight, only `role-inquiry-remover-intended.md` describes a screen and that screen is the Specialist desk — so nothing hand-written says what a next action should say, what order the chips belong in, or that a permission gate or a product-type gate should exist there; Chris's order and both gates are owner-set direction from this batch, not journey-derived, and an agent may not write them into the intended file; (2) **"Ready to Fund" is a step the code now has that no intended journey and no Phase 0 finding authorises** — Phase 0 row 10 recorded zero matches anywhere in the system for it; it was added by the build lead, ranked last, and given a derived meaning (nothing blocking, newest round not on hold, paperwork complete) that is a definition nobody wrote down, and it needs Chris to say whether that is what he means; (3) **the lens shows an eleventh chip that is not on Chris's list** — "Not enough information", the honest label for a client whose next step could not be worked out; it is clickable and filterable like the other ten, and Chris has not seen it; (4) **`role-sales-manager` and `role-inquiry-remover` can now be told on screen to "Get Consent" and have no route to do it** — both reach `/api/dashboard/client` and `/api/dashboard/clients` as staff, but both are blocked from `/api/consent/capture`, which admits owner, admin, closer and funding_advisor only; their own intended pages say that block is correct, so this is a real gap between what the screen asks of those two desks and what the system lets them do, and nothing was changed to close it; (5) **the funding chips fail closed on an unrecorded product type**, which is the safe posture `src/config/product-path.mjs` requires, but it means a client whose tier has never been written sees no funding chip at all — that is honest rather than wrong, and it will look like the funding chips are missing until `clients.outcome_tier` is filled in; (6) **these generated pages cannot show a screen change at all** — they record routes and gates, so a reader comparing intended against actual will see nothing of this work on either page, and this changelog line is the only record of it in `docs/journeys/`; (7) Phase 0's twelve live bugs are untouched and remain Chris's for a later batch — in particular `crs_count` on the client list still counts demo rows as real pulls, and the workflow at `src/workflows/c-03-inquiry-removed-resume-or-hold.mjs:45` still writes "Apply for Funding" onto a repair-only client's record without checking what they bought; only the display is gated here, not the write. | Chris approved the Phase 0 mapping and option A: his Employee Next Action field is blank on nearly every real customer, so the two screens he works from were showing an empty column, and one status was being shown to people who only bought credit repair | pending
2026-08-19 | (route table changed on every page — see below) | Agent Editor and the Bland voice agents, T13 of the 2026-08-18 fix batch. **One route added: `POST /api/agent-call`**, gated to the desks that contact clients (`owner, admin, sales_manager, closer, setter, inquiry_specialist`) with `requireAuth` then a separate `requireRole`, so the journey extractor recognises its shape and it does NOT join the two gates already sitting in the UNVERIFIED list. That single row is the whole journey delta: every generated page moves 175 routes to 176. **What the route is for:** nothing on this site could start a phone call. `src/adapters/bland.mjs` listens for a finished call and nothing ever caused one; `api/inquiry.mjs` proxies to a Vercel app `vendor/inquiry-remover/README.md` records as never deployed, so its 503 was not a missing setting — there was no host to point at. **The screen was telling a human the opposite.** Measured live 2026-08-19: `AG-04 Setter Josh` and `AG-09 Inquiry Removal AI` sat as `status=live` with a NULL prompt, empty guardrails and `updated_at = created_at = 2026-07-31`, and the Agent Editor painted them LIVE, 'acting on real clients on Voice', directly above its own checklist reading 'Promotion is blocked until every check passes'. Zero shadow-log rows, zero messages from an agent, zero `outbound_calls`, zero `bland` webhook captures. `db/migrations/177_agents_live_integrity.sql` corrects both rows to `draft` (keeping runtime and runtime_ref, which record the intent) and adds the invariant 037 never had — a live agent must have a prompt — as a NOT VALID check, because 037's only guard was `agents_runtime_required_ck`, which checks runtime and says nothing about whether the robot has anything to say. The same migration creates `agent_triggers` and `agent_runs`, the two tables the screen's '0 triggers' and '0 runs' counters have never had a source for, both with their RLS shape declared (ENABLE, FORCE, named policy) so they cannot repeat the six-dispute-tables failure T16 fixed. `src/agents/runtime.mjs` now writes an `agent_runs` row at one boundary point on every wake, including the wakes that decided to do nothing — that outcome is by far the most common and was recorded nowhere. **BEHAVIOUR CHANGE worth its own line:** the Agent Editor's Save button now records WHERE an agent runs (`runtime`, `runtime_ref`), appended to the end of the UPDATE because `src/http/agents-write.test.mjs` reads those parameters positionally; until now a saved agent could never be pointed at a real call. The new `src/messaging/providers/bland-voice.mjs` sends `agents.prompt` as Bland's `task`, which is what makes the Agent Editor the source of truth — before it, the words on a real call lived in a hardcoded template literal in `vendor/inquiry-remover/src/agents/setter-prompt.js`, so editing the screen changed nothing a caller heard and editing the file changed nothing the screen showed. It is the only new outbound traffic and it sits in the one directory CLAUDE.md §12 permits, behind the MESSAGING fence, which defaults to BLOCKED. **NOT changed, deliberately:** `src/agents/select.mjs` still refuses `runtime='bland'` — `144_agent_runtime.sql:29-30` records that as an owner decision and the screen now explains it in words instead of contradicting it. GAPS recorded, not reconciled: (1) **no `-intended.md` file names the Agent Editor at all** — `docs/journeys/` holds only affiliate, client, gate-relay, role-* and white-label — so nothing hand-written says what this screen should do, and no `-intended.md` was created or edited; (2) no `-intended.md` describes placing a phone call, so `/api/agent-call` is recorded here as a route and a gate only, and no screen calls it — adding a start-call button is a send control on a screen no journey covers and is left to the owner; (3) the guardrail switches disagree with the engine that enforces them — the screen reads an unset flag as off, `src/agents/guardrails.mjs` reads the same unset flag as on — which fails safe, and the screen now labels them 'NOT SET · ON BY DEFAULT' rather than silently implying the rule is not applied; (4) guardrails are not evaluated on a voice call at all, only on text replies; (5) the two pre-existing untraceable gates (`finance/crs-pull`, `gifts/message-blaster`) are still UNVERIFIED and are not this work's. | 2026-08-19 live audit: the Agent Editor showed two robots as live and covering the desk when neither had ever spoken to anyone and nothing in the build could make them, and showed eight cancelled agents as editable drafts | (this commit)
2026-08-19 | white-label, role-owner (+ every -actual.md: the /api/social/posts gate) | Approve controls so the two held queues can drain, owner-directed follow-up to T11. **No route added and no migration:** `api/social/posts.mjs` already dispatched on an action, so approve and refuse were added there, and `api/brand/review.mjs` already implemented approve and reject, already gated owner/admin and already routed — that half was buttons only. T11 stopped a post the compliance engine marks `needs_approval` at `awaiting_approval` instead of sending it, which was right, but nothing could then approve one, and `src/compliance/screen.mjs:207` returns `needs_approval` for `credit_repair` unconditionally — so every credit-repair post stopped forever. Now an owner or admin can approve or refuse a held post from the waiting list on Social Studio, and approve or send back a submitted brand from the Creative Factory review queue. **APPROVE AND REFUSE ARE OWNER/ADMIN ONLY, enforced on the server, not by hiding a button** — a partner approving their own credit-repair post would defeat the entire hold. That tightening is the only journey delta: `/api/social/posts` moves from `partner, staff` to `employees: owner, admin` plus partner, matching the shape `/api/brand/review` already has. **Recorded fidelity loss:** the extractor reads a gate per endpoint, so the page now describes the whole of `/api/social/posts` as owner/admin-limited when in truth only approve and refuse are — list, queue and discard still serve a partner. That is the same loss `api/brand/review.mjs` already carries, so it matches precedent rather than introducing a new problem, and it is recorded here rather than hidden. Three defects found by adversarial review and fixed before merge, each PROVEN by a test that fails against the pre-fix code: (1) a refusal did not check whether it had actually changed a row, so a post moved by a concurrent approve could be reported refused while it stayed queued and still sent — the refuse arm now RETURNs and throws the same NOT_HELD the approve arm already threw, and because both statements share one transaction the mirror-row write rolls back with it; (2) `withPartnerScope({kind:'partner'})` scopes row-level security to the NAMED partner, not to the caller's company, so one company's owner could approve another company's held post — an org predicate copied verbatim from `api/brand/review.mjs` now guards it, applied ONLY to approve and refuse, and a post belonging to another company answers identically to one that does not exist so the endpoint cannot confirm it is there; (3) two screens still told the reader the opposite of what the code does — the approval panel said "nothing can approve one yet", and the copy-check preview a writer reads BEFORE saving said "needing approval does not hold it back… the post still goes out at its time", which had been false since T11 merged. Both rewritten. GAPS recorded, not reconciled: (1) **no `-intended.md` names an approval step**, so this is recorded as a gate change only and no `-intended.md` was edited; (2) **nothing records WHO refused a post** — there is no `rejected_by` column on either table and none was added; approving does record who and when, refusing is distinguishable from an automatic refusal only by the reason written onto the row; (3) **approve and refuse sit behind the existing "is this partner's marketing suite switched on" check**, so switching a partner's marketing off — the natural reaction to a marketing problem — locks their held posts in place permanently; that ordering is an owner decision and was deliberately not changed; (4) posts already handed to a social account had appeared on no tab at all, so they were added to the waiting list — without that an approved post would have vanished and looked deleted, but it means the waiting count rises on first load for posts that were always there. | 2026-08-19 owner direction after the T11 merge: a post the copy check held, and a brand a partner submitted, both sat in a queue that nothing in the product could empty | (this commit)
2026-08-19 | role-inquiry-remover (no route or gate moved) | **COMPLIANCE REVIEW REQUIRED** — **OWNER DECISION, 2026-08-19: letter generation is triggered from the case detail view on the Specialist desk** — a "Generate letters" action on the case the Specialist already has open. Existing screen, existing surface, **no new page, tab, or menu row**, which keeps the standing no-new-surfaces rule intact. Logged as owner-set. This closes the gap the T4 entry below recorded: `POST /api/repair/generate` existed and worked but nothing could reach it, so a real Specialist could not start it by clicking anything. **No `-actual.md` changed and that is the correct result** — the route and its gate were already added and recorded in the entry below; this adds only a control on an existing screen, and these pages track routes and gates, not buttons. `npm run journeys` re-run, output byte-identical. The control sits in the case detail's letter column, calls `POST /api/repair/generate` with that case's client, and **mails nothing** — `POST /api/repair/send` with `mail: true` is still the only thing that reaches a bureau, unchanged. It reports every outcome the engine can return in plain words, including the refusals: no credit report on file, nothing to dispute, no name on record, already generated, and a per-bureau skip list naming why each bureau was skipped. A letter that was NOT made is reported as prominently as one that was. It deliberately does NOT reload the case row on success, because letters land on the Repair tab and re-rendering would wipe the sentence the Specialist needs to read. Proven in a browser across all six outcomes, with an assertion that **zero** requests capable of reaching a bureau or a client were made: `docs/workflows/fix-2026-08-18/evidence/T4/proof-generate-letters.mjs`. GAP recorded, not reconciled: `role-inquiry-remover-intended.md` still does not name a generate step — its desk list stops at item 7 and its ground-truth routes are `read/repair-cases`, `repair/send` and `repair/exceptions`. The step is owner-set rather than journey-derived, and an agent may not write it into the intended file; a human should add it the next time that journey is revised on purpose. | Owner decision closing the open question T4 raised: the engine could make letters but no human could start it | (this commit)
2026-08-19 | role-inquiry-remover (+ every page's route count) | **COMPLIANCE REVIEW REQUIRED** — Inquiry desk and dispute letters, T4 of the 2026-08-18 fix batch. **One route added: `POST /api/repair/generate`, gated owner + admin + inquiry_specialist** (`api/repair/generate.mjs`, `requireAuth` then a separate `requireRole`, all work in `src/repair/analyze.mjs`). That single row is the whole journey delta: the Specialist's repair group moves 2 routes to 3, every page moves 176 routes to 177, and no existing gate moved. The route exists because the three functions that write a dispute case, its items and its letter had NO CALLER anywhere in the repo — `dispute_letters` has always been empty, so `can_send` on the Repair desk was structurally false and desk item 6 ("Send letters appears only when a letter body is ready") could never fire. `analyze.mjs` reads the newest credit file already on record, runs the existing Metro 2 engine, and stores one case plus one letter per bureau with rule-backed findings. **It mails nothing** — `POST /api/repair/send` is still the only mail click, unchanged. It refuses rather than inventing: no credit file, a clean file, a missing consumer name, a finding with no severity and a letter the variance gate rejects are all recorded refusals, and a test asserts the `[Consumer Name]` placeholder never reaches a mailable letter. Also fixed, and invisible to these pages because they track routes and gates rather than behaviour: the Specialist desk never loaded at all (`data.js` carries `defer`, so the inline block below it read `window.FHData` during parse, found nothing and quit — the work queue sat on "Loading inquiry queue…", the four tiles and the bureau chips kept their placeholders, and no request was ever made; now inside a DOMContentLoaded wait like `client-control-panel.html` and `messaging.html`); the Send button threw `ReferenceError: VIEW is not defined` and printed it into its own label, because three call sites reached for `window.VIEW` which this page never creates (the handle is `window.FHInquiryView`); the page asked for `table.queue` and got the FIRST of three, so rows would have landed under the wrong header; and two write paths committed a change and then reported failure — `clear_inquiry` called the event bus with the wrong number of arguments (the name landed in the `db` slot and the bus threw "event name required" AFTER the inquiry was already cleared) and `confirmRemoval` wrote `case_status = 'Cleared'`, which is not a member of the `inquiry_case_status` enum, so Postgres answered 22P02 and the `inquiry.removed` emit on the next line never ran. Neither branch had ANY test coverage; both do now. GAPS recorded, not reconciled: (1) **`role-inquiry-remover-intended.md` does not name a generate step.** Its ground-truth list is `read/repair-cases`, `repair/send` and `repair/exceptions` only. The route was pre-approved by the owner in the T4 brief and is logged here as owner-set, but nothing hand-written says who triggers letter generation or from where — and per the standing no-new-screens rule no control was added, so today it can only be called directly. That decision is open and is on the fix board. (2) **No `-intended.md` describes the "inquiry complete → next funding round" hop at all**, so it was deliberately NOT built; the event now fires correctly from all three paths, `funding_rounds` for the test client is still 0, and the hop needs an owner decision plus the money chain. (3) The Repair desk stays empty for real clients regardless of this change, because nothing ever creates an optimization-pipeline card — measured live 2026-08-19, ZERO clients in the whole database have one; the one-line producer belongs in `src/workflows/c-06-crs-results-router.mjs` and the missing subscription in `src/register-all.mjs`, neither owned by T4, both filed as cross-thread requests. (4) Three audit findings were already fixed and are recorded as corrections, not re-fixed: the six credit-dispute tables are unlocked on live (migration 200 is applied), one `dispute_authorization` consent exists, and `inquiry.removed` has fired once. | 2026-08-18/19 live audit: the Specialist desk showed four dashes and "Loading inquiry queue…" forever with no failed request, Send turned into the words "VIEW IS NOT DEFINED", clearing an inquiry saved and then said it failed, and no dispute letter had ever been generated | (this commit)
2026-08-19 | (no journey changed — correction to the same day's T14 entry below) | T14 second pass, after an adversarial audit of the committed branch. **The thank-you fix recorded below did not actually work, and this entry corrects it.** The gate was right to key on `fh_booking_v1`, but `clickfunnels-fragments/04a-book-top.html` wrote that key from a 400ms `setInterval` the moment ClickFunnels' hidden slot fields were populated — which happens when a visitor clicks a time, while the contact form is still empty and before anything is submitted. `readBooking()` returned a record whenever start and end were present and let name, email and phone default to empty strings, and the Book-button listener was registered in the capture phase, so it persisted before the request was sent and regardless of whether it succeeded. The result: pick a slot, close the tab, and the thank-you page still read "Your Call Is Booked." with a real-looking date and working Add-to-Calendar buttons for a meeting no advisor knew about. Three changes close it. The interval no longer persists at all — it only rebinds handlers as ClickFunnels re-renders. `readBooking()` now returns null unless a name AND an email are present, so a browsing visitor cannot mint a booking record. `persist()` stamps `submittedAt`, and the thank-you gate now additionally requires that stamp to be under six hours old and the appointment start to be in the future — which also closes a second finding, that a booking which had already happened, or was cancelled and left behind in the browser, was shown as booked forever. Five cases are proven in `docs/workflows/fix-2026-08-18/evidence/T14/gate-cases.json`: no key, slot-picked-but-never-filled, week-old record, already-happened call, and a real fresh booking — only the last is claimed as booked. Also this pass: the "What happens on the call" and "Before we talk" blocks were still shown to visitors with no booking, one of them advising "Need to move it? Reschedule straight from the confirmation email" — both are now hidden unless a booking is proven. On the education page the earlier honesty pass had missed the scrolling banner, which still promised "40+ video lessons per program", "Complete template libraries" and "Lifetime access with updates", and the FAQ, which claimed **"Most students complete a program in 4 to 8 weeks"** — there are no students and nothing has opened; the banner items are replaced with the true status and the FAQ now says plainly that nobody has taken the program so we cannot say how long it takes. `/education/learn/`, the honest "nothing has opened yet" page added in the entry below, was linked from nowhere and unreachable; it is now in the education footer. NOTHING ELSE CHANGED — no route, no gate, no role, so every `-actual.md` regenerated identically and no journey moved. GAPS recorded, not reconciled: (1) **the audit that found all this was incomplete** — it raised 32 candidate findings and hit a session limit at 19 of 71 agents, so 7 were adversarially verified and 25 were never checked in either direction; the ones acted on here were re-verified by hand against the code, and the remaining 25 are candidates, not facts, and are not claimed either way; (2) **the education endpoint has no test that has ever executed** — `scripts/run-suite.mjs:69` exits as soon as any unit test fails, two unit tests already fail on untouched `main`, so the pg tests never start, and `src/http/education-enroll.pg.test.mjs` run directly with no `DATABASE_URL` reports 0 tests; the 5840 passing in the entry below therefore contains no database test at all, which is a repo-wide measurement problem rather than a T14 one, and `scripts/run-suite.mjs` is not this thread's file; (3) nothing in the product reads the education enrollment rows, so a staff member still cannot see a request that arrives; (4) the fragments remain ClickFunnels-hosted, so this correction — like the fix it corrects — changes nothing on the live site until a person pastes it into the ClickFunnels editor. | 2026-08-19 adversarial audit of the committed T14 branch: the booking key was written on slot-click rather than on booking, so the "fixed" thank-you page still told visitors with no booking that their call was booked | (this commit)
2026-08-19 | (no role journey changed — one public route added) | Apply funnel, public pages and education, T14 of the 2026-08-18 fix batch. One route added: `POST /api/public/education-enroll`, the door behind the /education/enroll/ form. The generator classes it with the other genuinely-open no-session routes (`/api/public/survey-submit`, `/api/public/unsubscribe`, `/api/contracts/sign`), so every `-actual.md` gains one route and one open route and NO role's can-reach or blocked list changed. That is correct: a stranger on a marketing page has no session. Four things changed that these pages cannot show, because they track routes and gates rather than screen behaviour. (1) The **enrollment form saved nothing at all** — its script held `var WEBHOOK = ''` and only posted when that string began with "http", then a bare `setTimeout` hid the form and displayed "You're enrolled in the queue. Your portal credentials are on the way." No row, no lead, no email; every person who filled it in was told they had enrolled and was then dropped. It now posts for real, waits for the server, shows success only on a confirmed write, and shows a visible failure with a retry otherwise. `db/migrations/245_education_enrollments.sql` gives those rows somewhere to land, with the same row-lock policy and app-role grants every other tenant table carries. **No event is emitted, deliberately:** `src/events/canonical.mjs` has no name for an education enrollment, and the nearest fit, `entry.captured`, is the funding pipeline's front door — firing it would start s-01, s-02, n-01 and at-01 against an education buyer who never saw a funding survey. A name was not invented and `allowNonCanonical` was not passed. (2) The **thank-you page told every visitor their call was booked**. `clickfunnels-fragments/05-thank-you.html` carried "Your Call Is Booked." as static markup with nothing checking a booking, and the only conditional on the page un-hid the .ics button. Anyone who typed the URL saw it, and so did every non-qualifying homepage survey submitter, because `SURVEY_REDIRECTS` sends both DOWNSELL and MANUAL_REVIEW there. The claim, the calendar block and the "check your email for your calendar invite" line are now all gated on the same `fh_booking_v1` key the .ics button already used; the default markup is the no-booking wording, so a visitor with JavaScript off sees the honest version. `SURVEY_REDIRECTS` was deliberately NOT changed — with the page honest, a downsell visitor now reads a true sentence, and inventing a new downsell destination is a step no `-intended.md` names. (3) The **application promised a second step that was never built**. `02a-apply-top.html` hard-coded "Application · Step 1 of 2"; grep proves the string "Step 2 of 2" exists nowhere in the repo, and the real shape is contact, then eight ClickFunnels survey cards, then book. The false counter is removed rather than a step invented. (4) The **incomplete-application nudge chased people who had finished**. Its completion check read `events` by `client_id`, but `api/public/survey-submit.mjs` emitted `entry.captured` and `survey.submitted` with no client at all, so the check could never succeed and the "they completed it" branch was dead code. Both halves are fixed: the endpoint now resolves-or-creates the client and stamps both events with the id, and the check also matches the email inside the payload so rows written before today still resolve. Note the job itself was already switched back on in `f3fb9a7`; the audit finding that it was deregistered is stale. Honesty pass on the education pages: the claims that a student portal exists, that login credentials are issued, that lessons are available immediately, and a decorative progress bar reading 40% complete, are all reworded or removed — none of `courses`, `lessons`, `modules` or `enrollments` has ever existed in `db/migrations`, and `public/education/learn/` is a plain page saying so rather than a faked player. GAPS recorded, not reconciled: (1) **no `-intended.md` file names the apply funnel, the thank-you page or education at all** — the 2026-08-18 gap audit scored both G4a and G4d "ground truth MISSING", so every verdict in this thread is currently unscoreable against anything hand-written, and an agent may not write an `-intended.md` to close that; (2) the apply, booking and thank-you pages are hosted by ClickFunnels, so the two fragment fixes change nothing on the live site until a person pastes them into the ClickFunnels editor — merging this branch does not ship them; (3) the 555-phone rejection on the apply form is ClickFunnels' own validator, the words "invalid country code" appear nowhere in this repo, and no file here can change it; (4) nothing takes payment for an education program and no CRM screen lists the enrollment rows, so a human must work them by hand; (5) `src/config/offers.mjs` has no key for `credit-mastery` or `capital-strategy`, the two programs the page actually sells, so there is nothing for the pay/unlock chain to grant and no price is stored on the row; (6) the education pages still show `support@fundhubeducation.com`, a domain unrelated to the rest of the site — if that mailbox does not exist, refund requests are going nowhere. | 2026-08-19 live re-walk: the thank-you page told anyone who typed the URL that their call was booked, the application claimed a step 2 that does not exist, and the $5,000 education enrolment form told people they were enrolled while saving nothing | (this commit)
2026-08-19 | client (+ role-owner, role-sales-manager, role-closer, role-funding-advisor, role-inquiry-remover, affiliate, white-label, README) | **COMPLIANCE REVIEW REQUIRED** — Client Portal and welcome video, T1 of the 2026-08-18 fix batch. **One route added: `GET/HEAD /api/content/welcome-video`, gated `staff, client`** (`requirePrincipal(req,res,["staff","client"],{db})` then a separate `requireRole` for the staff branch). That single row is the whole journey delta: `client-actual.md` moves 27 of 176 reachable to 28 of 177 and gains a `content — 1 route` node; every other page moves its total only. It is the first client-readable content route and it exists because an uploaded welcome video was **write-only** — `api/content/upload.mjs` wrote `content_videos.storage_key` and nothing anywhere read it back, `api/content/tiles.mjs` does not even SELECT that column. A client is pinned to their own session on it: `?client_id=` is not read for a client principal, the same shape as `api/read/portal-summary.mjs`. The bytes are served on a signed link with no session at all, because a `<video src>` cannot carry an Authorization header — the same trade `api/documents/[id].mjs` already makes. **The bigger half of this work changed no route and no gate:** `public/portal-login.html` never stored the `account` object that `api/auth/magic-link-verify.mjs` has always returned, so a client who signed in from an emailed link reached the portal with no file id at all — and `public/app/client-portal.html` took an early `return` that skipped every read below it. Proven live on https://fundhub.ai as the real client on 2026-08-19: before, "We could not load your file." and **zero** `/api/read/*` requests; after, six requests, all 200, and the dispute card's real legal wording in place of "Sign in to load the legal wording" shown to somebody who was signed in. The card's "You already signed" was investigated and is **correct, not stuck** — it is a fresh server read of the same validity predicate the soft-pull gate uses. Also: `api/content/tiles.mjs`'s bare catches now forgive exactly one SQLSTATE each (42703 for the missing price column, 42P01 for the missing video tables) so a grant refusal or an RLS deny stops arriving on screen as "0 videos"; the false "Welcome videos are not stored in the database yet" is gone (171 shipped long ago); a save that committed is no longer reported as failed when only the re-read broke; and the portal's dead "Download ↓" button, which never had a click handler and had no client-readable endpoint to call, no longer renders. GAPS recorded, not reconciled: (1) **nothing in the code maps a client to a `content_tier_map.tier_code`** — the Content screen offers product codes, `clients.outcome_tier` is the CRS ladder and is not one, and the portal's tier chip is hardcoded markup; `tierCodeForClient()` therefore returns null with the reason `no_tier_rule_in_code` and every client falls through to the `default` mapping the Content screen already offers, which is an owner decision left visible in code rather than invented; (2) **no `-intended.md` names a client reading their own chat history**, so `GET /api/chat/portal-thread` was designed and deliberately NOT built — a client can send a message and can never see it again, and a staff reply never reaches them; (3) no `-intended.md` names a client-facing view of their credit score, inquiries, funding applications or dispute letters, so all four stay staff-only and unbuilt; (4) the portal's upload card holds a **third** copy of the same id resolver, so a magic-link client still reads "Uploads are off" — reported, not fixed, because closing it needs either a second session round trip or DOM listener-ordering tricks; (5) the two pre-existing untraceable gates (`finance/crs-pull`, `gifts/message-blaster`) are still UNVERIFIED and no third was added. | 2026-08-19 live audit: a client clicked the sign-in link in their email, the site said they were signed in, and the page said "We could not load your file" — with the welcome video, the six offer tiles and the signature box all dead behind the same one missing line | (this commit)
2026-08-19 | white-label (+ every -actual.md: 176 routes to 179) | Marketing suite, T11 of the 2026-08-18 fix batch. **Three routes added.** `POST /api/brand/review` (`api/brand/review.mjs`) — a partner submits their OWN brand for review via `requirePrincipal`, and only an owner or admin may approve or reject, as a SEPARATE `requireRole` call because `requireAuth` silently ignores a `roles` key. `GET /api/social/channels` and `GET/POST /api/social/settings`. All three land with a gate shape the extractor recognises, deliberately: two routes (`finance/crs-pull`, `gifts/message-blaster`) already sit in the UNVERIFIED list and T11 must not join them — checked, the list is still exactly those two. **Brand Studio never loaded the brand it was about to save.** `public/app/brand-studio.html` loaded `data.js` with `defer` and then ran its wiring during parse, so `FHData` did not exist yet: in org mode the block returned in silence, in partner mode it threw `FHData is not defined`, which also killed the status-bar block further down. Proven live 2026-08-19 as `partner@fundhub.ai`: all five boxes `value:""`, **no `/api/partner-brand` GET ever made**, and Save answering "Legal entity is required" with zero network traffic while the server held `entity_name: "E2E WL Click17 Co"`. Now inside `DOMContentLoaded`, matching `finance-os.html` and `client-control-panel.html`. **The Creative Factory Enqueue card was invisible to partners for a reason nobody guessed:** the card's own footnote links to `campaign-manager.html`, and `public/app/shell.js` hides `a.closest('.card')` for any screen a role may not open — so one prose link took the whole primary-action card, and the Approve/Reject row with it, off the page. Fixed inside the screen; `shell.js` is T0's and was not touched. **Creative enqueue was rejected by the database for an owner:** `generation_jobs.requested_by` is a foreign key to `accounts(id)` and the handler wrote a staff id, which are separate tables that can never coincide, so the insert raised and the handler answered 500. `db/migrations/241` supersedes 045 with the typed-requester shape 077 already uses (kind + one FK per kind); the old column is kept, nullable and never written again, because NULL means unknown and must survive. **BEHAVIOUR CHANGE, the biggest in this thread:** `db/migrations/240` adds `awaiting_approval` to `social_posts` and redefines 049's screen-gate trigger so a post the compliance engine marks `needs_approval` can only reach the holding pen, never `queued`. Before this, `src/social/scheduler.mjs` treated `needs_approval` and `passed` identically and 049's own trigger allowed it, so a post the engine said needed a person went out anyway. `src/compliance/screen.mjs` returns `needs_approval` for `credit_repair` **unconditionally** — its comment says the gate cannot be turned off — and **nothing anywhere writes `social_posts.approved_at`**, so from this branch forward every credit-repair social post stops and stays stopped. That is the safe direction and it is deliberate, but it is a stop, and it is recorded here as one. 240 also moves already-queued posts into the pen, but ONLY those whose screening said `needs_approval` and never `passed`; a `passed` post is left where it is, a `posting` post is left alone, and a queued post with no screening row at all is reported rather than guessed at. Nothing is deleted and no post is sent that would not have been sent a minute ago — the migration can only stop a send. GAPS recorded, not reconciled: (1) **no `-intended.md` names a brand-approval step, a social-approval step, or any Social Studio or Creative Factory screen behaviour** — they record which role reaches which route and nothing more, so the reviewer flow is recorded here as a route and a gate only, and no `-intended.md` was edited; (2) **nothing in the product can approve a held social post or a submitted brand** — both endpoints exist and are gated, no screen calls approve or reject, so both queues fill and nothing drains them; that is an owner decision about where the button lives and it needs no new screen; (3) `role-closer-intended.md`, `role-sales-manager-intended.md` and `role-funding-advisor-intended.md` still claim those roles reach Campaigns and Creative Factory while `shell.js` bounces them — missing ground truth, not a T11 break; (4) Connect Facebook / Instagram / LinkedIn still answer 503, because `META_APP_ID`, `META_APP_SECRET`, `LINKEDIN_CLIENT_ID` and `LINKEDIN_CLIENT_SECRET` are unset — no code change fixes that, and the screen now says so instead of telling a partner to press a button they do not have; (5) no `creative_providers` row exists in any migration, so a creative job now queues honestly and then fails honestly — the screen was changed to say the queue cannot run rather than that it starts on its own in a few minutes. | 2026-08-19 live audit on https://fundhub.ai: a partner's Brand Studio showed five empty boxes and refused to save a brand the server already held, the Creative Factory button that makes an ad was invisible, the connected-accounts tile read 0 of 8 with no way to connect, and a post the compliance engine said needed a person was sent anyway | (this commit)
2026-08-19 | (no journey changed by this work — recorded deliberately) | **COMPLIANCE REVIEW REQUIRED** — Client self-serve identity capture, T3-11 of the 2026-08-18 fix batch. **No route added, no role gate moved, every `-actual.md` byte-identical** — `npm run journeys:check` reports up to date. The change rides on the GET side of the existing `/api/consent/capture`, which was already routed and already gated. **Owner decision, 2026-08-19:** identity capture is client self-serve; staff never see, type or handle a Social Security number. That decision cost nothing to honour, because the client-facing form already existed — `public/app/soft-pull-approve.html` is a public signed-link page with no CRM shell and no staff nav (`src/http/app-nav-matches-shell.test.mjs:36`), its form already collects name, SSN, date of birth and address, and `api/soft-pull-approve.mjs` already stores the identity AND captures consent in one submission. The only missing piece was a way for staff to hand that link over, so that is all that was built: the consent GET now also returns a freshly signed link with its expiry, and a plain true/false on whether an identity is already on file. **A boolean and a timestamp are the entire permitted surface** — no SSN, no last four, no date of birth, no address may cross that endpoint, and the test asserting it is a deny-list scan over the whole serialised body rather than a check of three named fields, so a field added later cannot slip past it. Proven by seeding a real identity, invoking the handler and scanning every value in the response. The link is minted for a staff session only — it is a bearer credential and a signed-in client has no use for a link to a page they can already reach. Both extras are soft-pull only: a dispute-authorization request never reads the identity table at all. Two failure paths fail soft rather than blanking the screen: a missing signing key returns the link as null with a sentence naming the setting, and a failed identity read degrades to "we did not find out", which the screen renders differently from "we hold nothing". On the client page, three fixes and no redesign: it was showing the internal string `soft-pull-v1` to a consumer as a heading, a plain "what happens next" list now appears before the form rather than only in small print under the button, and the address is trimmed so a box holding one space cannot read as filled in. GAPS recorded, not reconciled: (1) **no `-intended.md` names identity capture, a bureau pull or the approval page** — they record which role may open which route and nothing else — so none of this screen behaviour is covered by anything hand-written and it was built against the live audit evidence instead; (2) the base-web-address helper is now copied three times across the repo and the copies read two DIFFERENT settings (`PUBLIC_BASE_URL` and `APP_BASE_URL`) — the copy used here was taken byte-identical from the one that mints this same link for the emailed version, so the copied link lands where the emailed one lands, but the duplication is somebody's separate job; (3) `src/consent/disclosures.mjs` already holds a human title for each consent version and `api/soft-pull-approve.mjs` does not forward it, which is why the client page was printing a version string as a heading — the label is now typed into the page because that endpoint belongs to another thread. | 2026-08-18/19 live audit: with consent on file, all three bureau buttons still refused with "no identity on file", and staff had no way to put one there | (this commit)
2026-08-19 | (no journey changed by this work — recorded deliberately) | **COMPLIANCE REVIEW REQUIRED** — Credit file: consent, bureau pull and underwriting, T3 of the 2026-08-18 fix batch. **Every `-actual.md` regenerated byte-identical, and that is the correct result:** no route was added or removed and no role gate moved. `finance/crs-pull`, `consent/capture`, `read/underwrite` and `demo/simulate` all keep the gates they had; the new `simulate: true` field on `POST /api/finance/crs-pull` is a request-body field behind the existing gate, not a route. These pages track routes and gates, and none of this work is either. What changed behind them: (1) a signed SOFT-PULL-CONSENT contract now writes the `client_consents` row the credit-pull gate actually reads — a new second listener on `contract.signed` (`src/handlers/contract-consent.mjs`), gated on the authorization `subtype`, not the template key, so a contract that merely borrowed the name cannot unlock a credit pull. This REVERSES the engineering decision recorded at `src/handlers/contract-signed.mjs:41-48` ("Two writers for one permission is how a revocation stops biting") — **owner-set 2026-08-19**, and that earlier reasoning was taken as the specification: a withdrawal dated after the signature wins forever, and a replayed event cannot write a second row. (2) `POST /api/consent/capture` was missing the `org_id` check both its siblings run, so staff at one company could record consent naming another company's client; it now matches `api/finance/crs-pull.mjs`. (3) The demo seed wrote credit scores under two nested keys no reader looks at, so a client seeded as FULL_FUNDING read $0 and score 0 — the seed now writes the real bureau shape `result.scores` / `scoreModels` / `bureaus`, plus the six `clients.custom_fields` underwriting counts nothing has ever written, and the same client now reads score 731 and $939,500. The reader was NOT bent to fit the seed: `result.scores` is what a real pull stores (`src/finance/crs-map.mjs:196-223`) and the seed exists to imitate a real pull. (4) A bureau pull can now be rehearsed end to end without contacting a bureau (`simulate: true`), stamped in five places, inventing no numbers and writing no outcome tier. (5) The closer dashboard never asked for its data at all — its wiring ran while `data.js` still carried `defer`, so `FHData` did not exist yet and the whole calculator quit in silence; measured before and after, the screen goes from 3 network calls to 7. (6) The Client Control Panel now reads consent state before offering a pull button, shows utilisation honestly as "not measured" when nothing measured it, and the dead GHL Contact button is gone. GAPS recorded, not reconciled: (1) **no `-intended.md` names consent capture, identity capture, a bureau pull or an underwriting number** — they record which role may open which route and nothing more — so none of the screen behaviour above is covered by anything hand-written, and it was built against the live audit evidence instead; (2) staff still have no way to put a client's identity on file, so every bureau button now fails at the SECOND gate (422 "no identity on file") instead of the first — the consent half is fixed and the identity half is an open owner decision, recorded on the fix board, not guessed at here; (3) `src/demo/platform-seed.mjs` carries the identical scores defect and is owned by no thread in this batch, so all 8+ platform demo clients still read $0; (4) `src/liabilities/index.mjs:65` omits `accountIdentifier` from its key list while `src/tradelines/index.mjs:94` includes it, which is a live latent bug on the REAL pull path, not just the demo one. | 2026-08-18/19 live audit: all three bureau buttons refused, a signed consent contract did not count as consent, a $125,000 demo file underwrote at $0, and the closer's calculator showed five dashes | (this commit)
2026-08-18 | (no journey changed by this work — recorded deliberately) | Bank-application launcher, proxy and lenders, T8 of the 2026-08-18 fix batch. **Every `-actual.md` regenerated byte-identical and that is the correct result:** no route was added or removed and no role gate moved — all eight T8 handlers were already in the ROUTES map, verified one by one, and `api/proxy/launch.mjs` keeps the `requireAuth` + separate `requireRole` shape the extractor recognises. Three real defects fixed, each proven broken on https://fundhub.ai on 2026-08-18 before it was touched. (1) Pressing Apply on a bank application answered 503 and recorded **nothing** — `src/proxy/launch.mjs` threw on missing Oxylabs credentials BEFORE `insertProxySession`, so `proxy_sessions` stayed at 0 rows and the attempt was invisible to the audit trail. The check is gone; `launchCredentials()` already returns the same `oxylabs_credentials_missing` error and the same 503 further down, AFTER the pending row exists, so a refused launch now leaves a row at `status='failed'` with `error_code` set. Behaviour change worth its own line: a client with no city or state on file now reports `client_location_missing` instead of the credentials error, because that check now runs first — that is the more useful of the two answers and it is deliberate. (2) The failure box on the Apply screen showed an error code and nothing else, so the manual fallback the audit went looking for never appeared. It now carries a plain `next_step` per failure and states that browser routing is **NOT** active and the application was **not** opened. It deliberately does **not** print host, port, username or password on a failure: the session never came up, nothing was geo-verified, and printing settings that were never checked would be a lie about where the bank thinks you are. (3) `src/banking/accounts-store.mjs` built `ON CONFLICT` without repeating either partial index's `WHERE` predicate (081 line 123, 103 line 162), so Postgres found no arbiter and every provider sync died 42P10 on its very first account — no bank account has ever reached `bank_accounts` through that path against a real database. Fake-database tests could not see it. Also fixed: `api/lenders.mjs` could answer `ok:true` with `lender:null`, which the Lenders screen wrote straight back into its row cache, costing the row its id and breaking that row's next Save; it now returns 409 `save_failed`. GAPS recorded, not reconciled: (1) **no `-intended.md` file describes screen behaviour at all** — they record which role can open which route and nothing more — so nothing hand-written covers the Apply failure copy, the Lenders import, or a connect-a-bank control, and the wording decisions above are recorded here and in code comments instead; (2) `read/proxy-sessions` is routed and gated {owner, funding_advisor} but **no page in `public/` fetches it**, so even a successful proxy session is invisible on screen — T8 did not add a page, because that is a new CRM surface and the owner's call; (3) real bank linking stays an open, honest seam — `src/banking/plaid.mjs` refuses with `not_configured` / `not_implemented` and never returns an empty account list as if it were the truth, and its own header says an agent must not close it; (4) the `lenders` table is still empty for the org, so lender matching honestly returns 0 — the import path is now proven end to end against a scratch Postgres with obviously-fake fixture rows, but no real lender was invented to make the count non-zero. | 2026-08-18 live audit: pressing launch on the bank-application tool failed with a server error and left no record it had ever been tried, and no client could ever match a lender | (this commit)
2026-08-18 | (no journey changed by this work — recorded deliberately) | Bank-application launcher, proxy and lenders, T8 of the 2026-08-18 fix batch. **Every `-actual.md` regenerated byte-identical and that is the correct result:** no route was added or removed and no role gate moved — all eight T8 handlers were already in the ROUTES map, verified one by one, and `api/proxy/launch.mjs` keeps the `requireAuth` + separate `requireRole` shape the extractor recognises. Three real defects fixed, each proven broken on https://fundhub.ai on 2026-08-18 before it was touched. (1) Pressing Apply on a bank application answered 503 and recorded **nothing** — `src/proxy/launch.mjs` threw on missing Oxylabs credentials BEFORE `insertProxySession`, so `proxy_sessions` stayed at 0 rows and the attempt was invisible to the audit trail. The check is gone; `launchCredentials()` already returns the same `oxylabs_credentials_missing` error and the same 503 further down, AFTER the pending row exists, so a launch refused AT OR AFTER the credentials check now leaves a row at `status='failed'` with `error_code` set. **Six earlier refusal paths still write nothing** (`src/proxy/launch.mjs` client_not_found, application_not_found, client_mismatch, lender_not_found, application_url_missing, client_location_missing) — they fail before a sessid exists, and that gap is recorded here rather than closed. Behaviour change worth its own line: a client with no city or state on file now reports `client_location_missing` instead of the credentials error, because that check now runs first — that is the more useful of the two answers and it is deliberate. (2) The failure box on the Apply screen showed an error code and nothing else, so the manual fallback the audit went looking for never appeared. It now carries a plain `next_step` per failure — including a catch-all, so an expired sign-in no longer shows the bare word "unauthorized" — and both failure boxes (the server one and the dropped-connection one) state that browser routing is **NOT** active and the application was **not** opened. **This is NOT the manual fallback of T8-11 and must not be read as closing it:** the manual proxy-settings screen is still reachable only on a successful launch, and launches cannot succeed while the credentials are unset. It deliberately does **not** print host, port, username or password on a failure: the session never came up, nothing was geo-verified, and printing settings that were never checked would be a lie about where the bank thinks you are. (3) `src/banking/accounts-store.mjs` built `ON CONFLICT` without repeating either partial index's `WHERE` predicate (081 line 123, 103 line 162), so Postgres found no arbiter and every provider sync died 42P10 on its very first account — no bank account has ever reached `bank_accounts` through that path against a real database. Fake-database tests could not see it. Also fixed: `api/lenders.mjs` could answer `ok:true` with `lender:null`, which the Lenders screen wrote straight back into its row cache, costing the row its id and breaking that row's next Save; it now returns 409 `save_failed`. GAPS recorded, not reconciled: (1) **no `-intended.md` file describes screen behaviour at all** — they record which role can open which route and nothing more — so nothing hand-written covers the Apply failure copy, the Lenders import, or a connect-a-bank control, and the wording decisions above are recorded here and in code comments instead; (2) `read/proxy-sessions` is routed and gated {owner, funding_advisor} but **no page in `public/` fetches it**, so even a successful proxy session is invisible on screen — T8 did not add a page, because that is a new CRM surface and the owner's call; (3) real bank linking stays an open, honest seam — `src/banking/plaid.mjs` refuses with `not_configured` / `not_implemented` and never returns an empty account list as if it were the truth, and its own header says an agent must not close it; (4) the `lenders` table is still empty for the org, so lender matching honestly returns 0 — the import path is now proven end to end against a scratch Postgres with obviously-fake fixture rows, but no real lender was invented to make the count non-zero. | 2026-08-18 live audit: pressing launch on the bank-application tool failed with a server error and left no record it had ever been tried, and no client could ever match a lender | (this commit)
2026-08-18 | (route table changed on every page — see below) | Bookings, calendar and inbound webhooks, T7 of the 2026-08-18 fix batch. **One route added: `GET /api/bookings`, gated `staff`** (`api/bookings.mjs` via `requirePrincipal(req,res,["staff"],{db})`, all SQL in `src/bookings/store.mjs`, org-scoped on every query). That single row is the entire journey delta — every generated page moves 174 routes to 175 and "Everything else" 31 to 32; nothing else in any diagram changed. A booking is now stored as a booking: `db/migrations/225_bookings.sql` creates `bookings` and backfills it from the `booking.created` / `.cancelled` / `.noshow` events already recorded, keyed on (company, provider booking id) — deliberately NOT on the vendor label, because the same appointment reported under two labels was becoming two rows. `starts_at`/`ends_at` stay NULLable: a payload with no start time is unknown, not epoch. The four booking handlers in `src/handlers/comms.mjs` stop hard-coding `sourceWorkflow: "calcom"` and record the true origin — measured live 2026-08-18, 27 `booking.created` events came from ClickFunnels and **0 have ever come from Cal.com**, while all 16 booking tasks read "calcom". The same migration repairs those 16 rows, but only where a real event proves the true source; an unmatched row keeps the label it has. **BEHAVIOUR CHANGE worth a line of its own:** a cancellation or no-show whose booking id matches nothing no longer invents a booking row — it is logged as a miss, which is what the one-time backfill already did, so the live handler and the backfill finally agree. `src/sales/call-outcomes.mjs` now finds a closer's unlogged calls by title (`Strategy session%`) instead of by the vendor word, which is what stops the relabel emptying the Closer Call, My Numbers and Sales Floor lists. On the screen side, `public/app/calendar.html` never asked for its appointments at all — its inline script ran while `data.js` still carried `defer`, so `FHData` did not exist yet and the read was skipped in silence; it is now inside a `DOMContentLoaded` handler like `pipeline.html` and `messaging.html`, and a failed or HALF-failed read is now visible instead of painting a convincing empty week. The dead `demozone` demo drawer was deleted. Inbound webhooks that verify now leave a row in `webhook_captures` at the one router chokepoint, so "received and ignored on purpose" can be told apart from "vanished" — PostGrid and Bland were always answering correctly and were left exactly as they were. GAPS recorded, not reconciled: (1) **no `-intended.md` file names a Calendar step at all** — grep for "calendar" across `docs/journeys/*.md` hits only this changelog — so nothing hand-written says the owner's Calendar must show a booked call, and an agent may not add it; (2) no `-intended.md` describes bookings as a concept, so `/api/bookings` is recorded here as a route and a gate only; (3) the two pre-existing untraceable gates (`finance/crs-pull`, `gifts/message-blaster`) are still UNVERIFIED and are not this work's. | 2026-08-18 live audit: the owner's Calendar said "Nothing booked" on a day with a real booking, no bookings table existed, and every booking was stamped with a vendor that has never sent one | (this commit)
2026-08-18 | (no role journey changed — recorded deliberately) | Messaging, opt-out and inbound mail, T5 of the 2026-08-18 fix batch. One route added: `GET/POST /api/public/unsubscribe`, the door behind the unsubscribe link now carried by every outbound email. The generator classes it exactly where it belongs — with `/api/contracts/sign` and `/api/soft-pull-approve` under "genuinely open, no sign-in needed, reachable by anyone and not by this journey in particular" — so every `-actual.md` moved from 174 routes to 175 and from 15 open routes to 16, and NO role's can-reach or blocked list changed. That is the correct result: this is a page a person opens while signed OUT, and the signed token in the URL is the credential. Also changed, and invisible to these pages because they track routes and gates rather than behaviour: an inbound email whose whole body is STOP now records an opt-out on the email channel; Mailgun's own `unsubscribed` event does the same instead of being ignored; and an inbound text from a client whose stored phone is not in +1 format now resolves to that client instead of nobody. GAPS recorded, not reconciled: (1) no `-intended.md` names an unsubscribe step, because none of them describes any public no-session page — they describe API routes reachable by a signed-in principal, so `/unsubscribe.html` is outside what they cover, the same way `/contract.html` and `/portal-login.html` already are; (2) the route counts in every `-intended.md` are still the 2026-08-02 numbers and now trail actual by more than this change alone; (3) no `-intended.md` describes opt-out or consent-withdrawal behaviour on any channel, so nothing hand-written covers whether an emailed STOP should stop email — that judgment is recorded here and in the code comments instead. | 2026-08-18 live audit: a client emailed STOP, it was filed as an ordinary message, `opt_outs` held zero rows, and 173 email templates promised an unsubscribe link that did not exist and pointed at a page that 404'd | (this commit)
2026-08-18 | (no journey changed by this work — recorded deliberately) | Database and security, T16 of the 2026-08-18 fix batch. Three things changed and none of them is a route or a role gate. (1) Six credit-dispute tables were locked shut on the live database — the row lock was switched on outside the migration files and no key was ever attached, which in Postgres means deny everything to the ordinary app login. The app read zero rows and could not write at all, and neither failure raised an error, so the whole credit-dispute feature has been quietly doing nothing. `db/migrations/200_dispute_rls_policies.sql` unlocks all six and, importantly, DECLARES the lock state in a file so a test database and the live database finally describe the same thing — the existing guard could never see this because the drift did not live in the files it reads. `201_no_bare_rls_sweep.sql` is the standing net for the next table that drifts the same way; this is the third time this class of bug has shipped (109, 154, now 160/161). (2) Deleting a demo client failed with a server error and left the client behind while destroying its own data first. The delete list was hand-written, named 3 of the 16 tables that can refuse, and silenced every error but the last. It now reads the list from the database itself, runs as one atomic statement, and reports which table refused. (3) `202_client_fk_indexes.sql` indexed 23 columns that point at a client and had no index, which is what made that delete slow enough to time out. **Every `-actual.md` regenerated byte-identical, and that is the correct result:** no route was added or removed, no role gate moved, and `api/demo/simulate.mjs` keeps the exact `requireAuth` + separate `requireRole` shape the journey extractor recognises — deliberately, because two other routes already sit in the extractor's UNVERIFIED list and this one must not join them. Journeys track routes and gates, not database row locks. GAPS recorded, not reconciled: (1) no `-intended.md` file describes the database, row locks, or demo teardown at all, so nothing hand-written covers any of this work — the audit that found it used a board order, not a journey; (2) 147 of 175 tables carry an allow-all row policy, so one shared app login can read every client's file — that is an owner-level decision about the product's security model, deliberately NOT changed here, and it is recorded on the fix board; (3) `affiliate_payout_lines` carries a guard trigger with no demo exemption, so a demo client whose commission landed in a paid affiliate run still cannot be torn down — it now fails loudly by name instead of silently. | 2026-08-18 live audit: the credit-dispute feature had nowhere to read or write, and pressing delete on a demo client errored and left it there | (this commit)
2026-08-18 | (no journey changed by this work — recorded deliberately) | **COMPLIANCE REVIEW REQUIRED** — Background jobs and automations, T6 of the 2026-08-18 fix batch. Two workflows that existed on disk but were never served are now served — `s-02-incomplete-survey-nudge` (on `entry.captured`, 400 rows and nothing listening) and `inquiry-call-sweeper` (cron `*/15`) — an **owner decision taken 2026-08-19**; `src/workflows/index.test.mjs` now fails if any `inngest.createFunction` on disk is neither served nor named in `DELIBERATELY_UNSERVED` with a reason, so 53-written/51-served cannot hide again. `GET /api/read/workflows` stopped reporting `engine_active` from a single env-var check and stopped calling a workflow "live" because an event row shares its name; statuses are now `scheduled` / `trigger_seen` / `trigger_never_seen` / `no_emitter`, each row carries `can_start_today` with a plain reason naming the switched-off secret, and `run_records_available` is always false because nothing in this database records a workflow finishing. `POST /api/journeys/run` now excludes demo journey rows (they have no start event, fired nothing, and their presence stopped the six real journeys being walked at all) and reports the skipped keys; the runner passes `skipInngest: true` so the always-rolled-back harness can no longer start real automations over the network; `PUT /api/journeys` validates node config per step type. **Every `-actual.md` regenerated byte-identical, and that is the correct result:** no route was added or removed and no role gate moved — `journeys`, `journeys/run` and `read/workflows` keep the gates they had (`FINANCE` after `requireAuth`, `STAFF` for the registry read). These pages track routes and gates, not workflow registration or screen wording. GAPS recorded, not reconciled: (1) no `-intended.md` names the Automations or Journeys screen behaviour at all — they describe route reach only — so nothing hand-written covers what either screen should say, which is why the wording changes were made against the audit evidence rather than against an intended journey; (2) `role-owner-intended.md` does not say who may run a journey test, so the existing owner/admin gate was left exactly as found; (3) the six journey keys the runner knows (`client`, `setter`, `closer`, `advisor`, `affiliate`, `partner`) still do not match the eight journeys these pages track — `role-owner` and `role-inquiry-remover` have no tree, recorded here and in the runner's own header, not invented. | 2026-08-18 live audit: the Automations screen told the owner 49 of 51 jobs were live when the count had risen only because auditors fired test events, two clock jobs were labelled dead while working, and the journey test reported "0 of 51 automations reached" while quietly able to fire live automations | (this commit)
2026-08-18 | role-closer (+ role-owner, role-sales-manager, role-funding-advisor, role-inquiry-remover, client, affiliate, white-label, README) | `/api/payment-links` now reads **owner, admin, sales_manager, closer** instead of **owner, admin, sales_manager**. A closer gained exactly one action — creating a payment link. Reading the list, texting the link and expiring a live ask are unchanged and still refused to a closer; those refusals are pre-existing decisions and were not reopened. The route gate is the union of everyone who can reach the route at all (the convention `scripts/journeys/extract.mjs` already applies to a method-dependent gate), with the narrowing enforced inside the handler. | T2-20: the closer is the person whose job is taking payment on the call, and was the one role refused — proven live on https://fundhub.ai, `403 "this endpoint is limited to owner, admin, sales_manager"`. A closer already mints the same links through `api/closer-deck.mjs` (`CLOSER_DECK_ROLES`), so this is parity with a door that already existed, not a new capability. **Gate shape matters beyond the role list:** the first version of this fix called `requireRole(res, staff, rolesFor(req))`, and `extract.mjs`'s regex reads only a bare identifier — so it silently extracted the route as "any signed-in employee" and `npm run journeys:check` went stale across 9 files. Regenerating in that state would have published, on the pages a non-coder reads, the false claim that a setter or an inquiry specialist can ask a client for money. The gate is now a literal `new Set([...])` the extractor can read. GAP recorded, not reconciled: no `-intended.md` file names who may create a payment link, so there is no hand-written journey to check this against. | (this commit)
2026-08-18 | (no journey changed by this work — recorded deliberately) | Nav and reachability, T0 of the 2026-08-18 fix batch. Two built screens became reachable: `content-admin.html` (added to shell.js's ALL and to OWNER_ADMIN_ONLY, matching ROLE_SETS.OPS, with a Content row under Marketing) and `consent-capture.html` (added to ALL behind a new `CONSENT_DESK_ONLY` list — owner, admin, closer, funding_advisor, matching `CONSENT_ROLES` in api/consent/capture.mjs — with a Consent row under Client ops and a CLIENT_SCREENS entry so the row carries the client). Both had been bouncing every role, the owner included. An admin no longer opens `client-portal.html`, `affiliate.html` or `partner-galaxy.html`: `ROLE_TABS.admin` moved from `"*"` to its own branch subtracting a new `ADMIN_BLOCKED` list. **The owner is deliberately unchanged and that is an open question for the owner, not a decision an agent made** — shell.js and client-portal.html both carry a supported owner walk of a client's portal. A signed-out client on `client-portal.html` now lands on `/portal-login.html` instead of the staff password page they can never satisfy; staff and affiliates keep `/login.html`. Width: `.fh-maxw` gained `width:100%` (an auto inline margin cancels flex stretch, which was sizing wrappers by their content) and crm-sidebar.css now carries the §1 1800px cap for every rail screen. **Every `-actual.md` regenerated with a route-count drift that is NOT from this work:** 173 routes to 174 and UNVERIFIED 1 to 2, from a route added by earlier work whose gate the extractor cannot trace (`finance/crs-pull`, `gifts/message-blaster`). These pages track routes and gates; this change touched neither, only which screens a role's browser will open. Regenerating turned the pre-existing red "the journeys are not stale" case green. GAPS recorded, not reconciled: (1) no `-intended.md` names a sign-in page for any role, so nothing hand-written covers where a signed-out client should land; (2) no `-intended.md` says who may OPEN `client-portal.html` — they describe API routes only — which is why the owner half was left alone; (3) a partner still has no Campaigns row: `white-label-intended.md`'s "Campaigns (6 routes) — should be reachable" is a route count that four row-DENIED staff roles carry verbatim, so it does not authorise a nav row. | 2026-08-18 live audit: Content Admin threw the owner out, nothing linked to the consent screen, and an admin's menu opened a client's credit file | (this commit)
2026-08-17 | (no journey changed — recorded deliberately) | Creative Factory declutter. The Creative Factory screen lost the builder-facing text a user could see (file-and-line references, migration numbers, database column names, raw data dumps, and the `CF-00`-style panel codes) and lost the open 29-row table of block rules with its CROA / FTC Act / FTC Endorsement Guides citations. In its place is a closed section, "Why creative gets stopped", written in plain words, with a shorter table (Applies to / What it stops / Who sets it) and no law numbers. It sits inside a new closed Reference card holding four closed sections. Panel order is now partner scope → writing budget → generate and decide → jobs → library → review queue → brand kits → Reference. The full cited list moved to `docs/compliance/creative-block-reasons.md`. **Every `-actual.md` regenerated byte-identical, and that is the correct result:** the screen calls the same eight endpoints before and after (the seven `/api/creative/*` routes plus `GET /api/partner-marketing/usage`), no route was added or removed, no gate moved, and `npm run journeys:check` reports "docs/journeys is up to date (9 files)". These pages track routes and gates, not screen wording or panel order. GAPS recorded, not reconciled: (1) all eight `-intended.md` files still say Creative Factory is **4 routes** — it has been **7** since `actions`, `generate` and `run` landed on 2026-08-02, the same day the intended pages were generated. (2) `white-label-intended.md` §5 says the usage card shows "tokens used this month vs a 250,000 cap"; the card is now headed "Writing budget" and says "words", and the cap is whatever `/api/partner-marketing/usage` returns (`src/brand/meter.mjs` defaults to 250,000, but a partner row may set its own). (3) No `-intended.md` file has ever described the on-screen block-reason table or its citations, so neither their presence nor their removal is covered by any hand-written journey. | Owner: "The CROA/FTC compliance rules table with statute citations does not belong on a marketing screen — move it somewhere appropriate or collapse it" | (this commit)
2026-08-17 | (no journey changed — recorded deliberately) | Contracts/Documents de-dup. The Contracts screen became a contract-template loader (queue, four status tiles, detail card, Send-reminders and Void all removed); Documents absorbed the sent-contract queue, its five states, download, Void and a new per-contract Remind. The `contracts.html` nav row left Funding for Admin as "Contract templates". **Every `-actual.md` regenerated byte-identical, and that is the correct result:** no route was added or removed (`remind` and the `document_id` filter ride existing routes) and no role gate moved (`OWNER_ADMIN_ACTIONS` and ROLE_SETS.STAFF unchanged; nav visibility deliberately left alone — board Q1). Journeys track routes and gates, not screen layout or menu grouping. GAP recorded, not reconciled: the staff-side tampered-contract warning went with the detail card and was not rebuilt on Documents — the refusal itself is still enforced in `src/contracts/sign.mjs` (409 content_changed) and still shown to the signer on `public/contract.html`. | Owner: "Contracts is confusing, janky, and redundant with Documents… one place for document handling, one for contract template setup" | (this commit)
2026-08-17 | role-owner | Owner and admin keep the whole lender book. A new role group, `LENDERS` (owner, admin, funding_advisor), now shows in the README role-set table next to FINANCE, STAFF, OPS and HIRING. The four lender routes read owner, admin, funding_advisor instead of all seven staff roles on every journey page, including client, affiliate, white-label and role-inquiry-remover, where they sit in the blocked list. | Owner: the Lenders list is for the funding advisor and the owner only | 7efa24e + 8659d5f (actual pages regenerated in a41f2fe)
2026-08-17 | role-funding-advisor | The funding advisor keeps all four lender routes and is now the only staff role besides owner and admin who can reach them. Reach is unchanged; what changed is the company it keeps. The Lenders sidebar row is now advisor-only (`ADVISOR_ONLY` in shell.js) instead of shared with every staff rail. | Owner: the Lenders list is for the funding advisor and the owner only | 7efa24e + 8659d5f (actual pages regenerated in a41f2fe)
2026-08-17 | role-sales-manager | The sales manager lost the lender book. Same four routes moved to blocked. `GET /api/read/lender-matches` is deliberately unchanged and still open to the sales manager. The Lenders row is off the sales manager's sidebar. | Owner: the Lenders list is for the funding advisor and the owner only | 7efa24e + 8659d5f (actual pages regenerated in a41f2fe)
2026-08-17 | role-closer | Closers lost the lender book. `GET /api/read/lenders`, `POST /api/lenders`, `GET /api/read/lender-observations` and `POST /api/lender-observations` moved from can-reach to blocked — 118 of 170 routes became 117 of 173. `GET /api/read/lender-matches` is deliberately unchanged and still open to closers, so the "which lenders fit this client" box on the closer dashboard still works. The Lenders row is off the closer's sidebar. GAP: no `-intended.md` file mentions lenders at all — they were written 2026-08-02 and the lender database landed 2026-08-04, so nothing hand-written records who should reach it. Recorded, not reconciled. | Owner: the Lenders list is for the funding advisor and the owner only | 7efa24e + 8659d5f (actual pages regenerated in a41f2fe)
2026-08-17 | role-owner (+ all actuals regen) | Company Brain became a full chat. Two new routes: `GET/POST /api/company-brain/upload` (staff add a document) and `GET/POST /api/company-brain/threads` (saved conversations, scoped to one org AND one staff member). An uploaded document is NOT answerable until the owner approves it — `retrieve.mjs` filters on `approval_status` inside the query, before ranking, on both the staff and affiliate paths. Approve/reject stays owner-only (H-3); admin still excluded. Drive files backfill as approved, so Drive behaviour is unchanged. | Owner: full-width chat, document upload, approval gating | (this commit)
2026-08-17 | role-owner (+ all actuals regen) | Owner/admin can change a staff job (`POST /api/auth/staff-role`). Invite and forgot-password send through Resend. Gift download `/api/gifts/message-blaster` is UNVERIFIED (gate not traced). Intended still says 7 sign-in routes; actual is 10. | Named staff-accounts fix | (this commit)
2026-08-17 | role-inquiry-remover | Specialist desk: same login (`inquiry_specialist`), badge name Specialist, one page with Inquiries / Repair toggle. New `GET /api/read/repair-cases`. Stuck-file path opened to Specialist. COMPLIANCE REVIEW REQUIRED — dispute letters, human send only. | Owner: GO on repair screen as inquiry role, renamed Specialist, toggle not a new sidebar row | (pending commit)
2026-08-17 | role-owner + role-closer + affiliate + white-label | Owner answers from the 2026-08-17 UI audit: beta screens stay on the owner rail; partner Home is partner-galaxy; Content mock is off the rail; owners are off the staff roster; booked never undercounts held; contract sign has no decline button; Present fills the client's first name. | Owner answers on ui-audit-2026-08-17 | (pending commit)
2026-08-17 | affiliate + white-label | Affiliates and white-label partners may download Darwin's Message Blaster for Mac (`GET /api/gifts/message-blaster`). Gift cards on affiliate.html and partner-galaxy.html. Not staff, not clients, not the DIY brokerage. | Owner: gift the Mac blaster to affiliates and WL partners | (pending commit)
2026-08-17 | role-owner | Owner/admin can read and save Content (`/api/content/tiles`, `/api/content/upload`). Closers and advisors cannot. | Named fix: Content had no save path | (pending commit)
2026-08-17 | gate-relay | If the watcher stops or the Mac slept, Telegram says the messenger went down (or that it is back). Silence is no longer the same as "nothing needs you." | Owner: add to job 1 | (pending commit)
2026-08-17 | gate-relay | A reply closes the question it was sent for (button or reply-to), not the oldest open one. The text names the thread. An unanswered question is sent again after 15 minutes. Wait with no question file fails out loud. | Owner: GO on job 1 plus wrong-thread yes is worse than a missed ask | (pending commit)
2026-08-17 | gate-relay | Telegram now sends the real ask (`context`) when the question is a stub like "Build?". | Owner: phone must say what he is deciding | (pending commit)
2026-08-17 | role-closer | Closers can read `GET /api/read/deal-math` — net cash and monthly from the numbers they type plus the drawn amount on the file. | Owner: close the deal-math HIGH; do not invent a draw | (pending commit)
2026-08-17 | role-closer | Closers can read `GET /api/read/closer-now` — which call they are on right now and which is next, with no client in the URL. Dashboard and call cockpit use it. | Owner: one read, not thirteen features | (pending commit)
2026-08-17 | gate-relay | New local messenger: skills write a gate file, Telegram asks Chris a one-word decision, his text or voice comes back as a decision file. SMS/Twilio adapter is a stub. Live Telegram unverified — bot token not in env yet. | Owner: run the build machine from the phone | (pending commit)
2026-08-16 | client | Portal shows the pre-qualified amount after the soft pull (`GET /api/read/portal-summary`), and portal chat now answers with Claude instead of only filing the message for staff. Staff Ask tab answered by Claude, grounded on the platform-help corpus. GAP: `client-intended.md` does not describe an AI reply in the portal — owner directed it on 2026-08-16, recorded here rather than silently reconciled. Company Brain Knowledge mode still cannot answer: zero documents are synced and retrieval needs OpenAI embeddings, which are not configured. | Owner: "the portal doesn't say how much you're pre-qualified for" and "the chat bots aren't plugged in — use one API" | (pending commit)
2026-08-16 | client | Clients can read their agreements on the portal (`GET /api/read/portal-contracts`) with sign links for pending contracts. Five placeholder contract templates seeded (repair trial, repair, funding, combo, soft pull). Present deck auto-picks template from offer. | Owner: multiple deal types need matching agreements visible to clients | (pending commit)
2026-08-16 | role-owner (actuals regen no-op) | Campaigns sample action log no longer ships Dana Reyes. | Overnight leftover furniture after live HTML scan | (this commit)
2026-08-16 | role-owner (actuals regen no-op) | Calendar, hiring, and template-editor no longer ship Jordan Blake / Marcus Webb / Carlos Bettencourt / Meredith Yao in the HTML. Route tables did not change. | Overnight P0 — live scan found 3 furniture pages | (this commit)
2026-08-16 | affiliate + white-label (+ all actuals regen) | Website apply is a real public door (`POST /api/public/partner-apply`). Affiliates can read their own roster row. Partners can save brand and publish funnel pages. Intended files still do not mention website apply or a personal URL. | Owner: fake affiliate and white-label come in through the site, get a login, get a URL, make a funnel | (this commit)
2026-08-16 | role-owner (+ all actuals regen) | Two auth routes added — `POST /api/auth/invite`, `POST /api/auth/suspend`. Owner makes a company login and can revoke it. Intended still says 7 sign-in routes; actual is 9. | Only Chris is staff; he creates and revokes logins | (this commit)
2026-08-15 | all eight | One finance route added — `GET,POST /api/company-brain/sync` (owner, admin, sales_manager). Sales floor shows today's Meet recordings from Drive. COMPLIANCE REVIEW REQUIRED — call recordings | Drive connector existed; keys were never set; SM needed a quick list | (this commit)
2026-08-15 | all eight | Two staff routes added — `POST /api/customer-insights`, `GET /api/read/customer-insights`. Three collections: start (existing apply survey), mid check-in 7 days after pay (phone/AI, not Meet), ending Google Meet interview after funded. COMPLIANCE REVIEW REQUIRED — answers may later feed ads/VSL | Beta customer insight store + auto tasks | (this commit)
2026-08-15 | client | Follow-up pack: S-04B booking confirm/24h/2h SMS, no-book chase after survey, fixed 3-way handoff copy, GHL agents retired, Twilio MMS MediaUrl support. COMPLIANCE REVIEW REQUIRED — outbound SMS | Owner: fix leftover fall-off / agents gaps | (this commit)
2026-08-15 | client | BS-01 SMS pre-call companion: three texts on booking (booked → +24h → 2h before call), no video links, stops if call held. Email drip unchanged. COMPLIANCE REVIEW REQUIRED — outbound SMS | Owner: SMS back-end selling off GHL | (this commit)
2026-08-15 | client | Portal pre-call polish: hero welcome, Facebook wins group, offers from catalog, empty states before call/credit pull, chat auto-opens with pre-call questions. COMPLIANCE REVIEW REQUIRED — offers, credit-pull empty states, chat collecting client data | Portal access is granted before the call | (this commit)
2026-08-15 | role-closer (+ all actuals regen) | Present soft-pull send + client soft-pull-approve (signed URL) + closer-set e-book email with placeholder PDF attachment. COMPLIANCE REVIEW REQUIRED — fee timing, consent, credit pull, payment rails | On-call soft pull from Present; refuse → e-book downsell | (this commit)
2026-08-14 | role-closer, role-owner, role-sales-manager | Regenerated actuals — `GET /api/read/closer-deck` and `POST /api/closer-deck` (closer desk). Present button on the call cockpit opens the 24-screen closer deck with live contact data. COMPLIANCE REVIEW REQUIRED — dispute letters, fee timing, payment rails | Plug closer deck into CRM | (this commit)
2026-08-14 | all eight | Regenerated actuals — `POST /api/repair/send` (STAFF) human PostGrid mail for repair DIY/DFY | Bureau letter mail W2 — shared send helper; COMPLIANCE REVIEW REQUIRED | (uncommitted)
2026-08-10 | all eight | Regenerated actuals after Metro 2 / DFY repair events and `repair/exceptions` route landed | Credit-repair spine commit a50ed59 left journeys/diagrams stale; CI suite failed | (this commit)
2026-08-06 | role-inquiry-remover | Regenerated actual after inquiry-gate build; remover queue expands into case detail (letter review, send, fraud-docs upload) on same page | Inquiry Gate v2 W5 — no new HTML/nav | 890f1ba
2026-08-05 | all eight | Regenerated actuals — `GET /api/read/company-activity` (STAFF) for Galaxy presence feed | Hook Galaxy to real shifts/agents activity; Demo Mode seeds open shifts | 403085b
2026-08-04 | role-owner, role-sales-manager, role-closer, role-funding-advisor | Regenerated actual journeys after pipeline board load path + s-01 card placement | Actual board path no longer flashes sample cards; new leads get a Sales card | c0fa78b
2026-08-04 | role-closer, role-sales-manager | actual regenerated — call cockpit, my-numbers, sales-floor screens + call_outcomes APIs | sales dashboards build | pending
2026-08-04 | all eight | Two routes added — `GET /api/read/agent-context`, `GET /api/read/agent-shadow-log` (STAFF). Agent runtime on `message.inbound` (select → context → guardrails → Anthropic/shadow → dispatch via gate). Migration 144 (renumbered from 142 at merge — main already had 142–143). AE-08 reads real shadow log; client control panel shows agent context card | Registry stored agents but nothing executed them; GHL cutover would kill conversational AI | (this commit)
2026-08-04 | role-inquiry-remover, client | Added inquiry-removal bridge routes `GET /api/read/inquiry-cases`, `GET,POST /api/inquiry-cases`, and webhook provider `inquiry-removal` under `/api/webhooks/:provider`. Inquiry Remover queue shows case call state / hold time with Mark Cleared + Close Case; Client Control Panel shows active inquiry-removal case status. Migration 143 (additive on 140). | Bridge IRA Airtable runtime into the platform CRM desk | (this commit)
2026-08-04 | role-owner, role-sales-manager, role-closer, role-funding-advisor, role-inquiry-remover, client, affiliate, white-label | Two routes added — `GET /api/staff/telemetry` (FINANCE) and `POST /api/staff/monitoring-consent` (owner only). Hubstaff poll sweeper + consent gate on `staff.monitoring_consent_at`. Staff & Teams gains Telemetry tab. COMPLIANCE REVIEW REQUIRED: employee monitoring consent capture | Spec §14 deep monitoring | (this commit)
2026-08-04 | role-funding-advisor, role-owner | Routes added — POST /api/proxy/launch, POST /api/proxy/end, GET /api/read/proxy-sessions (owner + funding_advisor) | Oxylabs residential Apply door: geo-targeted proxy before lender application_url; Chrome extension one-click + manual fallback | (this commit)
2026-08-04 | role-funding-advisor, role-closer, role-owner, role-sales-manager, role-inquiry-remover | Lender + inquiry ops port: `lenders` / observations / applications Airtable shape / application_decisions / funding_closeout(+items) / inquiry_removal_cases / ai_bureau_config / inquiry_prep / inquiry_log call_state / business_tradelines. Routes: lenders, lender-observations, applications, inquiry-cases, ai-bureau-config + reads. CRM Lenders (list + mismatch + AI bureau tabs); inquiry-remover case queue; closer lender-match tile; client panel case status; `inquiry.removed` on confirm/case close; closeout on `round.funded`. Migrations `138`–`140`. Tables empty; CSV/CRM import required | Platform had fundability scoring but no lenders, success fee, or AI inquiry bridge | (this commit)
2026-08-02 | all roles | regenerated actuals for social/publish, creative/run+actions, partner-brand/verify-domain, public/partner-page, hosted /sites | finish items 4 and 5 | 2e1e77e
2026-08-02 | all roles | regenerated actuals for chat/demo/campaigns/social/creative/partner-pages routes | session six-items build | c820270
2026-08-02 | client, role-owner, role-sales-manager, role-closer, role-funding-advisor, role-inquiry-remover, affiliate, white-label | Three routes added — `POST /api/read/company-brain` (STAFF), `POST /api/read/company-brain-affiliate` (affiliate/partner), `GET,POST /api/company-brain/reviews` (owner only). Migrations 130–133 (renumbered from 127–130 at go-live). Drive/OpenAI sync stays off until credentials are set | Company Brain steps 1–7 | (this commit)
2026-08-02 | role-owner, role-sales-manager, role-closer, role-funding-advisor, role-inquiry-remover, client, affiliate, white-label | One route added — `GET /api/read/search` (ROLE_SETS.STAFF). Global CRM search from the shell (top-bar button + ⌘K/Ctrl+K), results grouped by clients/contracts/documents/conversations/pipeline cards, org from the session. messaging.html gained `?conversation_id=` deep-link for click-through. Route total moves up by one on every staff journey; principals stay blocked | There was no search anywhere in the platform. Built end to end in one session: endpoint, ROUTES entry, FHData.search, shell overlay, messaging deep-link, endpoint + screen tests | (this commit)
2026-08-02 | client, role-owner, role-sales-manager, role-closer, role-funding-advisor, role-inquiry-remover, affiliate, white-label | One route added — `GET,PUT /api/org-brand` (any signed-in app principal can read; owner/admin staff write). Route total moves by one everywhere. CRM chrome themes from the org's own brand tokens; partner brand stays funnel-only. Migration renumbered 127→128 at go-live merge | Owner decision in docs/BRAND-THEMING-SPEC.md: partners must not recolor the internal CRM when they edit Brand Studio | (this commit)
2026-08-02 | all role + client actuals | regenerated after Agent Editor / pipeline / products write APIs and empty-state screen fixes | controls that looked saved now persist; sample KPIs show honest empties | (controls-persist, already on main)
2026-08-02 | client, role-owner, role-sales-manager, role-closer, role-funding-advisor, role-inquiry-remover, affiliate, white-label | Added `-intended.md` for all eight, generated after the fact from the same extracted data as `-actual.md`, and added a README section documenting that the join between this directory and `src/journeys/runner/`'s six CRM-automation keys was never finished (role-owner and role-inquiry-remover have no automation tree; the other four use shorter names) | The first live run of the Journey Runner against production (docs/JOURNEY-RUNNER-RESULTS.md, 2026-08-02) found no `-intended.md` existed at all, so nothing here could ever be compared against intent, and separately conflated the Journey Runner's journey keys with this directory's before the two systems were understood to track different things. The owner directed the intended files be created now, explicitly marked as written after rather than before the code — a stopgap, not a substitute for a human writing real intent next time these journeys change on purpose. No rename was made to either key set; documenting the gap was the owner's call over reconciling it, since reconciling would mean either inventing two roles' automation trees from nothing or rewriting the fuzzy-match logic several other checks depend on | (this commit)
2026-08-02 | client, role-owner, role-sales-manager, role-closer, role-funding-advisor, role-inquiry-remover, affiliate, white-label | Two routes added — `POST /api/auth/magic-link` (open) and `POST /api/auth/magic-link-verify` (open), portal sign-in by emailed link. Route total moves to 79 everywhere, and both show as open sign-in routes on every page, the same way `/api/auth/login` and `/api/auth/reset` already do. Only the `client` journey changes in substance: it is the only one that can now actually start a session, going from 11 reachable routes to 13 | A funding client could not sign in AT ALL. The tables and the session code have existed since 044_accounts.sql, but the only way to get a session was a password, and nothing in this system has ever set a password on a client's account — no screen, no script, no email. So the portal was reachable in principle and unreachable in practice. An emailed link uses the one thing we can already check: the address on their file. Nothing in the added path transmits — the email is queued into `messages` exactly like every other send in this codebase and the dispatcher is still deliberately unscheduled (CLAUDE.md §12), so the last step remains a switch only the owner throws. The task this came from stated that `accounts` and `account_sessions` have no migrations; that was checked and is not the case (044 creates both and is in the expected-migrations manifest), so no duplicate tables were written — see docs/PORTAL-AUTH-SPEC.md | (this commit)
2026-08-02 | client, role-owner, role-sales-manager, role-closer, role-funding-advisor, role-inquiry-remover, affiliate, white-label | One route added — `POST /api/documents-upload` (staff + client), client file uploads end to end. Adds a fifth `documents.kind` (`client_upload`, db/migrations/118_client_uploads.sql — renumbered from 117 during merge, main already had 117_account_magic_links.sql) so uploaded files go through the existing documents registry — same versioning, same signed-download path — instead of a second system. Files are sniffed by magic number (jpg/png/pdf only, 10 MB default cap) and stored via a new Netlify Blobs provider in src/documents/store.mjs, the storage decision for this build. A successful upload emits the existing canonical `docs.received` event (not a new `docs.uploaded` — none exists anywhere in this codebase; `docs.received` is already what src/workflows/f-06-funding-conditions-missing-docs.mjs listens for to clear a client's "Missing Documents" hold). The signed-download route (`/api/documents/:id`) now streams bytes through the store for opaque, non-URL storage keys instead of only redirecting — Netlify Blobs keys aren't public URLs the way Vercel Blob's were, so the redirect-only path could not have served them. Route lives at `/api/documents-upload`, not `/api/documents/upload`, because src/http/routes.test.mjs forbids an exact ROUTES key that would sit under the `documents/` prefix branch (the signed-download route) and depend on lookup order. Client-portal upload card and a new drop zone on the staff client screen (client-control-panel.html) both wired to it; route total moves to 78 everywhere. Full design in docs/UPLOADS-SPEC.md | Ticket: build client file uploads end to end, storage decision (Netlify Blobs) already made. Full non-pg suite green before commit; DATABASE_URL unset in this sandbox so the new .pg.test.mjs (upload, reject invalid type/size, storage round-trip, event emission) could not run here — see docs/UPLOADS-SPEC.md for what needs verifying against a real database | (this commit)
2026-08-02 | role-owner, role-sales-manager, role-closer, role-funding-advisor, role-inquiry-remover, client, affiliate, white-label | One route added — `/api/payment-links` (GET, POST; owner/admin/sales_manager, same ROLE_SETS.FINANCE gate as its neighbours finance/subscriptions and finance/cards). Route total moves to 78 everywhere | Built the CRM's "create a payment link" action end to end: 119_payment_links.sql (new table, distinct from `invoices`), src/payment-links/index.mjs (create/send/expire/paid), src/adapters/commas.mjs gained buildCommasCheckoutUrl (pure URL construction — no outbound fetch was added anywhere) and a `ref` field on the inbound webhook normalizer so a payment.received event can find the link that asked for it, and src/handlers/payment-links.mjs reacts to that event to settle the row. Subscriptions.html gained a Payment Links panel. Full detail, including the vendor-shape assumptions this needs confirmed against a live Commas sandbox, is in docs/PAYMENT-LINKS-SPEC.md | (this commit)
2026-08-02 | client, role-owner, role-sales-manager, role-closer, role-funding-advisor, role-inquiry-remover, affiliate, white-label | Launch-readiness pass on the staff reply inbox. No route added or removed, so the totals hold at 80 — but three gates changed behaviour. (1) OWNERS NO LONGER CLOCK IN: `requireActiveShift` gained an opt-in `exempt` option and all three shift-gated endpoints (api/messages, api/inquiries, api/tasks) pass SUPER_ROLES, closing the question ACTIVE-SHIFT-ROLLOUT.md §3c and §4 explicitly refused to guess. An exempt caller's work is recorded against a NULL shift, which is the honest value rather than an invented one. (2) HELD MESSAGES NOW ACTUALLY SEND: netlify/functions/staff-message-sweeper.mjs, a Netlify scheduled function every five minutes, scoped to messages a person wrote. (3) The texting-hours rule is stated on the compose box before anything is typed | Owner decisions, 2026-08-02, plus two defects found while acting on them. The overnight hold was enforced and announced nowhere, so the first anyone knew of it was a reply that did not go — and the screen's promise that a held text 'will go out on its own' was untrue, because nothing in this repository had ever called the dispatcher on a clock. The sweeper is deliberately NOT the Inngest one: registering that needs INNGEST_EVENT_KEY, which CLAUDE.md §11 reserves for the owner because it makes 47 workflows live, and it would drain thousands of stale workflow-queued messages onto real people. A migration repointing SMS to Twilio was written on the owner's stated direction and then WITHDRAWN — acting on it surfaced A2P 10DLC, which three places in this repo already record as the reason Twilio ships disabled; unregistered carrier traffic is filtered silently, and a migration runs unattended. 118 is a documented no-op carrying the switch for a human. The related real defect is unfixed and named: the GHL relay addresses by ghl_contact_id, which is NULL for every client created since the cutover, so those customers cannot receive a text reply. Also: the inbox list query was rebuilt so it stops scanning the whole conversations table per page load, the needs-reply tab moved server-side, and Playwright landed — CLAUDE.md §6 gate 4, listed as required since the file was written and never once runnable. 18 browser tests, blocking in CI. Full spec in docs/REPLY-INBOX-SPEC.md | (this commit)
2026-08-02 | client, role-owner, role-sales-manager, role-closer, role-funding-advisor, role-inquiry-remover, affiliate, white-label | Three routes added — `GET /api/read/inbox` and `GET /api/read/messages` (both STAFF-gated) and `POST /api/messages` (staff principals only, and gated on an open shift). Route total moves to 80 everywhere. The staff reply inbox is built end to end: an inbound SMS or a client's own email now finds-or-creates that client's conversation for that channel and threads the message onto it; messaging.html stops being sample markup and becomes a real inbox (conversation list newest-first with a needs-reply marker, thread view, compose box, client context panel), and a staff reply goes out through the existing dispatcher and the existing compliance gate. `messages.sender_staff_id` (120, renumbered from 117 during merge — main already had 117/118/119) records who wrote it. Inbound email threading is keyed on the sender matching a client record — a bank statement forwarded to a client's monitoring inbox is NOT threaded as though the client had written it | COMPLIANCE REVIEW REQUIRED: this is the first path in the repository where an employee types free-form text and it reaches a consumer. Nothing about the gate changed — opt-out, quiet hours and the restricted-wording rules apply to a typed reply exactly as they apply to a template, and compose deliberately does not call the gate itself so there is one verdict taken at send time. Template approval does not apply because there is no template row; that is stated and tested rather than assumed. Two gaps are recorded rather than filled: an emailed reply files no `staff_events` row (the frozen telemetry vocabulary has no kind for an email, and a call site does not get to mint one), and nothing schedules `dispatchDue()`, so a text deferred for quiet hours has nothing to release it at 11am. Full spec in docs/REPLY-INBOX-SPEC.md | (this commit)
2026-08-02 | all eight | No route or screen change — the fix is underneath. Now that the outbound queue actually drains, two things that were safe only because nothing sent are not safe any more, and both are closed. (1) A journey run mints fake clients and puts real-looking messages in the queue; the run-level fence refuses a run when the company's routing can reach the outside world, but the QUEUE OUTLIVES THE ROUTING — run against the memory provider today, connect Mailgun next week, and the next drain mails invented people. The dispatcher now refuses a synthetic client outright whenever the resolved provider can transmit, permanently and with no override. (2) `messaging_settings.alert_email` was collected and never read; three or more failures in one pass now file a task for the owner saying emails are not going out, once per company per day | The fence's own header recorded this as an open question left for whoever plugged sending in — that was the previous commit, so it is answered here rather than left as a note. The rule is the INVERSE of the old one: not "only test clients may be sent to", which refused every real client and is why the fence was unwired in the first place, but "a test client may never be sent to for real." The alert is a task and NOT an email on purpose — it fires exactly when sending is broken, so an emailed alert is the one message guaranteed not to arrive. Also fixed the same committed-DDL race in the outbox test cleanup that was fixed in the hiring tests | (this commit)
2026-08-02 | all eight | The platform can send email at all. One route added — `POST /api/messages-outbound` (`status` is STAFF; `dispatch`, `settings`, `email_invoice` and `email_invoice_backlog` are owner/admin), so route totals and role reach move by one everywhere. Routed as `messages-outbound` rather than `messages` because the already-merged staff-reply-inbox branch claims `POST /api/messages` for a staff reply — both branches independently wrote a handler at `api/messages.mjs` for two unrelated features, and this one was renamed at merge time (see docs/MERGE-LOG.md section 3). One workflow joined the registry (`message-dispatch-sweeper`, 48 -> 49). The change underneath every journey page is that the outbound queue finally drains: twenty-six workflows have been calling `sendTemplated()` and writing queued `messages` rows since they were written, and NOTHING HAS EVER SENT ONE. Invoices were worse — `invoices.status` has included 'sent' since 017 and no code ever emailed one, so two workflows have been raising invoices nobody was told about. `invoices.emailed_at` now records what actually left, separately from what we claimed | The owner asked for invoices by email and for any other sending gaps, and rejected sending as a hidden global switch: "it's not like the workflow is something that we turn on... it should be an option, the ability to set it up... it's a fucking CRM." So the gate moved rather than disappearing — db/migrations/126_outbound_switch.sql gives each company its own `outbound_enabled`, a daily send cap, and a record of who changed it, all visible in the CRM on the ops-admin Outbound Mail panel. INNGEST_EVENT_KEY is still unset and still the owner's to set (CLAUDE.md §11); registering the sweeper sends nothing without it, and the same drain runs today from a button. The compliance gate, the daily cap and the absence of provider credentials are all unchanged. Flagged COMPLIANCE REVIEW REQUIRED — the invoice email is fee-timing copy. Full decision record in docs/CONTRACTS-SPEC.md Part 4 | (this commit)
2026-08-02 | all eight | Contracts now really send, and chase. No new routes: `/api/contracts` gained `run_reminders`, and one workflow joined the registry (`contract-chaser`, 47 -> 48), so every journey page's route totals and role reach are unchanged. What changed underneath them is that the outbound path finally has a CALLER — src/messaging/dispatch.mjs has said in its own header since it was written that "dispatchDue() runs when something calls it, and today nothing does", and that was true of the whole platform, not of contracts in particular. Pressing Send now queues one message per signer carrying that person's own link and hands THOSE ROWS ONLY to the dispatcher | The owner asked why it did not send, and pointed out that chasing is just a workflow. Both fair. Dispatching by id rather than calling dispatchDue() keeps the blast radius to the one contract: draining the whole queue is the switch CLAUDE.md §11 reserves to the owner, and it stays unthrown. The chaser IS registered — which is not the same act, because Inngest invokes nothing until INNGEST_EVENT_KEY is set — and it also runs today without Inngest through the new endpoint and a button on the Contracts screen. The message dispatch sweeper deliberately stays out of the registry and its guard test still passes | (this commit)
2026-08-02 | all eight | The contract generator becomes a signing service. No new routes — the three added earlier the same day gained actions instead (`upload_template`, `save_fields`, `create_client` on `/api/contracts`; file serving and signer lists on `/api/read/contracts`; per-signer links, file fetch and decline on `/api/contracts/sign`), so the route totals and role reach on every journey are unchanged. What moved on these pages is the client-facing surface: `/api/contracts/sign` now serves the DOCUMENT ITSELF to an unauthenticated caller, and remains one of the six genuinely open routes. A PDF can be uploaded, boxes dragged onto its pages, and the whole thing sent to several people who sign IN AN ORDER — under a sequential contract the second signer cannot open the document at all until the first has signed, refused at the endpoint rather than hidden on the screen | The owner asked for DocuSign inside the CRM ("we upload PDFs and do the contract thing... put in fields, like date, name, address, whatever, from the client information") with questions closed, so every remaining decision was taken by the build agent and written into docs/CONTRACTS-SPEC.md Part 2. Two dependencies were added under that grant: pdf-lib to fill and flatten PDFs, and pdf.js vendored into public/ so pages render with no build step and no third-party host. Storage went through src/documents/store.mjs, the same document store every other upload in this codebase already used — an earlier draft of this branch proposed its own competing storage design (a Postgres-backed fallback provider, auto-selected from BLOB_READ_WRITE_TOKEN/DATABASE_URL), which conflicted with the already-merged uploads branch's simpler design and was discarded at merge time rather than kept alongside it (docs/MERGE-LOG.md section 3). Additive throughout: source_kind defaults to 'text', and all 66 tests written for the typed-copy contracts pass unchanged | (this commit)
2026-08-02 | all eight | Three routes added — `POST /api/contracts` (staff write surface: template authoring, drafts, preview, send, void), `GET /api/read/contracts` (the library, the queue, and one contract with its frozen wording) and `GET/POST /api/contracts/sign`. The third is the important one on these pages: it is the second route in the whole platform that takes NO SESSION AT ALL, so it joins `/api/documents/<id>` in the "genuinely open" list on every journey and the open-route count moves from 5 to 6. Auth on it is the HMAC in the link, not a principal — which is why it reads as open here and is not. One new CRM screen (contracts.html, in every sidebar, `ROLE_SETS.STAFF` with an owner/admin-only wording card inside it) and one client page outside the CRM (`/contract.html`, no shell.js, no session) | The contract generator: a staff member writes a contract once, sends it to any client, and gets it back signed, with no developer involved at any step. Built against 030's documents/document_versions rather than a new immutability scheme — the rendered wording is frozen at send and hash-checked at signature against `document_versions.checksum`, so a contract altered after sending cannot be signed at all. Regenerated with `npm run journeys` from the code, per CLAUDE.md §4, not hand-edited. NOTE THE GAP THIS DOES NOT CLOSE: there is no `*-intended.md` anywhere in this directory, so nothing was checked against an intended journey before building — reported rather than reconciled. Full decision record in docs/CONTRACTS-SPEC.md | (this commit)
2026-08-01 | client, role-owner, role-sales-manager, role-closer, role-funding-advisor, role-inquiry-remover, affiliate, white-label | One route added — `GET /api/read/workflows` (STAFF-gated), the workflow registry. automations.html rebuilt to read it live instead of showing 18 hardcoded sample rows; every row now comes from `src/workflows/index.mjs`'s `functions` array (the real 47 Inngest functions), grouped by id-prefix family, with per-row `last_triggered_at` pulled from the `events` table and a `status` of live / never_triggered / dormant. Route total moves to 76 everywhere | The screen had zero data access — none of the 47 real workflows appeared anywhere on it. `engine_active` reports honestly that INNGEST_EVENT_KEY has never been set in this environment (CLAUDE.md §11 — a human must ask for that), so every workflow currently shows `dormant` rather than inventing "went out fine" copy for automations nobody has switched on. The old per-row ON/OFF/PAUSED toggle was dropped: there is no per-workflow enable flag anywhere in the backend, only the one global engine key, so a per-row toggle would have implied a control that does not exist | (this commit)
2026-08-01 | all eight | Every page regenerated after five feature branches were merged into one line of history (messaging foundation, dispatcher + sweeper, delivery webhooks, journey runner, Finance OS). Route total moves to 75: the journey-runner branch adds `POST /api/journeys/run` and the Finance OS branch reshapes the finance screens without adding or removing a route. Role reach numbers move with it | The five branches were generated from three different snapshots of the tree, so each one's `-actual.md` was accurate against its own base and wrong against the merged result. Regenerated from code, per CLAUDE.md §4 — not reconciled by hand, and not taken from any one branch's copy | (this commit)
2026-08-01 | role-owner, role-closer, role-funding-advisor, role-inquiry-remover, affiliate, white-label | Finance OS consolidation: eleven Finance screens (money-map, banking-surface, card-stack, bank-accounts, bills-cashflow, banking-entry, finance-command, finance-add, alerts, deal-model) deleted; finance-os.html rebuilt as the one screen, reading real endpoints (money-map, finance-command, underwrite, the new read/transactions, finance/alerts, finance/entities) with no client-side mock data. subscriptions.html kept (it's Fundhub billing the client, not the client's money) and moved to Setup next to Products & Commissions. Route change driving this regeneration: added GET /api/read/transactions — bank_transactions has had a real writer since the mock banking provider landed and no reader anywhere until now; STAFF-gated and org-scoped like every neighbouring read. Design elements with no real data behind them anywhere in this codebase — a net-worth trend, bureau-score history, a business-credit score, and a five-factor score breakdown — were dropped rather than faked; each is a genuine gap (no history table, no business-bureau integration, no factor-level scoring in the engine), not an oversight | Full non-pg suite green (3520 tests, 0 fail, 2 skip) before commit; DATABASE_URL unset in this sandbox so .pg.test.mjs files could not run here | (this commit)
2026-08-01 | role-sales-manager, role-owner, role-closer, role-funding-advisor, role-inquiry-remover, client, affiliate, white-label | `role-sales-manager` stops being a page that says the role does not exist. The role is BUILT: catalog row, task-routing CHECK widened, demo login, `ROLE_SETS.STAFF` and `ROLE_SETS.FINANCE` membership, shell.js nav and a pipeline.html landing (db/migrations/112_sales_manager_role.sql). Its generated page now carries a real flowchart like every other role. Three guard tests in scripts/journeys/generate.test.mjs were INVERTED rather than deleted — they used to fail if the string `sales_manager` appeared anywhere under src/, api/ or db/, and now fail if any of the five places that must carry it does not, so a half-built role is still caught. Every other role page changed too, because ROLE_SETS.FINANCE widening moves `/api/read/commissions` and its neighbours from a two-role gate to a three-role gate on every page that lists them | This reverses a recorded owner decision (2026-07-31: "do not build it yet, no agent should create this role on its own initiative") and it reverses it ON THE OWNER'S INSTRUCTION — Ticket 8 states "the correct resolution is to build the role, not to remove it from the editor". The guard was about agent initiative; this was directed, so the premise is spent. Commission visibility is company-wide, not own-team-only: an explicit owner answer to the H-6 question the ticket flags as one an agent must not decide (reasoning recorded with it — closers are the only commissioned staff, so the two scopes are currently the same people) | (this commit)
2026-08-01 | role-owner | One route added — `POST /api/journeys/run` (owner/admin only), the journey runner. Walks every branch of every journey with a throwaway client per path, fires the real events into the real workflows against a virtual clock, and reports where intent and code disagree. A Run button and results panel added to journeys.html | Ticket 8. The whole run happens inside a transaction that is always rolled back, so nothing it mints is ever visible to any dashboard — that is a stronger answer to "a synthetic client must never sit next to a real one" than filtering every read path, and it needs no deletes, which CLAUDE.md §11 reserves for the owner. First real walk: 9 paths across the six journeys, 19 of 47 workflows reached, 28 never reached | (this commit)
2026-08-01 | role-owner, role-closer, role-funding-advisor, role-inquiry-remover, client, affiliate, white-label | Finance OS command-center build, phase 3, closing the five gaps left open at the end of phase 2: (1) recurring bills can now be declared manually (`add_manual` on `/api/finance/bills`, 107_recurring_bills_manual.sql adds `source` and relaxes the four detection-only columns to NULL for a manual row rather than faking a detection that never ran) for an account too new to have transaction history; (2) `assign_entity` on `/api/finance/liabilities` lets a credit card be assigned to a wallet, matching what bank accounts already had; (3) alerts now queue the owner a text via a new `owner_notifications` table (108_owner_notifications.sql) on every genuinely new alert — still nothing sends, matching this codebase's existing "queue, never transmit" rule, one text per alert via a unique index on alert_id; (4) the roll-up dashboard gained a combined cash-out-including-marketing-spend chart, computed ONLY when viewing all clients (marketing spend cannot be attributed to one client, so combining it into a single client's view would misattribute cost — the per-client scope still shows marketing separately, with a named reason). No route or nav change on this pass; the two screens from phase 2 gained one panel each | Full suite green (3742 pass, 0 fail, 442 skip — DATABASE_URL unset, same baseline shape as CLAUDE.md's traps note) and lint clean before commit | (this commit)
2026-08-01 | role-owner, role-closer, role-funding-advisor, role-inquiry-remover, client, affiliate, white-label | Two new routes — `GET /api/read/finance-command` (roll-up dashboard across every client/entity) and `POST /api/read/finance-ask` (answers a fixed set of question shapes from real numbers, refuses rather than guesses on anything else) — added to every journey; two new screens, finance-command.html and finance-add.html, added to the shared Finance sidebar in every file that carries one | Finance OS command-center build, phase 2: a roll-up dashboard (cash, credit, investments, utilization, cash-in-vs-out and marketing-spend charts, all real SQL aggregates — no chart library added, inline SVG matching campaign-manager.html's existing pattern) and one add-anything screen (wallet, bank/investment account, credit card) so a client's accounts no longer require a client_id pasted into the URL. Per-card figures come from `tradelines`, not `card_liabilities` — the latter is a separate CRS-observation history and does not hold the live balance; this was caught before it shipped a wrong number. Recurring-expense creation stayed detection-only (Bills & Cash Flow's existing "Redetect") rather than adding an unvalidated manual-entry path. Outbound SMS on alerts, and marketing-spend fold-in beyond this chart, remain undone — see the task report | (this commit)
2026-08-01 | role-owner, role-closer, role-funding-advisor, role-inquiry-remover, client, affiliate, white-label | New route `/api/finance/entities` (create/rename/archive personal or business wallets under a client) added to every journey; pages regenerated by `npm run journeys` | Finance OS command-center build: 106_entities.sql adds an additive `entities` grouping layer over bank_accounts/card_liabilities/recurring_bills so a client's accounts can be split personal vs. business without any existing row needing a value. money-map.html and banking-entry.html were also added to shell.js's client-carry map — both screens already read `client_id` from the URL but were missing from `CLIENT_SCREENS`, so the sidebar never carried a client onto them and both always landed on "no client — paste one in" | (this commit)
2026-08-01 | role-owner | One route added — `GET/PUT /api/journeys` (owner/admin only) | The Journeys editor's "Apply to code" button used to just show a success toast and forget everything on reload — nothing was saved anywhere. Added a `journeys` table (db/migrations/113_journeys.sql, org-scoped like message_templates) and this route so Apply really writes the current step tree to the database, and the editor loads that saved state back on open instead of always starting from the built-in example. Also added the editor to the standard sidebar (public/app/journeys.html now loads shell.js like every other screen) so it has a real way in and a real way out, and to shell.js's OWNER_ADMIN_ONLY list to match the route's gate | (this commit)
2026-08-01 | role-owner | One route added — `POST /api/journeys/ask` (owner/admin only) | Built a real server-side proxy for the Journeys editor's "describe the change" box (public/app/journeys.html). It used to call api.anthropic.com straight from the browser, which cannot hold a secret key; the key now lives server-side and the browser calls this route instead, gated the same as every other authoring endpoint | (this commit)
2026-08-01 | role-owner, role-closer, role-funding-advisor, role-inquiry-remover, client, affiliate, white-label | Two routes added to every journey — `POST /api/auth/reset` (open — no session, matches `/api/auth/login`) and `POST /api/auth/admin-reset` (owner/admin only) | Login was blocked for the owner; built staff password reset (nine role test logins, staff-screen admin reset, forgot-password on the login page) to fix it and stop it recurring. First draft mixed public and admin-gated actions in one handler and the extractor correctly reported it `unverified` rather than guess — split into two files, one gate each, so both routes trace cleanly | (this commit)
2026-08-01 | role-owner, role-closer, role-funding-advisor, role-inquiry-remover, client, affiliate, white-label | Eight `/api/finance/*` routes now report the roles that gate them instead of "unverified"; sidebar Finance group carries all eight finance screens | Merging the W1-W10 audit branch brought the eight handlers in. Each aliases a shared set (`const CARD_ROLES = ROLE_SETS.FINANCE`) and the extractor followed only the two halves, never the alias, so every one drew a gate it could not see. Alias resolution added to scripts/journeys/extract.mjs | (this commit)
2026-08-01 | role-owner, role-closer, role-funding-advisor, role-inquiry-remover, client, affiliate, white-label | Two routes added to every journey — `GET /api/read/money-map` (owner/admin/funding_advisor/closer/inquiry_specialist/setter) and `POST /api/banking/sync-accounts` (owner/admin only) | The Money Map screen and the first writer `bank_accounts` has ever had; pages regenerated by `npm run journeys` rather than hand-edited, and the hand-written role-owner page from that branch was dropped in favour of the generated one | (this commit)
2026-07-31 | role-owner, role-closer, role-funding-advisor, role-inquiry-remover, client, affiliate, white-label | `/api/partner-brand` moved from "any signed-in employee" to owner/admin on every page | The journeys found the read was ungated — any employee could read a partner's trading name and registered address. Gated to owner+admin; pages regenerated | (this commit)
2026-07-31 | role-sales-manager | Page now records the owner's decision: the role is FUTURE WORK, stays tracked in CLAUDE.md §4, and must not be built yet | Owner confirmed the role is planned (Sarah runs sales), not dropped — the page previously asked which of two things was true | (this commit)
2026-07-31 | role-owner, role-closer, role-funding-advisor, role-inquiry-remover, client, affiliate, white-label | Two routes added to every journey — `/api/banking/revoke` and `/api/privacy/erasure`, both owner/admin only | The banking revoke + erasure work (PR #59) added them; the staleness guard caught the drift on merge and the pages were regenerated rather than hand-edited | (this commit)
2026-07-31 | white-label | First actual journey: the `partner` kind reaches 18 of 51 routes — campaigns, Creative Factory, its own brand tokens | No journey documentation existed for any flow; owner asked for actual-files only | (this commit)
2026-07-31 | affiliate | First actual journey: the `affiliate` kind reaches only the open routes — **no endpoint anywhere admits an affiliate** | Generated from the routing table; recorded as a finding rather than smoothed over | (this commit)
2026-07-31 | role-inquiry-remover | First actual journey for `inquiry_specialist`, the role behind the Inquiry Remover screen | Journey is named for the product, the role for the database column — mapping written down so nobody re-derives it | (this commit)
2026-07-31 | role-funding-advisor | First actual journey for `funding_advisor` | As above | (this commit)
2026-07-31 | role-closer | First actual journey for `closer` | As above | (this commit)
2026-07-31 | role-sales-manager | Page created stating the role DOES NOT EXIST in the code; no diagram drawn | CLAUDE.md §4 tracks the journey and nothing in `src/`, `api/` or `db/` defines the role. Drawing one would be inventing it | (this commit)
2026-07-31 | role-owner | First actual journey for `owner` | As above | (this commit)
2026-07-31 | client | First actual journey: the `client` kind reaches 9 of 51 routes, of which 7 are open or signature-verified rather than client-specific | Generated from the routing table and each handler's gate | (this commit)
2026-07-31 | role-owner | First traced flow: adding bank accounts and cards by hand, plus the mock import | `docs/journeys/` did not exist; W1 built the first writer for `bank_accounts` and traced what it does | (this commit)
2026-07-31 | client | Added the soft-pull consent segment to a NEW client-actual.md — capture, revoke, and the gate on requestSoftPull | Migration 099 made consent a real record instead of a CRM text field; the flow had no diagram because docs/journeys/ did not exist | (this commit)
2026-07-31 | role-owner | Created `role-owner-actual.md` — traced the Money Map flow end to end from code; every other owner path marked UNVERIFIED | The Money Map screen was built and a flow with no diagram is a flow nobody can check; the rest was not read so it was not drawn | (this commit)
2026-07-31 | (all eight) | **No `-intended.md` file exists for any tracked journey.** `docs/journeys/` did not exist until this commit | Recorded as a finding, not filled in: CLAUDE.md §4 says intended journeys are hand-authored and agents do not write them | (this commit)
2026-08-21 | client, role-owner, role-sales-manager, role-closer, role-funding-advisor, role-inquiry-remover, affiliate, white-label | One new staff route: `POST /api/pipeline-clients`. Same gate as pipeline card moves (staff, clock-in, owner skip). It finds or makes the client the same way the apply form does (`resolveClient` then `entry.captured`). No new screen. Pipeline now has a New Client box on the board it already had. No `-intended.md` edited. Every generated page moves 188 routes to 189. | Staff had no way to add a client except the funnel | (this commit)
