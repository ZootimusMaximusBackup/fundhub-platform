# Journey changelog

One line per journey change, newest at top. This is the human-readable record —
including when a change made a journey worse.

```
YYYY-MM-DD | <journey> | <what changed> | <why> | <commit>
```

Hand-maintained. The `-actual.md` pages next to it are generated (`npm run journeys`);
this file is not, because "what changed and why" is a judgement nobody can generate.

---

2026-08-02 | all eight | Three routes added — `POST /api/contracts` (staff write surface: template authoring, drafts, preview, send, void), `GET /api/read/contracts` (the library, the queue, and one contract with its frozen wording) and `GET/POST /api/contracts/sign`. The third is the important one on these pages: it is the second route in the whole platform that takes NO SESSION AT ALL, so it joins `/api/documents/<id>` in the "genuinely open" list on every journey and the open-route count moves from 5 to 6. Auth on it is the HMAC in the link, not a principal — which is why it reads as open here and is not. One new CRM screen (contracts.html, in every sidebar, `ROLE_SETS.STAFF` with an owner/admin-only wording card inside it) and one client page outside the CRM (`/contract.html`, no shell.js, no session) | The contract generator: a staff member writes a contract once, sends it to any client, and gets it back signed, with no developer involved at any step. Built against 030's documents/document_versions rather than a new immutability scheme — the rendered wording is frozen at send and hash-checked at signature against `document_versions.checksum`, so a contract altered after sending cannot be signed at all. Regenerated with `npm run journeys` from the code, per CLAUDE.md §4, not hand-edited. NOTE THE GAP THIS DOES NOT CLOSE: there is no `*-intended.md` anywhere in this directory, so nothing was checked against an intended journey before building — reported rather than reconciled. Full decision record in docs/CONTRACTS-SPEC.md | (this commit)

2026-08-01 | client, role-owner, role-sales-manager, role-closer, role-funding-advisor, role-inquiry-remover, affiliate, white-label | One route added — `GET /api/read/workflows` (STAFF-gated), the workflow registry. automations.html rebuilt to read it live instead of showing 18 hardcoded sample rows; every row now comes from `src/workflows/index.mjs`'s `functions` array (the real 47 Inngest functions), grouped by id-prefix family, with per-row `last_triggered_at` pulled from the `events` table and a `status` of live / never_triggered / dormant. Route total moves to 76 everywhere | The screen had zero data access — none of the 47 real workflows appeared anywhere on it. `engine_active` reports honestly that INNGEST_EVENT_KEY has never been set in this environment (CLAUDE.md §11 — a human must ask for that), so every workflow currently shows `dormant` rather than inventing "went out fine" copy for automations nobody has switched on. The old per-row ON/OFF/PAUSED toggle was dropped: there is no per-workflow enable flag anywhere in the backend, only the one global engine key, so a per-row toggle would have implied a control that does not exist | (this commit)

2026-08-01 | all eight | Every page regenerated after five feature branches were merged into one line of history (messaging foundation, dispatcher + sweeper, delivery webhooks, journey runner, Finance OS). Route total moves to 75: the journey-runner branch adds `POST /api/journeys/run` and the Finance OS branch reshapes the finance screens without adding or removing a route. Role reach numbers move with it | The five branches were generated from three different snapshots of the tree, so each one's `-actual.md` was accurate against its own base and wrong against the merged result. Regenerated from code, per CLAUDE.md §4 — not reconciled by hand, and not taken from any one branch's copy | (this commit)
2026-08-01 | role-owner, role-closer, role-funding-advisor, role-inquiry-remover, affiliate, white-label | Finance OS consolidation: eleven Finance screens (money-map, banking-surface, card-stack, bank-accounts, bills-cashflow, banking-entry, finance-command, finance-add, alerts, deal-model) deleted; finance-os.html rebuilt as the one screen, reading real endpoints (money-map, finance-command, underwrite, the new read/transactions, finance/alerts, finance/entities) with no client-side mock data. subscriptions.html kept (it's Fundhub billing the client, not the client's money) and moved to Setup next to Products & Commissions. Route change driving this regeneration: added GET /api/read/transactions — bank_transactions has had a real writer since the mock banking provider landed and no reader anywhere until now; STAFF-gated and org-scoped like every neighbouring read. Design elements with no real data behind them anywhere in this codebase — a net-worth trend, bureau-score history, a business-credit score, and a five-factor score breakdown — were dropped rather than faked; each is a genuine gap (no history table, no business-bureau integration, no factor-level scoring in the engine), not an oversight | Full non-pg suite green (3520 tests, 0 fail, 2 skip) before commit; DATABASE_URL unset in this sandbox so .pg.test.mjs files could not run here | (this commit)
2026-08-01 | role-sales-manager, role-owner, role-closer, role-funding-advisor, role-inquiry-remover, client, affiliate, white-label | `role-sales-manager` stops being a page that says the role does not exist. The role is BUILT: catalog row, task-routing CHECK widened, demo login, `ROLE_SETS.STAFF` and `ROLE_SETS.FINANCE` membership, shell.js nav and a pipeline.html landing (db/migrations/112_sales_manager_role.sql). Its generated page now carries a real flowchart like every other role. Three guard tests in scripts/journeys/generate.test.mjs were INVERTED rather than deleted — they used to fail if the string `sales_manager` appeared anywhere under src/, api/ or db/, and now fail if any of the five places that must carry it does not, so a half-built role is still caught. Every other role page changed too, because ROLE_SETS.FINANCE widening moves `/api/read/commissions` and its neighbours from a two-role gate to a three-role gate on every page that lists them | This reverses a recorded owner decision (2026-07-31: "do not build it yet, no agent should create this role on its own initiative") and it reverses it ON THE OWNER'S INSTRUCTION — Ticket 8 states "the correct resolution is to build the role, not to remove it from the editor". The guard was about agent initiative; this was directed, so the premise is spent. Commission visibility is company-wide, not own-team-only: an explicit owner answer to the H-6 question the ticket flags as one an agent must not decide (reasoning recorded with it — closers are the only commissioned staff, so the two scopes are currently the same people) | (this commit)
2026-08-01 | role-owner | One route added — `POST /api/journeys/run` (owner/admin only), the journey runner. Walks every branch of every journey with a throwaway client per path, fires the real events into the real workflows against a virtual clock, and reports where intent and code disagree. A Run button and results panel added to journeys.html | Ticket 8. The whole run happens inside a transaction that is always rolled back, so nothing it mints is ever visible to any dashboard — that is a stronger answer to "a synthetic client must never sit next to a real one" than filtering every read path, and it needs no deletes, which CLAUDE.md §11 reserves for the owner. First real walk: 9 paths across the six journeys, 19 of 47 workflows reached, 28 never reached | (this commit)
2026-08-01 | role-owner, role-closer, role-funding-advisor, role-inquiry-remover, client, affiliate, white-label | Finance OS command-center build, phase 3, closing the five gaps left open at the end of phase 2: (1) recurring bills can now be declared manually (`add_manual` on `/api/finance/bills`, 107_recurring_bills_manual.sql adds `source` and relaxes the four detection-only columns to NULL for a manual row rather than faking a detection that never ran) for an account too new to have transaction history; (2) `assign_entity` on `/api/finance/liabilities` lets a credit card be assigned to a wallet, matching what bank accounts already had; (3) alerts now queue the owner a text via a new `owner_notifications` table (108_owner_notifications.sql) on every genuinely new alert — still nothing sends, matching this codebase's existing "queue, never transmit" rule, one text per alert via a unique index on alert_id; (4) the roll-up dashboard gained a combined cash-out-including-marketing-spend chart, computed ONLY when viewing all clients (marketing spend cannot be attributed to one client, so combining it into a single client's view would misattribute cost — the per-client scope still shows marketing separately, with a named reason). No route or nav change on this pass; the two screens from phase 2 gained one panel each | Full suite green (3742 pass, 0 fail, 442 skip — DATABASE_URL unset, same baseline shape as CLAUDE.md's traps note) and lint clean before commit | (this commit)
2026-08-01 | role-owner, role-closer, role-funding-advisor, role-inquiry-remover, client, affiliate, white-label | Two new routes — `GET /api/read/finance-command` (roll-up dashboard across every client/entity) and `POST /api/read/finance-ask` (answers a fixed set of question shapes from real numbers, refuses rather than guesses on anything else) — added to every journey; two new screens, finance-command.html and finance-add.html, added to the shared Finance sidebar in every file that carries one | Finance OS command-center build, phase 2: a roll-up dashboard (cash, credit, investments, utilization, cash-in-vs-out and marketing-spend charts, all real SQL aggregates — no chart library added, inline SVG matching campaign-manager.html's existing pattern) and one add-anything screen (wallet, bank/investment account, credit card) so a client's accounts no longer require a client_id pasted into the URL. Per-card figures come from `tradelines`, not `card_liabilities` — the latter is a separate CRS-observation history and does not hold the live balance; this was caught before it shipped a wrong number. Recurring-expense creation stayed detection-only (Bills & Cash Flow's existing "Redetect") rather than adding an unvalidated manual-entry path. Outbound SMS on alerts, and marketing-spend fold-in beyond this chart, remain undone — see the task report | (this commit)
2026-08-01 | role-owner, role-closer, role-funding-advisor, role-inquiry-remover, client, affiliate, white-label | New route `/api/finance/entities` (create/rename/archive personal or business wallets under a client) added to every journey; pages regenerated by `npm run journeys` | Finance OS command-center build: 106_entities.sql adds an additive `entities` grouping layer over bank_accounts/card_liabilities/recurring_bills so a client's accounts can be split personal vs. business without any existing row needing a value. money-map.html and banking-entry.html were also added to shell.js's client-carry map — both screens already read `client_id` from the URL but were missing from `CLIENT_SCREENS`, so the sidebar never carried a client onto them and both always landed on "no client — paste one in" | (this commit)
2026-08-01 | role-owner | One route added — `GET/PUT /api/journeys` (owner/admin only) | The Journeys editor's "Apply to code" button used to just show a success toast and forget everything on reload — nothing was saved anywhere. Added a `journeys` table (db/migrations/113_journeys.sql, org-scoped like message_templates) and this route so Apply really writes the current step tree to the database, and the editor loads that saved state back on open instead of always starting from the built-in example. Also added the editor to the standard sidebar (public/app/journeys.html now loads shell.js like every other screen) so it has a real way in and a real way out, and to shell.js's OWNER_ADMIN_ONLY list to match the route's gate | (this commit)
2026-08-01 | role-owner | One route added — `POST /api/journeys/ask` (owner/admin only) | Built a real server-side proxy for the Journeys editor's "describe the change" box (public/app/journeys.html). It used to call api.anthropic.com straight from the browser, which cannot hold a secret key; the key now lives server-side and the browser calls this route instead, gated the same as every other authoring endpoint | (this commit)
2026-08-01 | role-owner, role-closer, role-funding-advisor, role-inquiry-remover, client, affiliate, white-label | Two routes added to every journey — `POST /api/auth/reset` (open — no session, matches `/api/auth/login`) and `POST /api/auth/admin-reset` (owner/admin only) | Login was blocked for the owner; built staff password reset (nine role test logins, staff-screen admin reset, forgot-password on the login page) to fix it and stop it recurring. First draft mixed public and admin-gated actions in one handler and the extractor correctly reported it `unverified` rather than guess — split into two files, one gate each, so both routes trace cleanly | (this commit)
2026-08-01 | role-owner, role-closer, role-funding-advisor, role-inquiry-remover, client, affiliate, white-label | Eight `/api/finance/*` routes now report the roles that gate them instead of "unverified"; sidebar Finance group carries all eight finance screens | Merging the W1-W10 audit branch brought the eight handlers in. Each aliases a shared set (`const CARD_ROLES = ROLE_SETS.FINANCE`) and the extractor followed only the two halves, never the alias, so every one drew a gate it could not see. Alias resolution added to scripts/journeys/extract.mjs | (this commit)
2026-08-01 | role-owner, role-closer, role-funding-advisor, role-inquiry-remover, client, affiliate, white-label | Two routes added to every journey — `GET /api/read/money-map` (owner/admin/funding_advisor/closer/inquiry_specialist/setter) and `POST /api/banking/sync-accounts` (owner/admin only) | The Money Map screen and the first writer `bank_accounts` has ever had; pages regenerated by `npm run journeys` rather than hand-edited, and the hand-written role-owner page from that branch was dropped in favour of the generated one | (this commit)
2026-07-31 | role-owner, role-closer, role-funding-advisor, role-inquiry-remover, client, affiliate, white-label | `/api/partner-brand` moved from "any signed-in employee" to owner/admin on every page | The journeys found the read was ungated — any employee could read a partner's trading name and registered address. Gated to owner+admin; pages regenerated | (this commit)
2026-07-31 | role-sales-manager | Page now records the owner's decision: the role is FUTURE WORK, stays tracked in CLAUDE.md §4, and must not be built yet | Owner confirmed the role is planned (Sarah runs sales), not dropped — the page previously asked which of two things was true | (this commit)
2026-07-31 | role-owner, role-closer, role-funding-advisor, role-inquiry-remover, client, affiliate, white-label | Two routes added to every journey — `/api/banking/revoke` and `/api/privacy/erasure`, both owner/admin only | The banking revoke + erasure work (PR #59) added them; the staleness guard caught the drift on merge and the pages were regenerated rather than hand-edited | (this commit)
2026-07-31 | white-label | First actual journey: the `partner` kind reaches 18 of 51 routes — campaigns, Creative Factory, its own brand tokens | No journey documentation existed for any flow; owner asked for actual-files only | (this commit)
2026-07-31 | affiliate | First actual journey: the `affiliate` kind reaches only the open routes — **no endpoint anywhere admits an affiliate** | Generated from the routing table; recorded as a finding rather than smoothed over | (this commit)
2026-07-31 | role-inquiry-remover | First actual journey for `inquiry_specialist`, the role behind the Inquiry Remover screen | Journey is named for the product, the role for the database column — mapping written down so nobody re-derives it | (this commit)
2026-07-31 | role-funding-advisor | First actual journey for `funding_advisor` | As above | (this commit)
2026-07-31 | role-closer | First actual journey for `closer` | As above | (this commit)
2026-07-31 | role-sales-manager | Page created stating the role DOES NOT EXIST in the code; no diagram drawn | CLAUDE.md §4 tracks the journey and nothing in `src/`, `api/` or `db/` defines the role. Drawing one would be inventing it | (this commit)
2026-07-31 | role-owner | First actual journey for `owner` | As above | (this commit)
2026-07-31 | client | First actual journey: the `client` kind reaches 9 of 51 routes, of which 7 are open or signature-verified rather than client-specific | Generated from the routing table and each handler's gate | (this commit)
2026-07-31 | role-owner | First traced flow: adding bank accounts and cards by hand, plus the mock import | `docs/journeys/` did not exist; W1 built the first writer for `bank_accounts` and traced what it does | (this commit)
2026-07-31 | client | Added the soft-pull consent segment to a NEW client-actual.md — capture, revoke, and the gate on requestSoftPull | Migration 099 made consent a real record instead of a CRM text field; the flow had no diagram because docs/journeys/ did not exist | (this commit)
2026-07-31 | role-owner | Created `role-owner-actual.md` — traced the Money Map flow end to end from code; every other owner path marked UNVERIFIED | The Money Map screen was built and a flow with no diagram is a flow nobody can check; the rest was not read so it was not drawn | (this commit)
2026-07-31 | (all eight) | **No `-intended.md` file exists for any tracked journey.** `docs/journeys/` did not exist until this commit | Recorded as a finding, not filled in: CLAUDE.md §4 says intended journeys are hand-authored and agents do not write them | (this commit)
