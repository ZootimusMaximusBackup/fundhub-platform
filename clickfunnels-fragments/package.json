{
  "name": "fundhub-clickfunnels-fragments",
  "private": true,
  "type": "module",
  "scripts": {
    "harness": "node harness/build.mjs",
    "serve": "npm run harness && node harness/static-server.mjs",
    "test": "npm run harness && playwright test",
    "test:update": "npm run harness && playwright test --update-snapshots"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2"
  }
}
